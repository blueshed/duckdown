title: Gallery
nav: Gallery
order: 3
description: A collection — four works, written once in collection.json.

# Gallery

Every work below is a page of its own, and none was written by hand: the
works are data, in `collection.json`, and `item.md` beside it is the one page
every work gets. The same four works appear [by year](/by-year.html), grouped
by a field instead of by section.

{{items}}
