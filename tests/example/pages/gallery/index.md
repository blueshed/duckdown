title: Gallery
nav: Gallery
order: 3
description: A collection — four works, written once in collection.json.

# Gallery

Every work below is a page of its own, written by `collection.json` rather than
by hand. The same four works appear [by year](/by-year.html), grouped by a
field instead of by section.

{{items}}
