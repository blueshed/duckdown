title: Site map
description: Every page on this site, by folder.

# Site map

Every page, nested by folder, with `{{sitemap}}`. It is the same walk as
`{{pages}}`, made recursive, so drafts, the 404 page and folders starting with
`-` stay out of it.

{{sitemap}}
