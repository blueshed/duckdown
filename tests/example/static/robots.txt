# Crawlers ask for /robots.txt at the root, and duckdown answers it from here.
# Everything is allowed. To ban all spiders, replace the Allow line with:
#   Disallow: /
# To point them at the sitemap, add its full address, which only you know:
#   Sitemap: https://example.com/sitemap.xml
User-agent: *
Allow: /
