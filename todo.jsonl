{"n": 1, "status": "fixed", "severity": "bug", "area": "login", "file": "server/edit/login.html", "summary": "The login page has no stylesheet: borderless inputs and a Sign In button that is white on white", "detail": "login.html:7 links ./styles.css, which resolves to /styles.css. Nothing serves that path, so it falls through to handleSite and 404s, and every var(--...) in the page's own <style> is undefined (Enter still submits the form). Already in HEAD, not new in 0.0.2. auth.ts reads login.html from disk to run it through HTMLRewriter, so Bun never bundles its link as it does for /edit. Options: serve server/edit/styles.css at a stable path from main.ts and point the link there; or inline it in renderLogin. The login page's own rules are more specific than styles.css's globals (the * reset, button), so those don't fight. Found 19 Sep 2026 setting up the preview; Peter spotted it too.", "note": "19 Sep 2026: login.html links /edit/styles.css, served by a Bun.file route in main.ts (Bun 1.4 routes take a BunFile). A test checks the link and the route (200, text/css). In the pane on 127.0.0.1 (its own cookie jar, so signed out) the login card has bordered inputs and a visible Sign In button."}
{"n": 2, "status": "fixed", "severity": "bug", "area": "auth", "file": "server/main.ts, server/edit/components/Browser.tsx, server/edit/store.ts", "summary": "/edit shows the editor to anyone: signed out, you get an empty file list instead of the login page", "detail": "main.ts:15 serves the /edit HTML import with no auth check. The client's first fetch, /edit/pages/, is redirected to /login?next=%2Fedit%2Fpages%2F; fetch follows it, res.ok is true on the login HTML, res.json() throws, and Browser.tsx:17 swallows the error. store.ts loadFile has the same blind spot (with ?path= it would load the login page's HTML into the editor), as does ImageBrowser.tsx. The next it carries is the API path, not /edit. Already in HEAD. Fix shape: requireAuth answers fetch requests (no text/html in Accept) with 401, and one shared client fetch sends the page to /login?next=<current URL> on a 401; server.test.ts:112 pins today's 302 for API calls. First check whether Bun 1.4 lets a handler guard an HTML import directly, which would be simpler.", "note": "19 Sep 2026: requireAuth answers fetches with 401 and still redirects page loads (Accept: text/html); server/edit/api.ts wraps every editor fetch and sends the page to /login?next=<path+search> on a 401. /edit stays an unguarded HTML import (a handler can't serve an HTMLBundle; it only exposes index and files), so the editor's first request does the redirect and the empty shell shows for a moment first. The 302 test became two: 401 for a fetch, 302 with next for a page load. In the pane, signed-out /edit?path=index.md landed on /login with next=/edit?path=index.md."}
{"n": 3, "status": "fixed", "severity": "bug", "area": "preview", "file": "server/edit/components/Preview.tsx, server/edit/components/CssPreview.tsx, CHANGELOG.md", "summary": "The uncommitted 0.0.2 sandbox starves both previews: no site.css, no -theme.css, no images", "detail": "Preview.tsx:55 and CssPreview.tsx:37 gained sandbox=\"allow-scripts\". Without allow-same-origin the srcdoc document's origin is null, and in the preview pane's browser every subresource it asks for fails, public or not: /static/site.css, /edit/pages/-theme.css (which would also lose the SameSite=Lax cookie) and /static/images/*. So the markdown preview renders in Times with no theme and broken images, and the CSS preview loses site.css and its sample logo (the CSS being edited is inlined, so that part still shows). Fix shape, tested: sandbox=\"allow-same-origin\" alone. Scripts still cannot run, which is the point of the 0.0.2 fix; the frame shares the editor's origin again, so both stylesheets, the cookie and images load. Then drop the now-inert onerror=\"this.remove()\" at Preview.tsx:45 (a missing theme is just a failed <link>) and reword CHANGELOG.md:10. Never allow-scripts together with allow-same-origin: the frame could lift its own sandbox.", "note": "19 Sep 2026: fixed as tested: sandbox=\"allow-same-origin\" on both iframes, the inert onerror removed, the CHANGELOG entry rewritten. In the demo the preview rendered with site.css and loaded /static/images/logo.svg."}
{"n": 4, "status": "fixed", "severity": "ops", "area": "server", "file": "server/main.ts, server/config.ts", "summary": "The server needs a pid file", "detail": "Asked for by Peter, 19 Sep 2026. Purpose to confirm (stopping a stray dev server, or a process manager in production?): it decides the default path and whether a pid file naming a live process (process.kill(pid, 0)) should stop a second server starting. Shape: write process.pid at startup and remove it on exit, SIGINT and SIGTERM; path from an env var in config.ts; the code in its own module, since main.ts is resources and routes only. Care points: bun --hot re-runs main.ts in the same process on every reload, so exit handlers must be registered once per process, not once per reload; bun test loads .env (checked) and server.test.ts spawns its server with the runner's env, so the test server must not overwrite or delete the dev server's pid file; gitignore the file.", "note": "19 Sep 2026: purpose never confirmed, so built as both a handle and a lock. server/pid.ts writes ./duckdown.pid before listening (DUCKDOWN_PID moves it, empty turns it off), refuses to start if it names another live process, and removes it on exit, SIGINT and SIGTERM (registered once per process for --hot). *.pid is gitignored; tests give each server its own pid file and cover write, refusal and removal. Proved in use the same day: the desktop app reported the preview server stopped while bun run dev and the server still held :8080; kill $(cat duckdown.pid) stopped it, the file went and the port freed."}
{"n": 5, "status": "fixed", "severity": "low", "area": "dev", "file": ".env, tests/example", "summary": "The dev server, and so the preview, edits the tracked seed site the tests copy", "detail": ".env sets DUCKDOWN_PATH=./tests/example, so every save, upload and delete made in the editor lands in tracked files, and server.test.ts copies tests/example as its fixture. Options: point dev at a gitignored working copy seeded from tests/example (as create/setup.ts seeds ./site for a new site); or keep it and check git status before committing.", "note": "19 Sep 2026: new DUCKDOWN_SEED: when DUCKDOWN_PATH doesn't exist, the first run copies the seed there (storage.ts seedLocalSite, called from main.ts). The local .env now has DUCKDOWN_PATH=./.dev-site and DUCKDOWN_SEED=./tests/example; .dev-site/ is gitignored; a test covers seeding. Removed the first demo's tests/example/pages/hello.md. In the second demo every save landed in .dev-site and tests/example stayed clean. .env is untracked, so another clone needs the same two lines (CLAUDE.md, Storage)."}
{"n": 6, "status": "fixed", "severity": "low", "area": "docs", "file": "feature.md, CLAUDE.md", "summary": "feature.md and CLAUDE.md lag the code, and feature.md now overlaps this file", "detail": "feature.md still describes the Crank.js rewrite (the client is Railroad), and one of its 14 open boxes, password encryption, is covered by 0.0.2's hashing. CLAUDE.md still says Feather icons (lines 61 and 84); 0.0.2 moved to lucide-static. Decide for feature.md: fold its open items in here and retire it, or keep it as the feature map and tick it alongside each CHANGELOG entry. create/setup.ts strips both feature.md and todo.jsonl from scaffolded sites.", "note": "19 Sep 2026: kept feature.md as the feature map (it now says so, and points bugs and tasks here) and brought it up to date: Railroad, Lucide, password hashing ticked, the new features. CLAUDE.md: Lucide, api.ts, pid.ts and .dev-site in the tree, the 401/redirect split, DUCKDOWN_SEED and DUCKDOWN_PID, and the Railroad section (text() is gone; keyed list() rows are signals; no bare catch around .set()). README mentions the pid file."}
{"n": 7, "status": "fixed", "severity": "bug", "area": "editor", "file": "server/edit/components/Browser.tsx, server/edit/components/ImageBrowser.tsx", "summary": "The editor's file list is empty: railroad 0.11's keyed list() hands each row a Signal, and Browser.tsx reads it as a plain item", "detail": "Found 19 Sep 2026 demoing the editor, signed in: /edit/pages/ answers 200 with index.md, -theme.css and guide/, yet the list shows no rows. In 0.11.0's keyed form list(items, keyFn, render), render receives ReadonlySignal<T> (node_modules/@blueshed/railroad/jsx.ts, the list() overloads), so f.name is undefined and f.name.endsWith(...) throws. After its first render list() re-syncs synchronously inside files.set(), so the throw lands in load()'s bare catch {}: no console error, and folders.set() never runs. ImageBrowser.tsx has the same keyed rows (f.name; f.path in the thumbnail src and onclick), so expect rows there with no names and a src of /edit/browseundefined (not yet seen). New in the uncommitted 0.0.2 upgrade from 0.1.1; the server tests can't see it. Fix shape: read the row signal in each keyed render (f$.peek() in handlers, f$.map(...) for text), or use the index-based list(items, (f) => ...) form; and stop the empty catch hiding render errors. While it's broken, hasTheme is never set (it comes after the throwing files.set), so New offers Theme even where a -theme.css exists (seen at the root, 19 Sep 2026); choosing it overwrites that file (todo 8).", "note": "19 Sep 2026: Browser.tsx and ImageBrowser.tsx read rows with f$.map(...) and f$.peek(); both load()s moved the signal updates out of the try, so a render error now surfaces (one did, from a half-edited hot reload). In the pane: the list shows guide, -theme.css and index.md; New offers no Theme where one exists; the image sidebar shows names and loaded thumbnails."}
{"n": 8, "status": "fixed", "severity": "bug", "area": "editor", "file": "server/edit/store.ts, server/edit/components/Browser.tsx, server/routes/pages.ts", "summary": "New overwrites an existing file without asking: a page named after an existing one replaces it with a stub", "detail": "Browser.tsx onCreate (line 25) hands every choice to store.ts createFile (line 27), which PUTs a template (title: <name>, or the -theme.css stub) straight to /edit/pages/<path>, and the PUT in pages.ts writes whatever it gets. Nothing checks for an existing file, so New > Page 'index' at the root replaces index.md, and New > Theme where a -theme.css exists (offered while todo 7 is open) replaces the theme. From reading the code, not reproduced: reproducing it destroys the file. Fix shape: createFile checks the name against the listing the browser already holds and refuses (or asks); belt and braces, a create-only PUT on the server, e.g. If-None-Match: * answered with 412 when the file exists.", "note": "19 Sep 2026: PUT /edit/pages/* with If-None-Match: * answers 412 when the file exists; createFile sends it and the New dialog shows '<path> already exists' and stays open. Tests cover the 412 and the create. In the demo a second New > Folder 'hello' showed 'hello/index.md already exists' and the saved page was untouched."}
{"n": 9, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/Browser.tsx", "summary": "New > Folder titled the folder's index.md 'index', so its nav entry read 'index'", "detail": "createFile took its title from the file name it was given, and the folder branch passed 'index.md'. buildNav shows an index.md's nav or else its title, so every new folder joined the nav as 'index'.", "note": "19 Sep 2026: the folder branch now passes the folder's name, so hello/index.md starts as 'title: hello'. Seen in the second demo."}
{"n": 10, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/Editor.tsx, server/edit/components/ImageBrowser.tsx", "summary": "Icon-only buttons have no accessible name: the editor's Delete and the image sidebar's Close", "detail": "Found 19 Sep 2026 in the second demo: find() couldn't locate the sidebar's close button, and a scan found the editor's trash button (class danger-subtle) unnamed too. Screen readers announce them as just 'button', and an agent can only click them by position. Fix: aria-label (and title, for a tooltip) on both, e.g. 'Delete page' and 'Close images'.", "note": "19 Sep 2026: aria-label (and title) on the editor's Delete file, the image sidebar's Close images and the notice's Dismiss; Icon's span is aria-hidden, since every button now carries text or a label. In the pane, find() locates all three by name and no button on the page is unnamed; editor.test.tsx clicks them by label."}
{"n": 11, "status": "fixed", "severity": "bug", "area": "editor", "file": "server/edit/store.ts, server/edit/components/Editor.tsx", "summary": "Save flashes 'Saved' even when the save failed", "detail": "From reading the code 19 Sep 2026: Editor.tsx save() awaits saveFile() and then always clears dirty and flashes 'Saved'; saveFile() ignores the PUT's status, and a network error rejects past both. A 500 or a dropped connection therefore reads as saved, and closing the tab loses the work (a 401 now goes to login via api.ts). Fix shape: saveFile returns whether res.ok (false on a throw); save() keeps dirty and flashes 'Not saved' (button.danger) when it didn't.", "note": "19 Sep 2026: saveFile and deleteFile return whether they worked. A failed save keeps the edit dirty and flashes 'Not saved' (button.danger) while the notice gives the reason; a failed delete leaves the file open. Covered in editor.test.tsx (a 507 staged with intercept())."}
{"n": 12, "status": "fixed", "severity": "low", "area": "auth", "file": "server/auth.ts, server/edit/components/Header.tsx", "summary": "Logout is a GET, so any page on the site can sign an editor out", "detail": "From reading the code 19 Sep 2026: handleLogout clears the cookie on GET /logout, so <img src=\"/logout\"> in a page (or its preview) signs out whoever views it. Editors write the pages, so the harm is small; a cross-site page probably can't do it (Chrome won't let a cross-site subresource response set the Lax cookie), not tested. Fix shape: POST-only /logout; the header's Logout link becomes a small form button.", "note": "19 Sep 2026: /logout is POST-only ({ POST: handleLogout } in main.ts) and refuses Sec-Fetch-Site: cross-site with 403 (Sec-Fetch-Site rather than an Origin/Host comparison, which a proxy rewriting Host would break). The header's Logout is a form styled like the links beside it. Tests: POST clears the cookie, cross-site is refused, GET signs nobody out."}
{"n": 13, "status": "fixed", "severity": "low", "area": "docs", "file": "tests/example/pages/guide/pages.md, server/markdown.ts", "summary": "The guide says nav: puts any page in the navigation, but only index.md pages are read", "detail": "guide/pages.md's table says nav 'Adds this page to the site navigation'; buildNav only reads index.md files (their nav, else their title). Found 19 Sep 2026 while planning the second demo, which used a folder for that reason. Decide: correct the guide (a folder's index.md joins the nav), or let buildNav take any page with a nav: key.", "note": "19 Sep 2026: corrected the guide (tests/example/pages/guide/pages.md): nav: names a folder's index.md in the navigation, else its title:, and other pages need a folder of their own. Copied to .dev-site too. buildNav unchanged."}
{"n": 14, "status": "fixed", "severity": "bug", "area": "site", "file": "server/routes/site.ts", "summary": "Published pages mangle $ sequences: '$$5' shows as '$5', and '$&' pastes in the template's {{content}}", "detail": "Found 19 Sep 2026. site.ts:29-33 fills the template with String.replace(pattern, string), and a replacement string reads $$, $&, $` and $' as patterns. So LaTeX-style $$...$$, prices written $$, and any page that shows regex replacement syntax come out wrong on the site, while the editor's preview (built as a template literal) shows them right: what you preview is not what gets published. Checked with renderMarkdown + the same replace: 'Costs $$5. In a regex, `$&` is the match.' published as 'Costs $5 ... <code>{{content}}amp;</code>'. Fix: replacer functions, .replace(\"{{content}}\", () => content), on all five; a site test with a $$ page.", "note": "19 Sep 2026: all five template fields are filled with replacer functions. server.test.ts publishes a page titled '$$ and $& and $'' with 'Costs $$5' and '$&' in code, and checks they come out as written."}
{"n": 15, "status": "fixed", "severity": "feature", "area": "login", "file": "server/auth.ts, server/edit/login.html", "summary": "Signing in should land in the editor", "detail": "Peter, 19 Sep 2026. Without a next, login redirected to / (the site's home page), and /login while already signed in showed the form again.", "note": "19 Sep 2026: safeNext takes a fallback; login uses /edit (the form's hidden next defaults to it too), and GET /login when signed in redirects straight to next. The seed site's 'Login to edit' link now goes to the editor; tested both ways."}
{"n": 16, "status": "fixed", "severity": "engineering", "area": "errors", "file": "server/edit/api.ts, server/edit/notice.ts, server/edit/components/Notice.tsx, server/routes/error.ts", "summary": "Make failures speak", "detail": "Peter, 19 Sep 2026. Client: empty catches, ignored statuses, errors swallowed in load(). Server: a thrown handler error was Bun's default page, a broken users.json read as a wrong password, a theme that failed to load vanished.", "note": "19 Sep 2026: notice.ts (speak/hush) + Notice (role=alert) show one message until dismissed; api(what, url, init, allow) speaks for every failure it doesn't allow, and a network failure resolves to Response.error(); apiJson likewise for bad JSON; app.tsx speaks for uncaught errors and rejections. Server: Bun.serve error handler (routes/error.ts) logs and answers 500 with a line (detail in development); a broken users.json puts 'can't be read' on the login page, a missing one is logged; a theme that won't load is logged and the page renders without it. Seen in the pane: /edit?path=typo.md shows 'Couldn't open typo.md: 404 Not Found'."}
{"n": 17, "status": "fixed", "severity": "engineering", "area": "tests", "file": "bunfig.toml, tests/", "summary": "100% test coverage", "detail": "Peter, 19 Sep 2026. The server ran as a subprocess (so none of it counted) and the editor's code had no tests at all.", "note": "19 Sep 2026: bun run test = bun test --coverage with coverageThreshold 1.0 (lines and functions) in bunfig.toml; all 28 app files at 100%, 128 tests, about 4s. The server runs in-process (tests/setup.ts preload sets the env first), the editor runs in happy-dom (new dev dependency @happy-dom/global-registrator) against it, S3Storage runs through Bun's real S3Client against tests/fake-s3.ts, and process.test.ts keeps three real subprocess checks. Only tests/** is excluded. A run that leaves code uncovered exits 1 (checked). Testability changes are listed in the CHANGELOG."}
{"n": 18, "status": "fixed", "severity": "bug", "area": "auth", "file": "server/auth.ts", "summary": "A mangled session cookie made every request a 500", "detail": "Found 19 Sep 2026 while rewriting auth.ts: a three-part cookie whose parts weren't base64 made atob throw inside verifyJwt, which getUser and requireAuth didn't catch.", "note": "19 Sep 2026: verifyJwt returns null (signed out) for anything it can't decode; a test sends 'duckie_token=a.b.c!' and gets a 401."}
{"n": 19, "status": "fixed", "severity": "bug", "area": "storage", "file": "server/storage.ts", "summary": "safePath let a sibling folder through when its name began with the root's", "detail": "Found 19 Sep 2026: resolved.startsWith(root) passed '\u2026/pages-old/x' for the root '\u2026/pages'. Latent before today (paths weren't decoded, so a '..' couldn't arrive in one), reachable once names are decoded (todo 20).", "note": "19 Sep 2026: the resolved path must be the root or start with root + separator. Tested at the storage level and over HTTP (..%2Fpages-old%2Fsecret.md is refused, 500 via the error handler)."}
{"n": 20, "status": "fixed", "severity": "bug", "area": "editor", "file": "server/utils.ts, server/routes/site.ts, server/edit/api.ts, server/markdown.ts", "summary": "Names with spaces were stored URL-encoded ('About%20us.md'), and '#' or '?' broke URLs", "detail": "Found 19 Sep 2026: after() used the raw pathname, so storage keys kept their %-escapes, while the editor interpolated names into URLs unencoded.", "note": "19 Sep 2026: the server decodes the path (after(), and the site route); the editor builds every URL with urlPath() (each segment encoded, slashes kept), including thumbnails, the markdown image URL and View; nav links are encodeURI'd. Tested: 'About us' is stored as 'About us.md' and served at /About%20us.html."}
{"n": 21, "status": "fixed", "severity": "bug", "area": "pages", "file": "server/routes/pages.ts", "summary": "Opening a page that doesn't exist opened an empty folder listing as its text", "detail": "Found 19 Sep 2026: GET /edit/pages/missing.md fell through to list(), a 200 {\"files\":[],\"folders\":[]}, which loadFile opened; saving would have written that JSON into a new file.", "note": "19 Sep 2026: a missing .md or .css is a 404 (so api() speaks); folder paths still list. Seen in the pane with ?path=typo.md."}
{"n": 22, "status": "fixed", "severity": "design", "area": "site", "file": "server/markdown.ts, server/routes/site.ts", "summary": "Every page view walks the whole pages tree for the nav", "detail": "Noted 19 Sep 2026 (review). buildNav lists every folder and reads every index.md on each request: fine on local disk for a small site, slow and chatty on S3. Options to decide with Peter: cache the nav and drop it on the editor's writes (pages PUT/DELETE), with a short TTL for changes made outside the editor (none in development); or feature.md's 'duckie publish' to static files, which removes the per-request cost altogether.", "note": "19 Sep 2026: Peter chose the cache ('since the server is responsible for writing, a nav cache is fine'). server/nav.ts: siteNav() builds once and keeps it; the pages route's PUT and DELETE call navChanged(); a failed build isn't kept. No TTL: a file changed on disk behind the server's back shows after the next editor save or a restart (said in CLAUDE.md, with the rule that any new write path must call navChanged()). Tested: a folder written straight to disk stays out of the nav until an editor write, then both appear; the unit test counts list() calls."}
{"n": 23, "status": "fixed", "severity": "ops", "area": "server", "file": "server/stop.ts, server/pid.ts, package.json", "summary": "A stop script that stops the server the pid file names", "detail": "Peter, 19 Sep 2026.", "note": "19 Sep 2026: bun run stop \u2192 server/stop.ts \u2192 stopServer() in pid.ts. Signals only a process whose command line names main.ts (a stale pid may have been reused), removes a stale file, SIGTERMs and waits (5s), tidies a pid file the server left, and says what it did; exit 0 once nothing runs here, 1 if it left something running. Tests cover every path with real processes (stand-ins, a reused pid, one that ignores SIGTERM, and a real server). First real use stopped the preview pane's server, which the app had restarted on its own; restarted it with preview_start."}
{"n": 24, "status": "fixed", "severity": "feature", "area": "markdown", "file": "server/markdown.ts", "summary": "Linkable headings, and a contents list with toc: true", "detail": "First writing-and-styling pass, agreed with Peter 19 Sep 2026. Bun.markdown's headings: { ids: true, autolink: true } gives <h2 id=\"\u2026\"><a href=\"#\u2026\">\u2026</a></h2> (repeats get -1). toc: true in the front matter puts a contents list (h2/h3) after the page's first h1. The preview API's placeholder toc field goes: the contents are part of the content.", "note": "19 Sep 2026: headings: { ids: true, autolink: true } on; toc: true (or yes) puts <nav class=\"toc\" aria-label=\"Contents\"> of the h2s and h3s after the first </h1>, or first if there's none; nothing without headings. The mark API's empty toc field is gone. The Writing Pages and Themes guides use it. Seen in the pane: the contents link jumps to its heading."}
{"n": 25, "status": "fixed", "severity": "feature", "area": "markdown", "file": "server/markdown.ts, tests/example/static/site.css", "summary": "Callouts: > [!NOTE], [!TIP], [!IMPORTANT], [!WARNING], [!CAUTION]", "detail": "First writing-and-styling pass, agreed with Peter 19 Sep 2026. GitHub's alert syntax. Bun renders it as a blockquote whose first paragraph starts [!NOTE]; post-process into <blockquote class=\"callout note\"> with a title. Elsewhere it stays a readable quote.", "note": "19 Sep 2026: a regex pass turns <blockquote><p>[!KIND] into <blockquote class=\"callout kind\"><p class=\"callout-title\">Kind</p>, for both 'text on the next line' and 'blank line then text', case-blind; any other quote is left alone. Colours are site.css variables (--note \u2026 --caution), tinted with color-mix. All five shown in the guide; seen in both light and dark."}
{"n": 26, "status": "fixed", "severity": "feature", "area": "markdown", "file": "server/markdown.ts", "summary": "[[Wiki links]] between pages", "detail": "First writing-and-styling pass, agreed with Peter 19 Sep 2026. Bun's wikiLinks gives <x-wikilink data-target=\"\u2026\">label</x-wikilink>, not a link: turn it into <a>, resolved like a relative link (from the page's folder; a leading / from the root), .html added, #heading kept, [[page|label]] for a label.", "note": "19 Sep 2026: wikiLinks on; <x-wikilink data-target> becomes <a class=\"wikilink\">: relative to the page's folder, a leading / from the root, .md dropped and .html added, #heading kept, [[#heading]] on the same page, names encoded. renderMarkdown takes the page's path (the site and the preview pass it). Labels are what you wrote unless you give one with |."}
{"n": 27, "status": "not fixed", "severity": "design", "area": "markdown", "file": "server/markdown.ts", "summary": "Maths ($x^2$, $$ blocks): needs a renderer, so a decision", "detail": "Split out of the first pass 19 Sep 2026: Bun.markdown's latexMath option left $x^2$ and $$\u2026$$ untouched here, so maths needs a renderer. The preview runs no scripts (sandbox), so it would have to be server-side (KaTeX's renderToString: a new runtime dependency, plus its CSS and fonts), opt-in per page (math: true) so $ in prices never turns into maths. Decide whether it's worth a dependency.", "note": "2026-09-19: decided against. KaTeX or MathJax would be duckdown's first frontend dependency, for something few sites need. The template is the escape hatch: add the <script> to templates/site.html, or to a layout: used by the pages that need it. Documented in feature.md and the guide."}
{"n": 28, "status": "fixed", "severity": "feature", "area": "css", "file": "tests/example/static/site.css, create/setup.ts, tests/example/pages/-theme.css", "summary": "A better default stylesheet, built on CSS variables, so a theme is a few lines", "detail": "First writing-and-styling pass, agreed with Peter 19 Sep 2026. Rebuild site.css on ~10 custom properties (colours, fonts, measure, radius; the callout colours), with real typography, dark mode, and styles for code, tables, callouts, the contents list and the nav. A theme then overrides variables on body.<theme>. New sites (bun create) get the same stylesheet.", "note": "19 Sep 2026: tests/example/static/site.css rewritten on variables (--bg --text --muted --accent --border --surface --font-body --font-heading --font-mono --measure --radius, plus the five callout tones), with prefers-color-scheme dark values, and styles for headings (quiet self-links), code, tables, quotes, callouts, the contents list, task lists and the nav. The duckdown theme is now a few variables (with its own dark accent). bun create copies this stylesheet into new sites. The CSS preview's sample now shows the nav, a callout, code and a table."}
{"n": 29, "status": "fixed", "severity": "feature", "area": "css", "file": "server/markdown.ts, server/routes/mark.ts, server/edit/components/Preview.tsx", "summary": "Themes cascade: a page gets the root's -theme.css, then each folder's down to its own", "detail": "First writing-and-styling pass, agreed with Peter 19 Sep 2026. Today loadThemeCss reads only the page's own folder, so guide/ pages never get the root theme. Concatenate root first, deepest last. The preview gets the same cascade from the server (the mark route, given the page's path), not a single <link>.", "note": "19 Sep 2026: loadThemeCss joins the root's -theme.css and each folder's down to the page's own; one that fails is logged and skipped. The mark route takes ?path= and returns the cascade as theme; the preview inlines it (no more one-folder <link>). Tested with a guide/-theme.css at both levels."}
{"n": 30, "status": "fixed", "severity": "feature", "area": "site", "file": "server/nav.ts, server/routes/site.ts", "summary": "The nav marks the current page (and the section you're in)", "detail": "First writing-and-styling pass, agreed with Peter 19 Sep 2026. The nav is one cached string, so mark it per request: aria-current=\"page\" on the page's own link, else aria-current=\"true\" on its nearest folder's (not the root's), for the stylesheet to show.", "note": "19 Sep 2026: nav.ts markCurrent(nav, file) runs per request on the cached nav: aria-current=\"page\" on the page's own link, else \"true\" on its nearest folder's (never the root's), replaced with a replacer function. site.css underlines it with the accent. Seen: Guide underlined on the guide pages."}
{"n": 31, "status": "fixed", "severity": "feature", "area": "site", "file": "server/routes/site.ts, templates/", "summary": "Per-page settings: layout, description, draft", "detail": "Proposed 19 Sep 2026 (review of how duckdown could grow while staying easy); after the first pass. layout: post picks templates/post.html (else site.html); description: fills the meta description and social tags; draft: true keeps a page off the site and out of the nav until it's ready (the editor still opens and previews it).", "note": "2026-09-19: layout: picks a template from templates/ (name guarded to [\\w-]+), description: fills a meta description via {{description}}, draft: true 404s for readers and stays visible to anyone signed in."}
{"n": 32, "status": "fixed", "severity": "feature", "area": "site", "file": "server/routes/site.ts, tests/example/templates/site_tmpl.html", "summary": "'Edit this page' on the site when you're signed in", "detail": "Proposed 19 Sep 2026 (review of how duckdown could grow while staying easy); after the first pass. The Tornado version showed an edit link to signed-in users (templates/site_tmpl.html, a leftover, and site.css's .user_edit still style it); the Bun version lost it. Link to /edit?path=<page>.md, and delete the leftover template.", "note": "2026-09-19: {{edit}} in the template renders an 'Edit this page' link for whoever is signed in, and nothing at all for everyone else. Styled .user-edit in site.css."}
{"n": 33, "status": "fixed", "severity": "feature", "area": "site", "file": "server/routes/site.ts, server/markdown.ts", "summary": "Folder listings: a tag that lists a folder's pages, newest first by date:", "detail": "Proposed 19 Sep 2026 (review of how duckdown could grow while staying easy); after the first pass. A blog index without maintaining menus: e.g. {{pages}} (or list: pages) renders the folder's pages with their titles and date:, newest first; cached with the nav, since the same writes change it.", "note": "2026-09-19: {{pages}} in a page's body expands to the folder's listing \u2014 newest first by date:, then by title, with date and description. Skips index, drafts, and - prefixed files. Cached alongside the nav and dropped by pagesChanged()."}
{"n": 34, "status": "fixed", "severity": "bug", "area": "editor", "file": "server/edit/app.tsx", "summary": "The CSS preview showed a theme unstyled until you typed", "detail": "Found 19 Sep 2026 checking the new stylesheet in the pane: opening -theme.css, the preview frame's srcdoc held the themed page (<body class=\"duckdown\">) but the frame showed the document from before the file loaded (class \"\", no theme); the first edit fixed it. app.tsx built both previews at startup and attached the CSS one in the same flush that changed its srcdoc; the browser kept the first document. Older than today.", "note": "19 Sep 2026: the preview panel is when(hasFile, () => when(isCss, CssPreview, Preview), placeholder), so each preview is built when shown, from the content then. Checked in the pane: a fresh load of ?path=-theme.css, and md \u2192 css \u2192 md through the file list, all show the right document at once."}
{"n": 35, "status": "fixed", "severity": "bug", "area": "editor", "file": "server/edit/components/Preview.tsx", "summary": "A preview render still pending ran after the page changed or the preview closed", "detail": "Found 19 Sep 2026 as a flaky test ('stop > says so when a server won't stop'): a 300ms render scheduled by the app test fired after the editor tests finished and spoke \"Couldn't render the preview\", which a later test's console spy caught. In the app it meant a closed or deleted page could still be rendered.", "note": "19 Sep 2026: the debounce is the effect's own cleanup (railroad effects may return one, run before the next run and on dispose): a new change, or the preview going away, cancels the pending render. Five full runs in a row clean."}
{"n": 36, "status": "fixed", "severity": "engineering", "area": "deps", "file": "package.json, bun.lock", "summary": "Take railroad from npm, not the GitHub repository", "detail": "Peter, 19 Sep 2026: @blueshed/railroad is published on npm.", "note": "19 Sep 2026: \"github:blueshed/railroad\" (commit 5d4b71b, version 0.11.0) \u2192 \"^0.11.0\" from the registry (latest, published 28 Jul 2026); bun.lock now pins the npm tarball. tsc clean, 148 tests at 100% on it."}
{"n": 37, "status": "fixed", "severity": "engineering", "area": "tooling", "file": ".claude/skills/railroad, .claude/skills/bun-route, CLAUDE.md", "summary": "Install railroad's Claude Code skills", "detail": "Peter, 19 Sep 2026: railroad ships skills. Its package has two under .claude/skills: railroad (the failure-mode checklist; its \u00a72 is exactly todo 7, keyed list rows are signals) and bun-route (HTML routes and Bun.WebView tests).", "note": "19 Sep 2026: read both in full first (documentation, nothing that runs anything), then copied into .claude/skills/ as the railroad README says. They're pinned copies: CLAUDE.md says to re-copy after a railroad upgrade. create/setup.ts already removes .claude from scaffolded sites."}
{"n": 38, "status": "fixed", "severity": "engineering", "area": "tooling", "file": ".claude/skills/duckdown/SKILL.md, .claude/skills/duckdown/reference.md, CLAUDE.md", "summary": "The duckdown authoring skill was out of date, and wrong in places", "detail": "Peter asked 19 Sep 2026. Written 20 Mar: it taught plaintext passwords in users.json (hashes since 0.0.2), themes per folder only (they cascade), hard-coded theme colours (variables now), {{title}} defaulting to \"duckdown\" (it's \"duckie\"), click-to-copy images (it's select, then Copy Markdown); it knew nothing of toc, callouts, wiki links, heading links, dark mode, the nav marks, New's no-overwrite, or the nav cache; and its name (duckdown-authoring) didn't match its folder.", "note": "19 Sep 2026: rewritten as a lean SKILL.md (where the content lives, writing a page, the six traps, how to check) plus reference.md with a contents list (content folder, pages and URLs, front matter, markdown, navigation, themes and every variable, template, static files and images, users, the editor, troubleshooting), following the skill-creator's pattern and railroad's bun-route. name: duckdown; a fuller, more assertive description. Claims checked against the code. CLAUDE.md now says to update the skill and the guide pages whenever a content feature changes."}
{"n": 39, "status": "fixed", "severity": "feature", "area": "site", "file": "server/routes/site.ts", "summary": "A folder's URL (/guide/ or /guide) is a 404: only /guide/index.html works", "detail": "Noted 19 Sep 2026 writing the skill: the site route maps a path to <path>.md, so /guide/ looks for 'guide/.md' and /guide for 'guide.md'. Serve the folder's index.md for both, the way / already serves index.md; the nav could then link folders as /guide/.", "note": "2026-09-19: site.ts resolves a folder to its index.md \u2014 /blog, /blog/ and /blog/index.html all serve the same page. Wiki links and the nav still emit index.html, so nothing else changed."}
{"n": 40, "status": "fixed", "severity": "design", "area": "markdown", "file": "server/markdown.ts", "summary": "A page whose first line reads like 'Update: closed Monday' loses it to the front matter", "detail": "Found 19 Sep 2026 writing the skill: parseFrontMatter takes any leading 'word: text' lines as metadata, so a page without front matter that opens with such a sentence silently drops it (checked). The skill now warns (start with a heading or a blank line). Options: treat only known keys (title, theme, nav, toc, x-\u2026) as front matter; or require the blank line that ends it; or YAML-style --- fences.", "note": "2026-09-19: front matter is now either a --- fence or the known keys (title, theme, nav, toc, layout, description, draft, date, and x-* for your own). 'Update: closed Monday' is a paragraph again. Breaking for anyone who invented their own bare keys \u2014 use x- or a fence."}
{"n": 41, "status": "fixed", "severity": "design", "area": "create", "file": "create/setup.ts", "summary": "New sites lose the authoring skill: bun create's setup deletes .claude", "detail": "Noted 19 Sep 2026: setup removes the whole .claude folder, which now holds the duckdown authoring skill (what a Claude session writing the new site's content should read), railroad's skills and launch.json (the preview pane). Decide what a new site keeps: likely skills/duckdown and launch.json; railroad and bun-route only matter to someone changing the editor.", "note": "2026-09-19: create/setup.ts now removes only the repo's own scaffolding; a new site keeps .claude/skills/duckdown and launch.json."}
{"n": 42, "status": "fixed", "severity": "bug", "area": "css", "file": "tests/example/static/images/logo.svg, tests/example/pages/index.md, tests/example/pages/-theme.css, tests/example/pages/guide/images.md", "summary": "The duck logo kept its own black, in every theme, and vanished in dark mode", "detail": "Peter asked 19 Sep 2026 whether CSS can colour the duck. It was placed with <img>, and an SVG shown that way is a separate document: the page's CSS and variables can't reach inside it, and currentColor means the file's own colour. On the dark theme the black duck was invisible.", "note": "19 Sep 2026: the page now has an empty <span id=\"logo\"> and the theme paints it: background: var(--duck, currentColor) with the SVG as a -webkit-mask/mask (contain, centred); the eye and wing are holes, so the page still shows through. The duckdown theme sets --duck: var(--accent), so it is teal in light and dark. Two things learned the hard way, both now in the Images guide and the skill: <svg><use href=\"/static/images/logo.svg#duck\"> reads better but draws nothing in Chrome or Safari (external use is Firefox-only; caught in the pane by an empty bbox); and an XML comment may not contain \"--\", so a comment mentioning --duck made logo.svg invalid and it rendered as nothing at all (xmllint --noout says so). logo.svg keeps a symbol id=\"duck\" drawn in var(--duck, currentColor), and follows the reader's light/dark setting when shown as an <img>."}
{"n": 43, "status": "fixed", "severity": "design", "area": "site", "file": "server/nav.ts, server/config.ts", "summary": "Pages written as files don't reach the nav until an editor save or a restart", "detail": "Seen 19 Sep 2026 adding a blog with the authoring skill: the post rendered at once, but Blog only joined the nav after saving the page in the editor (the cache is dropped by the editor's writes, todo 22). That's fine for the editor, but anyone \u2014 or any Claude session \u2014 writing pages as files meets it every time. Options: don't cache when DEBUG=1; or check the newest mtime under pages/ (local storage only, an extra stat per request); or leave it, since the skill and CLAUDE.md both say so.", "note": "19 Sep 2026: Peter chose the development exception. siteNav(pages, debug = DEBUG) builds per request when DEBUG is on and caches otherwise; navChanged() still drops it in production. Checked on the running dev server: a folder's index.md written straight to disk joined the nav immediately, and left it when deleted, with no editor save. The skill, CLAUDE.md and the CHANGELOG now say so; a unit test covers both ways."}
{"n": 44, "status": "fixed", "severity": "medium", "area": "site", "file": "server/routes/site.ts", "summary": "{{pages}} expanded inside code, so no page could show the tag", "detail": "The listing was substituted across the whole rendered page, including <code> spans and fenced blocks. The guide page documenting {{pages}} had the guide's own folder listing injected into its code sample and its inline code, so the one page that must show the syntax couldn't. Found while checking the seed site for stale docs.", "note": "2026-09-19: outsideCode() in utils.ts splits the rendered HTML on <code>...</code> and substitutes only between the pieces \u2014 a fence renders as <pre><code>, so one rule covers both. Code is literal, which is what writers expect. The other placeholders were never at risk: {{content}} is substituted last, so a page's text is inserted after they're consumed."}
{"n": 45, "status": "fixed", "severity": "medium", "area": "deploy", "file": "server/main.ts", "summary": "No /health route for a platform healthcheck", "detail": "Railway (and any platform) wants a cheap endpoint that proves the process is up without depending on content. Today the shallowest check is / , which renders index.md through storage \u2014 on S3 that's a network round trip per probe, and a content mistake reads as a dead service.", "note": "2026-09-20: /health in main.ts \u2014 a static Response, so the probe never touches storage."}
{"n": 46, "status": "fixed", "severity": "high", "area": "deploy", "file": "server/pid.ts", "summary": "A pid file on a persistent volume can permanently block startup in a container", "detail": "claimPidFile() refuses to start when the pid file names a live process, checking only isAlive(). In a container the app is a low pid (often 1). After a hard restart (SIGKILL, OOM, platform recycle) the file survives on the volume naming pid 1, which is always alive \u2014 so the new process throws and the service never starts again: a crash loop with no bad input. stopServer() already has isDuckie() (ps, command line names main.ts); claimPidFile doesn't use it.", "note": "2026-09-20: claimPidFile refuses only a live *duckdown* (heldByDuckie = isAlive && isDuckie, the check stopServer already used), and takes the guard as a parameter so both branches are tested deterministically. process.test.ts now runs two real servers for the refusal, and asserts a restart over a recycled pid."}
{"n": 47, "status": "fixed", "severity": "high", "area": "auth", "file": "tests/example/users.json", "summary": "A deployment seeded from the example site ships a public admin/admin", "detail": "DUCKDOWN_SEED copies tests/example (or create's scaffold) when the content folder is empty, users.json included. That's right for local dev and wrong the moment the site is on the internet: the editor is at a known URL with known credentials. Needs a deliberate answer \u2014 an admin password from the environment at seed time, or a refusal to seed default credentials when DEBUG isn't set.", "note": "2026-09-20: DUCKDOWN_ADMIN_PASSWORD (with DUCKDOWN_ADMIN_USER, default admin) is written into users.json at startup by ensureAdmin() \u2014 the secret lives in the platform, not in git, and changing it changes the password on the next restart. It rewrites only when the password actually changed, so a restart isn't an S3 PUT. Unset, nothing happens, which is every local run."}
{"n": 48, "status": "fixed", "severity": "low", "area": "deps", "file": "package.json", "summary": "GitHub reports 36 Dependabot alerts on master; duckdown has five dependencies", "detail": "The push to master printed 36 vulnerabilities (2 critical, 16 high). All 36 pointed at one manifest, client/package-lock.json \u2014 the Vue client, deleted on 2026-03-20 in the rewrite commit. None pointed at package.json or bun.lock, and none of the flagged packages (koa, axios, lodash, postcss, prismjs, minimist, loader-utils, node-forge\u2026) is in the current tree, which is railroad, lucide-static and three dev dependencies. GitHub had not closed them when the manifest went.", "note": "2026-09-21: checked against the alert API rather than guessed, then dismissed all 36 as not_used with the reason recorded on each. 0 open now. Left alone, a security page showing 36 alerts and 2 criticals teaches you to ignore it, and a real one in bun.lock would be lost in the noise."}
{"n": 49, "status": "fixed", "severity": "high", "area": "create", "file": "package.json", "summary": "bun create leaves a half-scaffolded site: the postinstall hook doesn't run setup", "detail": "`bun create blueshed/duckdown x` prints `$ bun run create/setup.ts` and never runs it. Proved it's Bun 1.4.2, not the script: a bun-create.postinstall of `echo HELLO` prints its own command line and produces no output, and `sh -c 'echo > ran.txt'` writes no file anywhere. Bun does read the field \u2014 it strips it from the package.json it writes \u2014 it just doesn't execute it. The symptom is worse than first logged: without setup there is no .env, so DEBUG isn't set, so the server refuses to start over a missing COOKIE_SECRET \u2014 a message that says nothing about the real cause.", "note": "2026-09-20: can't be fixed by changing the command, so duckdown stops depending on the hook. server/scaffold.ts spots a copy `bun create` left un-set-up (bun strips the bun-create key, so its absence plus a missing site/ and a present create/setup.ts is the signature) and the COOKIE_SECRET error carries the explanation and the one command that fixes it: bun run setup. Not auto-run: setup deletes tests/ and create/, and doing that silently because someone typed `bun dev` would be worse than the bug. Verified end to end on a faithful copy."}
{"n": 50, "status": "fixed", "severity": "medium", "area": "storage", "file": "server/storage.ts", "summary": "DUCKDOWN_SEED fills a local folder but never a bucket, so an S3 deployment starts empty", "detail": "seedLocalSite() returns immediately when IS_S3 (`if (s3 || !seed || existsSync(target)) return`). With DUCKDOWN_BUCKET set, a fresh deployment therefore has no pages, no template and no users.json \u2014 the site 404s and nobody can sign in, with nothing in the logs to say why. There is no supported way to get a scaffolded site/ into a bucket; blueshed.co.uk needed a hand-written upload script (scripts/seed-bucket.ts there). The fix mirrors the local path: when the bucket has no pages/index.md, copy the seed folder up, then say so in the startup banner. Guard it the same way \u2014 seed only when the target is empty \u2014 so it can never overwrite a live site.", "note": "2026-09-20: seedBucketSite() in storage.ts uploads DUCKDOWN_SEED when the bucket holds no pages/index.md, called from main.ts after seedLocalSite(). Guarded like the local path \u2014 only into an empty site, so it can never overwrite one being written \u2014 and it says in the log what it did, or that the seed folder wasn't there. Tested against the fake S3."}
{"n": 51, "status": "fixed", "severity": "high", "area": "site", "file": "server/routes/site.ts", "summary": "A placeholder used twice in a template was only filled once", "detail": "The template was filled with .replace(\"{{title}}\", fn), and String.replace with a string pattern replaces only the first occurrence. blueshed.co.uk's template used {{title}} in <title> and again in og:title, so every page published <meta property=\"og:title\" content=\"{{title}}\"> \u2014 the placeholder itself, on the live site.", "note": "2026-09-20: fill() uses a global regex per placeholder, so a template may use one anywhere and as often as it likes; {{content}} is still filled last so a page's own text is never scanned. Found by checking the deployed page's meta tags."}
{"n": 52, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/routes/pages.ts", "summary": "templates/ and static/ can't be edited, so a deployed site's template can only be changed out of band", "detail": "The editor reaches pages/ (and images through /edit/browse). templates/site.html, static/site.css and anything else under static/ are not editable. On a local site that's fine \u2014 edit the files. On a bucket-backed deployment the bucket is the only copy and is never re-seeded, so changing the template means writing to S3 by hand; blueshed.co.uk needed a script for it. Either let the editor open templates/ and static/ text files, or give duckdown a documented way to push them.", "note": "2026-09-20: the editor has sections. fileRoutes(prefix, store, changed) in routes/files.ts is the file API over one folder; pages/, templates/ and static/ each get their own route rooted in their own folder, so none can reach across them and users.json (hashes, at the site root) is in none of them. The browser has pages | templates | static, and switching puts down whatever was open so a file can't be saved into the wrong folder. A template is neither markdown nor CSS, so the preview says so instead of rendering its placeholders as prose."}
{"n": 53, "status": "fixed", "severity": "medium", "area": "site", "file": "server/nav.ts", "summary": "Three URLs serve each page and each claims itself canonical", "detail": "Since folders are served by their index, /journey, /journey/ and /journey/index.html all answer 200. {{url}} reports whichever was asked for, so each is canonical for itself \u2014 duplicate content, three ways. The nav and wiki links both emit /index.html, which is the least tidy of the three. Pick one form: have the nav and wiki links emit the folder path, and have {{url}} normalise (drop a trailing index.html, keep one trailing-slash convention) so the other two point at it.", "note": "2026-09-21: canonicalPath() in utils.ts is the one rule \u2014 index.md is /, a folder's index is /folder/, anything else is /page.html \u2014 and the canonical is computed from the key the request resolved to, not from the address asked for, so all three forms name the same page. The nav and markCurrent use the same rule, so the links crawlers follow are the canonical ones. The seed's template gained the canonical link (only the website's had it), the seed's own links were canonicalised, and the guide and reference say which of the three is the real address."}
{"n": 54, "status": "fixed", "severity": "low", "area": "seed", "file": "tests/example/templates/site_tmpl.html", "summary": "layout: had no user anywhere, and the seed shipped a broken Tornado template", "detail": "Nothing in tests/example, .dev-site or blueshed.co.uk used layout:, so a feature with tests had never been used in practice. Worse, the seed shipped templates/site_tmpl.html \u2014 {{ handler.one_meta_value('title') }}, {% end %} \u2014 the Python duckdown's Tornado template. Nothing referenced it, it would render its own tags as text if anything did, and it appeared in the editor's template list looking like a real choice.", "note": "2026-09-21: site_tmpl.html removed; templates/post.html added with a dated post in a new seed blog that uses it. {{date}} added so a template can show a page's date, sharing dateHtml() with the folder listings. Two test fixtures were quietly fighting over the name templates/post.html \u2014 the older layout test created and deleted it \u2014 which is why the new one failed only when the whole suite ran."}
{"n": 55, "status": "fixed", "severity": "medium", "area": "site", "file": "server/utils.ts", "summary": "dateHtml shows the previous day west of UTC", "detail": "new Date(\"2026-09-21\") parses as UTC midnight but the Intl.DateTimeFormat formats in the server's local timezone: with TZ=America/New_York the seed post renders \"20 September 2026\" under datetime=\"2026-09-21\", and the new server test (which expects 21 September) fails on such a machine. Passes on a UTC host, so deployment hides it. Pre-existing in the listings; moved into utils.ts unchanged. Fix: add timeZone: \"UTC\" to the formatter.", "note": "2026-09-21: Intl.DateTimeFormat now takes timeZone: \"UTC\". A date: has no time in it, so it is a label, not a moment \u2014 it must read the same wherever the server runs. Proved by running the whole suite under TZ=America/New_York, and units.test.ts now asserts dateHtml directly."}
{"n": 56, "status": "fixed", "severity": "low", "area": "create", "file": "create/setup.ts", "summary": "setup.ts carries a second copy of templates/site.html", "detail": "The scaffolder writes its own inline template, so e620f1a had to add the canonical link in two places and the two will drift again. Copy tests/example/templates/site.html as setup.ts already does for static/site.css.", "note": "2026-09-21: setup.ts copies tests/example/templates/site.html, as it already did for static/site.css. The inline copy was byte-identical, so nothing changed but the number of places to edit."}
{"n": 57, "status": "fixed", "severity": "low", "area": "site", "file": "server/routes/site.ts", "summary": "The plain-word guard is written twice, and template() is roundabout", "detail": "/^[\\w-]+$/ guards both layout (line 39) and css (line 105): it is the path-traversal guard, so one plainName() helper keeps the two from diverging. template() could be names = plain(layout) ? [layout, \"site\"] : [\"site\"] instead of a loop with a continue. The inner name in renderPage's fill loop also shadows the outer name.", "note": "2026-09-21: plainName() in the new page.ts guards both layout: and css:. templateFor() is a list, not a loop with a continue, and returns which template it settled on. The shadowed name went with the move."}
{"n": 58, "status": "fixed", "severity": "low", "area": "seed", "file": "tests/example/templates/post.html", "summary": "post.html prints a dangling \"Posted \" for a post with no date:", "detail": "{{date}} correctly returns nothing, but the label around it is fixed. The seed is the model users copy: show the label only with a date, or drop it.", "note": "2026-09-21: the template writes {{date}} alone; site.css puts the label in .posted::before and hides .posted:empty, so a post with no date leaves nothing behind."}
{"n": 59, "status": "not fixed", "severity": "low", "area": "site", "file": "server/routes/site.ts", "summary": "css: has no user; layout: and theme: already cover its ground (YAGNI)", "detail": "No page in tests/example or .dev-site says css:, as layout: had none until #54. theme: is a few variables, layout: is the whole shape; css: sits between them, and reference.md's three sizes of override could be two. Drop it, or hold it until something needs it (check blueshed.co.uk first).", "note": "2026-09-21: it has a user now \u2014 tests/example/pages/blog/one-page-that-looks-different.md, the css: demo. blueshed.co.uk doesn't use it, but the seed does, and the seed is what teaches it. Keeping it."}
{"n": 60, "status": "fixed", "severity": "low", "area": "docs", "file": "CLAUDE.md", "summary": "CLAUDE.md describes utils.ts as wildcard path extraction only", "detail": "utils.ts now also holds escapeHtml, outsideCode, canonicalPath and dateHtml. Also reference.md has a double blank line before \"Three sizes of override\".", "note": "2026-09-21: CLAUDE.md lists page.ts and describes utils.ts by what is in it; the site/mark section now describes the shared renderer. The double blank line in reference.md is gone."}
{"n": 61, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/routes/mark.ts, server/edit/components/Preview.tsx", "summary": "The preview builds its own shell, so editing a template changes nothing in it", "detail": "A resource pane can now open templates/site.html beneath the page, but the preview never sees it: Preview.tsx wraps the rendered markdown in a shell of its own making, so a draft template body has nowhere to go and {{nav}} is missing from the preview entirely. /edit/mark/ should render the whole document through the real template, taking an optional draft template body in the request, and Preview should send the open resource's draft when it is the template the page uses.", "note": "2026-09-21: server/page.ts holds parsePage() and pageHtml(); routes/site.ts and routes/mark.ts both go through it, so the preview is the real document \u2014 template, nav, {{pages}}, theme cascade. PUT /edit/mark/ takes JSON ({source, draft}) and answers {html, layout, meta}. An unsaved template is used when it is the one the page wears; a stylesheet still goes straight into the head with no round trip."}
{"n": 62, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/ResourcePane.tsx", "summary": "Nothing says when the open template isn't the one the page uses", "detail": "Opening templates/post.html while looking at a page whose layout: is site.html edits something the preview will never show. The pane had a line saying so; the PaneHeader refactor dropped it and it was not put back. Say it once, in the pane, without blocking the edit \u2014 you may well be editing the template you are about to switch to.", "note": "2026-09-21: pageHtml() reports the template it used, the preview puts it in pageLayout, and ResourcePane says \"The page you are looking at uses site.html\" when you have another template open. It says it once and edits anyway."}
{"n": 63, "status": "fixed", "severity": "feature", "area": "tooling", "file": "server/export.ts, package.json", "summary": "Static site export: pre-render the site to a static folder for CDN/static hosting", "detail": "Duckdown currently requires a running Bun server to render and serve pages. For static hosting (Cloudflare Pages, GitHub Pages, S3 static hosting, Netlify) without an always-on process, duckdown could pre-render the site: walk pages/, render every non-draft page through pageHtml() to its canonical path (index.html, folder/index.html, page.html), and copy static/ and images/. Expose via a CLI command (e.g. bun run export [outDir]).", "note": "2026-09-21: shipped in 0.1.0 as `bun run export` (server/export.ts). Writes every page at its one canonical address through pageHtml() \u2014 the same renderer the site route and the preview use \u2014 then copies static/. Drafts left out, {{edit}} empty, {{url}} from DUCKDOWN_ORIGIN. Reads through the storage layer, so it exports a folder or a live bucket. blueshed.co.uk runs on it."}
{"n": 64, "status": "fixed", "severity": "feature", "area": "site", "file": "server/page.ts, static/site.css", "summary": "Client-side search: index published pages for readers to search the site", "detail": "As a site grows beyond a few folders, readers have no way to find content without clicking through the nav. Duckdown could generate a lightweight search index (e.g. /search.json holding titles, descriptions, headings, and clean text) rebuilt on page changes or export, paired with a small search modal or input template placeholder (e.g. {{search}}) that works without external services.", "note": "2026-09-21: server/search.ts builds one entry per readable page (url, title, description, and plainText(body)); /search.json serves it, cached like the nav and dropped by routes/pages.ts on a write, and bun run export writes dist/search.json so it works published too. The matching is the site's own static/search.js \u2014 no dependency, no index format. Seed index is 12KB for 8 pages. Drafts excluded, and a test fetches every entry's url to prove no result 404s."}
{"n": 65, "status": "fixed", "severity": "low", "area": "create", "file": "package.json", "summary": "The bin entries are never exercised", "detail": "0.1.0 added bin: { duckdown, duckdown-export } so a site can depend on duckdown rather than vendor its server/. Nothing runs them: blueshed.co.uk calls node_modules/duckdown/server/main.ts by path, and no test installs the package. Either exercise them (a test that installs into a scratch folder and runs `bunx duckdown-export`) or drop them \u2014 config nothing reaches is the same as config that doesn't work.", "note": "2026-09-21: added with the release, unused by the one consumer. | 2026-09-21: fixed: `bunx duckdown init` (server/cli.ts) is a bin with a real user, and setup-script.test.ts runs it and the scaffold's `bun run export` through node_modules/duckdown. duckdown-export and duckdown-serve are the site's own scripts' targets."}
{"n": 66, "status": "fixed", "severity": "low", "area": "site", "file": "server/export.ts, tests/example/pages/", "summary": "A site's own 404 page works but is undocumented and undemonstrated", "detail": "outPath(\"404.md\") is \"404.html\", so a page at pages/404.md exports to dist/404.html, which is what static hosts serve for a miss \u2014 blueshed's serve.ts already looks for it. Nothing says so: not the README, not the skill, and the seed ships no 404.md, so nobody would discover it. Either document it and have the seed demonstrate it, or decide it isn't a feature and say what happens on a miss instead.", "note": "2026-09-21: found while checking serve.ts's 404 branch was reachable \u2014 it is. | 2026-09-21: fixed: the seed ships pages/404.md; README, the skill's reference and the guide say what it does; served and exported (see n79)."}
{"n": 67, "status": "fixed", "severity": "low", "area": "docs", "file": "CLAUDE.md, README.md", "summary": "s3:up, s3:down, s3:logs and setup are in package.json and in neither document", "detail": "The Scripts sections list dev, start, stop, test, check, export and dev:s3. The MinIO helpers behind dev:s3 and the `setup` the scaffolder needs are unlisted. Small, but the Scripts block is the place a reader looks to learn what they can run.", "note": "2026-09-21: found by diffing package.json's scripts against both documents. | 2026-09-21: fixed: README's Scripts block and CLAUDE.md's list s3:up/s3:down/s3:logs/setup/serve."}
{"n": 68, "status": "fixed", "severity": "medium", "area": "storage", "file": "server/storage.ts, server/routes/site.ts", "summary": "A content folder that goes missing serves 404s and says nothing", "detail": "seedLocalSite() runs once, at startup. Delete or move DUCKDOWN_PATH while the server is up and every page is a plain 404 with nothing in the log \u2014 /health still answers 200, so it looks like a routing or a port problem rather than an empty site. That contradicts the project's own rule that nothing fails silently. Either notice at request time that the configured root has gone and say so loudly (once, not per request), or reseed. Hit three times on 2026-09-21, by me, each time costing minutes of looking in the wrong place.", "note": "2026-09-21: raised after the user hit it \u2014 \"I could not navigate to blogs\". | 2026-09-21: fixed: routes/site.ts noticeMissingRoot() says once, in the log, at the first miss after the folder goes (again if it goes a second time)."}
{"n": 69, "status": "fixed", "severity": "low", "area": "site", "file": "tests/example/static/site.css", "summary": "The \"Edit this page\" button floats over the text on a narrow screen", "detail": "{{edit}} renders a position:fixed link at the bottom right. On a phone it sits over whatever is there \u2014 on /guide/pages.html it covers a row of the front-matter table. Only someone signed in sees it, so no reader is affected, but the author reading their own page is. Either dock it into the flow at the foot of the page below a certain width, or fade it until it is touched.", "note": "2026-09-21: below 34rem .user-edit goes back into the flow at the foot of the page. Fixed with a sweep rather than on its own: 8 seed pages at 320/375/414/768 also turned up the front-matter table pushing the page sideways at 320, because a code span like templates/post.html is one unbreakable word that sets the column's minimum. overflow-wrap: anywhere on inline code (and on links, for bare URLs) fixes it; pre code is reset to normal because a fenced block scrolls."}
{"n": 70, "status": "fixed", "severity": "high", "area": "export", "file": "server/export.ts", "summary": "An export of nothing succeeds, so a misconfigured deploy publishes an empty site", "detail": "Reproduced: `DUCKDOWN_PATH=/nonexistent bun run server/export.ts out` prints '0 page(s) and 1 file(s)', exits 0, and leaves only search.json. On Railway the build then passes, serve.ts answers /health, and the deploy goes green with every page a 404. vashti is exposed to exactly this: its .railwayignore drops .env, railway.json sets no variables, so unless DUCKDOWN_PATH is set in the dashboard the exporter falls back to config.ts's default ./tests/example, which does not exist in a site that installs duckdown as a dependency. Fix: exportSite() throws (not process.exit - see Testing in CLAUDE.md) when it wrote zero pages, naming the storage root it read, and does so BEFORE rmSync(out) so a bad run never wipes a good dist. The import.meta.main caller lets it exit non-zero. Related to n68 (same silence, served side) - do them together. Also reconsider the ./tests/example default in config.ts: when that folder is absent and ./site exists, ./site is what both real sites mean. Tests: export.test.ts, an empty Storage and a missing folder both reject; the existing dist survives.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: exportSite renders first and throws before rmSync when there is nothing to publish, naming where it looked; main() returns the exit code. config.ts defaultSitePath() picks ./site when ./tests/example is absent. Existing dist survives (tested)."}
{"n": 71, "status": "fixed", "severity": "high", "area": "serve", "file": "server/serve.ts (new), package.json", "summary": "serve.ts is copied byte-for-byte into both sites, untested, and miscounts page views", "detail": "website/serve.ts and vashti/serve.ts are identical (cmp) 60-line copies: the published flavour's server. It belongs in duckdown. Move it to server/serve.ts, export a `serveDist(dir, port)`/`route(req, dir)` that tests can call in-process (100% coverage applies), guard the Bun.serve behind import.meta.main, add bin `duckdown-serve`, and document `bun run node_modules/duckdown/server/serve.ts` as the published start command (README, skill's publishing section). This closes website ledger n3. Two defects to fix on the way in, both visible in the copy: (1) it calls logView() for every request except /health, so stylesheets, images and search.json are logged as 'view' lines and website's `bun run views` ranks /static/site.css among the most read - the served site logs pages only (routes/site.ts). Log only answers that are HTML (the found file ends .html, or the 404). (2) decodeURIComponent(url.pathname) throws URIError on a malformed escape such as /%E0%A4%A - answer 400, don't throw. Keep what is right about it: the traversal guard, the relative 301 for /blog -> /blog/ with the query kept, 404.html with status 404, max-age=300. Tests: traversal (/../x, encoded %2e%2e), folder redirect, query preserved, 404.html used when present and plain 404 when not, static not logged, malformed escape is 400. After release, each site deletes its serve.ts and points start at duckdown's.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: server/serve.ts, bin duckdown-serve, `bun run serve`; documented in README and the skill. Logs pages only; malformed escape is 400; tests/serve.test.ts. Published start command: `bun run node_modules/duckdown/server/serve.ts`. Follow-up in each site after release: delete its serve.ts and point start at that (closes website n3)."}
{"n": 72, "status": "fixed", "severity": "medium", "area": "site", "file": "server/routes/site.ts, server/utils.ts", "summary": "A malformed percent-escape in a URL is a 500 with a stack in the log", "detail": "Reproduced in-process: handleSite(new Request('http://x/%E0%A4%A')) throws URIError: URI error, which routes/error.ts turns into a logged 500. Crawlers and scanners send these all day, so a served site's log fills with stacks that are nobody's bug. after() in utils.ts has the same decodeURIComponent and the same exposure on /static/*, /edit/pages/* etc. One helper in utils.ts (decode or null), used by both; a path that won't decode is a 400 'Bad Request' (and still a logged view on the site route). Tests in server.test.ts for / and /static/.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: utils.decodePath() and BadRequest; site route and serve.ts answer 400, after() throws BadRequest which handleError answers 400 without a stack."}
{"n": 73, "status": "fixed", "severity": "high", "area": "site", "file": "server/routes/static.ts, server/export.ts, create/setup.ts", "summary": "duckdown's base files are copied into each site and never upgrade", "detail": "CLAUDE.md calls static/site.css 'duckdown's base', but a site owns a copy made on the day it was scaffolded. Measured: website is pinned to v0.2.0 yet its site/static/site.css differs from the seed's by 162 lines - its header comment still describes the removed `theme:` key, it has no .topbar and no search styles - and everything added since (search, the 16px input, the narrow-screen fixes in 08febc8) was re-typed by hand into website's theme.css, which now carries ~90 lines of base. Its copy of the authoring skill (.claude/skills/duckdown) has drifted too. vashti copied today and is already one commit from drifting. Bumping the dependency must be what upgrades the base. Proposal: the static route and the exporter fall back to the package's own tests/example/static/ for a named list (site.css, search.js) when the site has no such file - a site that wants to fork the base still can, by having the file; a site that doesn't, deletes its copy and gets upgrades. create/setup.ts then stops copying site.css. Decide where the base lives (tests/example/static is an odd home for shipped runtime files - perhaps server/base/, with the seed reading through the same fallback). For the skill: a `bun run node_modules/duckdown/...` one-liner or documented cp, mirroring how railroad's skills are refreshed. Tests: served and exported, with and without the site's own file. Follow-up in website: delete site/static/site.css and search.js, move the hand-ported base rules out of theme.css, check at 320/375/768.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: DECIDED (Peter): the base files live in server/base/ (site.css, search.js), not tests/example/static/. server/ is present whether duckdown is installed as a dependency or vendored by bun create (n80), so one fallback serves both modes; the seed site reads its base through the same fallback rather than carrying a copy. A site forks a base file by having its own in site/static/. The editor lists only what the site owns - a fallback file is not shown as the site's. | 2026-09-21: fixed: base files in server/base/ (Peter's decision); staticFile() and the exporter fall back to them; seed carries no copy; setup no longer copies site.css. The skill is refreshed with `cp -r node_modules/duckdown/.claude/skills/duckdown .claude/skills/` (README). Follow-up in website and vashti: delete site/static/site.css and search.js, move hand-ported base rules out of theme.css, check at 320/375/768."}
{"n": 74, "status": "fixed", "severity": "medium", "area": "templates", "file": "server/page.ts, .claude/skills/duckdown/reference.md, tests/example/pages/guide/", "summary": "A template can't read the page's own x- keys, so vashti has five copies of one template", "detail": "vashti/site-new/templates/album-{jadd,look,heartleap,things,news}.html are 53 lines each and differ in three: the cover image, the album's name, the buy link. Front matter already accepts x- keys (isKey in markdown.ts) but nothing can use them, so the only way to vary a template is to copy it. Fill {{x-anything}} in pageHtml from meta, escaped with escapeHtml like title, empty when the page doesn't set it, and before {{content}} so page text is never filled. It needs no request, so export gets it free. One loop over meta keys starting x- ; keep fill()'s function-valued replacement. Then vashti is one album.html and three x- lines per page. Update the skill's reference (placeholders table, front matter) and the guide page with it, per CLAUDE.md. Tests: markdown/page tests for escaping (a value with \" and <), absent key -> empty, not filled inside content.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: {{x-anything}} in pageHtml, escaped, empty when unset, before {{content}}; skill reference and guide updated."}
{"n": 75, "status": "fixed", "severity": "medium", "area": "templates", "file": "server/page.ts", "summary": "Shared template markup (the top bar and search form) is pasted into every template", "detail": "The seed's search form + .topbar is ~13 lines of markup that every template must repeat: twice in the seed (site.html, post.html), seven times in vashti. website already diverged from it (header.site instead of .topbar) and had to re-derive the CSS. When the search markup next changes, every copy in every site is stale. Proposal, needs Peter's yes before building: `{{include name}}` pulls templates/name.html into a template, name guarded by plainName(), resolved once before the other placeholders are filled so an include can use {{nav}}, no nesting (an include inside an include is left as written), honoured by the editor's draft template. Alternative that is smaller but less general: a {{search}} placeholder that emits the form. Prefer the include: it also covers vashti's album sleeve and any footer.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: not built: needs Peter's yes (unchanged). | 2026-09-22: APPROVED by Peter, for 0.3.0. Build it as specified: {{include name}} pulls templates/name.html into a template; name guarded by plainName(); resolved once, before the other placeholders are filled, so an include can use {{nav}}, {{x-...}} and the rest; no nesting - an {{include}} inside an included file is left as written; a missing or unguarded name fills as nothing and is logged once (failures speak - not a bare catch); honoured by the editor's draft template (an unsaved templates/topbar.html shows in the preview of any page whose template includes it - templateFor's draft mechanism, extended to includes) and reported so the editor can say which files a page wears; export gets it free since it needs no request. Then use it: the seed's site.html and post.html share templates/topbar.html (the nav + search form). Docs with it: reference.md's placeholder table and template section, the guide page, CHANGELOG under 0.3.0. Tests: page tests for an include using {{nav}}, a nested include left as written, a name with a slash or dot refused, a missing file, the draft path; export test that the seed's exported pages still carry the search form. | 2026-09-22: fixed. {{include name}} in page.ts, resolved once before the rest, no nesting, guarded, logged when it can't resolve, honoured by the editor's draft mechanism and reported to it. Used in the seed: templates/site.html includes templates/topbar.html. Docs and tests as specified. post.html was NOT given the include (see commit message): it has never carried this markup and deliberately has no site nav, which the guide and a test both say; the ledger note's claim that post.html already duplicated it was wrong."}
{"n": 76, "status": "fixed", "severity": "medium", "area": "export", "file": "server/export.ts, server/routes/site.ts, tests/example/static/robots.txt", "summary": "robots.txt is where no crawler looks, and the seed's bans everything", "detail": "tests/example/static/robots.txt says 'to ban all spiders uncomment the next two lines' above two lines that are already uncommented: User-agent: * / Disallow: /. It is harmless only by accident: it is served at /static/robots.txt, and crawlers ask for /robots.txt, which is a 404 served and exported alike. vashti copied the file as it stands. Fix both halves: the seed's robots.txt allows everything and names the sitemap (next item); and a short fixed list of root files - robots.txt, favicon.ico - is answered at the root from static/ (site route, before the page lookup) and written to the root of dist by the exporter. No general 'root files' mechanism: a list of two. Tests: server.test.ts and export.test.ts.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: ROOT_FILES (robots.txt, favicon.ico) answered at the root and exported there; the seed's robots.txt allows everything."}
{"n": 77, "status": "fixed", "severity": "medium", "area": "export", "file": "server/export.ts, server/search.ts, server/routes/", "summary": "No sitemap.xml, though the exporter already knows every page's canonical URL", "detail": "Both sites are published to be found, and neither has a sitemap. buildIndex() already walks every readable page and leaves drafts out; canonicalPath() gives the address. Write dist/sitemap.xml from the same walk (url + lastmod from `date:` when the page has one), and answer /sitemap.xml on the served site from the same cached index (dropped by pagesChanged() like the nav). With no DUCKDOWN_ORIGIN an exported sitemap can't be valid (it needs absolute URLs): skip it and say so in the existing origin warning. 404.md is left out (see n66). Document in the skill's publishing section. Tests: export + served, draft excluded, origin missing.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: sitemap.xml served (routes/sitemap.ts) and exported with DUCKDOWN_ORIGIN; from buildSite()'s page list, not the section index (n82)."}
{"n": 78, "status": "fixed", "severity": "medium", "area": "export", "file": "server/export.ts", "summary": "The exporter could check the site's internal links, and nothing else does", "detail": "reference.md says 'Links aren't checked'. The exporter has every page rendered and every static file listed, so it is the one place a check is nearly free. A scratch scan of the two real dists found vashti's news page pointing at include/music/*.mp3, two files that were never converted; website was clean (0 of 364). After writing, collect href/src from each page, resolve relative ones against the page's address, skip anything with a scheme and bare #fragments, decode entities and percent-escapes (Hart&#x27;sLeap.jpg is a real file - a naive scan calls it broken), and `say` each miss as 'news/index.html -> include/music/x.mp3'. Report, don't fail: add `--strict` (or DUCKDOWN_STRICT=1) to exit non-zero for a site that wants its deploy stopped. Return the count in Exported. Tests: a page with a good link, a bad one, an external one, an entity-encoded one.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: brokenLinks() in export.ts; `--strict` / DUCKDOWN_STRICT=1 fails; count in Exported.broken. Found the seed's own /login link, so /edit, /login and /logout are exempt."}
{"n": 79, "status": "fixed", "severity": "low", "area": "site", "file": "server/routes/site.ts", "summary": "The served site ignores the site's own 404 page that the published one uses", "detail": "Extends n66. serve.ts answers a miss with dist/404.html, so a published site with pages/404.md has a proper not-found page; the same site served by duckdown answers the bare string 'Not Found'. Same content, two behaviours, depending on flavour. In routes/site.ts, a miss renders pages/404.md through pageHtml with status 404 when it exists (drafts to signed-out readers too). Keep 404.md out of search.ts's index and the sitemap, and ship one in the seed so n66's 'undemonstrated' closes with it. Tests: server.test.ts both ways.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: notFound() renders pages/404.md with a 404 status, for a miss and a draft to a stranger; kept out of search, listings and the sitemap; the seed ships one."}
{"n": 80, "status": "fixed", "severity": "medium", "area": "create", "file": "create/setup.ts, server/init.ts (new), package.json, README.md", "summary": "Two ways in, one scaffold: install (duckdown as a dependency, upgrades by tag) and create (own the code)", "detail": "Supersedes this item's first wording, which replaced create with the dependency shape. Peter's decision: keep both, make the choice explicit. The two layouts differ in exactly one thing - where server/ lives - because the site folder (pages/, static/, templates/ behind DUCKDOWN_PATH) is already independent of the code. INSTALL (the documented default; what blueshed/website and vashti are): `bun add github:blueshed/duckdown#<tag>` then `bunx duckdown init`. Code stays in node_modules/duckdown/server/, scripts run it by that path, an upgrade is a tag bump. CREATE (ejecting: for someone who needs a different duckdown): `bun create blueshed/duckdown my-site`, as now. Code is ./server/, scripts run server/main.ts, and there is no upgrade path - bun create strips the history, so say plainly in the README that someone who wants ownership AND upstream merges should fork on GitHub instead. Build nothing for that. ONE SCAFFOLD: a single exported function (e.g. scaffold(root, { vendored: boolean }) in server/init.ts) writes everything the two share - site/ (index.md, theme.css, templates/site.html; NOT site.css or search.js, which come from server/base/ via n73's fallback), .env, .gitignore (node_modules, dist, .env, *.pid, .DS_Store, site/users.json), .claude/skills/duckdown, .claude/launch.json, a published-flavour Railway file with DUCKDOWN_PATH and DUCKDOWN_ORIGIN spelled out, a short CLAUDE.md, and package.json scripts dev/stop/export/start whose paths are the only thing the flag changes (start = duckdown's serve from n71). create/setup.ts calls it vendored after its tidy-up; a new bin `duckdown init` (import.meta.main guard, throws rather than process.exit) calls it in dependency mode, pinning the dependency to the installed package's own version tag. It must refuse to overwrite an existing site/ or package.json scripts - say what it skipped. That gives the bins a real user: close n65 with this. TESTS SURVIVE OWNERSHIP: setup() currently deletes tests/ and bunfig.toml, so the owner inherits 1,700 lines held at 100% coverage and loses the suite on day one - exactly when they intend to change the code. In create mode keep tests/, bunfig.toml and the seed they run against (tests/example); still remove feature.md, todo.jsonl, create/, and the railroad/bun-route skills only if the editor's tests don't need them (they don't; they are authoring aids - but keep them, an owner edits the editor). If keeping the suite proves impractical, stop and say why rather than quietly deleting it. MOVING BETWEEN THEM is possible and worth one README paragraph, not a command: to take ownership, copy node_modules/duckdown/server to ./server and repoint the scripts; to go back, delete ./server and add the dependency. The content never moves. ORDER: after n71 (serve) and n73 (server/base/), so the scaffold copies neither. Tests: setup-script.test.ts runs the scaffold in both modes on scratch folders and asserts the tree, that the only difference in package.json is the script paths, that init refuses to clobber, and that `exportSite` works against each scaffolded site/. Update README (install first, create second, the table of differences), the skill's publishing section, and CLAUDE.md's Project Structure.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: rewritten after discussion with Peter - create AND install, sharing one scaffold; tests kept in create mode; n73's base files live in server/base/ so the same fallback serves both. | 2026-09-21: fixed: server/init.ts scaffold(root, {vendored}); `duckdown init` (server/cli.ts) and create/setup.ts both call it; scripts' paths are the only difference; init never overwrites and says what it left alone; create keeps server/, tests/, bunfig.toml and the railroad/bun-route skills; tests run both modes plus `bun run export` in each. railway.json holds build/start/healthcheck; the two variables are listed in the generated CLAUDE.md, since Railway's file cannot carry them. README has install first, create second, the table and the fork advice."}
{"n": 81, "status": "fixed", "severity": "low", "area": "site", "file": "server/routes/static.ts, server/routes/site.ts", "summary": "The served site sends no validators, so every view re-downloads every file", "detail": "Probed in-process: a page answers with Content-Type only, and so does /static/site.css. serve.ts (published) sets max-age=300; the served flavour sets nothing, so browsers heuristically cache or refetch 15MB of vashti's images at will. Add an ETag (Bun.hash of the bytes is enough) and answer If-None-Match with 304 on /static/*, plus the same short max-age serve.ts uses; leave pages uncached when a user is signed in (the {{edit}} link differs). Low because both current sites are published, not served.", "note": "2026-09-21: from a review of the two sites that depend on duckdown (Workshop/blueshed/website, Workshop/vashti). | 2026-09-21: fixed: conditional() ETag/304 on static files and signed-out pages; max-age=300 (no-cache in development); a signed-in page is private, no-cache."}
{"n": 82, "status": "fixed", "severity": "medium", "area": "search", "file": "server/search.ts, tests/example/static/search.js, server/export.ts", "summary": "Search finds the page but not the place: index by section, and link to the heading", "detail": "Peter's report from vashti: searching a song title finds /jadd/ - a 559-line page of twenty songs - and drops the reader at the top. The belief was that the pages have no anchors. They do: renderMarkdown asks Bun.markdown for heading ids, so the exported page already has <h2 id=\"train-song\">, duplicates numbered (same, same-1), and site.css already gives :target headings scroll-margin-top. What's missing is that the index has one entry per PAGE with all its text in one string, so a result can only ever link to the page. FIX, server side: one entry per SECTION. In buildIndex, render the page (renderMarkdown - do NOT reimplement Bun's slugger; read the ids out of the HTML so they can never disagree with the page) and split the rendered HTML at <h1-6 id=...>. Text before the first heading is the page's own entry (url = canonicalPath, as now). Each heading starts an entry { url: canonical + '#' + id, title: page title, section: heading text, description: '' , date, text: that section's words }. Get the words by stripping tags from the section's HTML and decoding the five entities, dropping <pre> blocks first as plainText does for fences; then plainText() and its tests either go or are reduced to what is still used - a line no test reaches is dead. Keep `description` on the page entry only. Index size stays about the same (same words, cut differently) - check vashti's search.json stays near its current 150KB. The sitemap (n77) must NOT take its URLs from this index any more if it does - it wants pages, not sections: give buildIndex's walk a page-level list both can use. FIX, browser side (static/search.js): score() gains the section heading between title and description (a word in the heading: 8). A result shows 'Page title - Section' and links to entry.url with its #id. Group so one page can't fill all eight slots: at most three sections per page, best first. extract() uses the section text. SECOND HALF - the words themselves, for a hit deep in a long section with no heading near it (vashti's news page: 950 lines, 27 headings, long entries): append a text fragment to the link - url + '#' + id + ':~:text=' + encodeURIComponent(the matched words as they appear in entry.text). The browser scrolls to and highlights the phrase; supported in Chrome, Safari 16.1+ and Firefox 131+, and a browser that doesn't know it ignores everything after ':~:' and still lands on the heading, so it degrades to the first half for free. Rules: use the first term's actual occurrence from entry.text (original case, not the lowercased copy), a few words of it (3-6) so it is unique enough, percent-encode, and '-' ',' '&' inside the text must be encoded too (encodeURIComponent leaves '-' alone: replace it with %2D, because a bare dash is fragment-directive syntax). For a page entry with no id the fragment is just '#:~:text=...'. A same-page result (you searched from the page the hit is on) must still scroll: setting location.hash to the same page is a fragment navigation, which works - test it by hand, and close the panel on click. This changes what search.json holds, so: the skill's reference.md search section, the guide page, CHANGELOG, and note for sites that forked search.js that an old search.js still works against the new index (extra `section` field ignored, urls now carry #ids - that is the point) - with n73's fallback most sites won't have a fork. Tests: search.test.ts - a page with two headings yields three entries with the right urls; duplicate headings get -1; a draft yields none; text in a fence is left out; heading text with & and an apostrophe. editor/happy-dom can load search.js against a staged index to assert grouping and the fragment encoding. Then check vashti by hand at 375: search 'train song', land on the song.", "note": "2026-09-21: asked for by Peter, from using search on vashti. | 2026-09-21: fixed: buildSite() cuts pages at headings using the rendered ids; search.js scores section, groups three per page, links url#id:~:text= ('-' encoded), closes the panel after the click. tests/search-js.test.ts loads the real script in happy-dom. Checked by hand at 320/375/768 on the seed (no overflow; a real click lands on the heading). Not yet checked on vashti: search 'train song' at 375 once it runs this version. Index size is unchanged in kind (same words, cut differently). | 2026-09-21: the claim that a browser ignoring the text fragment still lands on the heading was wrong in some Chromium builds: see n84, fixed."}
{"n": 83, "status": "fixed", "severity": "low", "area": "pages", "file": "server/nav.ts, server/page.ts, .claude/skills/duckdown/reference.md, tests/example/pages/", "summary": "A site map a reader can read: {{sitemap}} lists every page, as {{pages}} lists a folder", "detail": "n77 is sitemap.xml, for crawlers. This is the other old thing of the same name: a page that lists the whole site, which is also the honest fallback when search doesn't find it. nav.ts already walks the tree for the nav and builds a folder's listing for {{pages}}; {{sitemap}} in a page is the same listing made recursive - nested <ul class=\"sitemap\">, each folder labelled by its index.md's title (linked to the folder's address), pages by title, in the same order {{pages}} uses, drafts and 404.md left out, folders with a leading '-' left out as the nav leaves them out (check what {{pages}} does and match it). Filled in pageHtml exactly where {{pages}} is: outsideCode so the guide can print the tag, needs no request so export gets it, cached and dropped by pagesChanged() like the rest. Style through the existing variables in the base site.css - a nested list needs almost nothing. The seed gets pages/sitemap.md (title: Site map, not in the nav) and the template's footer or the 404 page links to it; 404.md is the natural place to use it. Update reference.md's placeholder/tag table and the guide with it. Tests: markdown/nav tests for nesting, a draft, a '-' folder, the tag inside a code span left as written; export.test.ts that sitemap.html lists a nested page.", "note": "2026-09-21: asked for by Peter, from using search on vashti. | 2026-09-21: fixed: {{sitemap}} in pageHtml via nav.ts siteMap() (shares folderEntries with {{pages}}); seed pages/sitemap.md, linked from 404.md; ul.sitemap in base site.css; export writes sitemap.html."}
{"n": 84, "status": "fixed", "severity": "high", "area": "search", "file": "server/base/search.js, server/base/site.css, tests/search-js.test.ts", "summary": "On vashti a search result does not scroll: the text fragment cancels the jump to the heading", "detail": "Verified against a scratch copy of vashti exported with the unreleased tree and served by server/serve.ts, in the desktop app's browser at 375x812. Searching 'train song' gives the right results (Diamond Day - Train Song -> /jadd/#train-song:~:text=Train%20wheels%20beating%2C%20the%20wind, '-' encoded, panel inside the viewport, input 16px, no sideways scroll). A REAL CLICK on the first result loads /jadd/ and stays at the top: scrollY 0, the heading 3058px down. Isolated by navigating directly: '/jadd/#train-song' scrolls (scrollY 3042, heading 16px from the top); '/jadd/#train-song:~:text=...' does not; '/jadd/#:~:text=Train%20wheels%20beating' does not. So in this browser a text directive that isn't honoured also suppresses the element-id fallback. n82's detail said an unsupporting browser 'still lands on the heading' for free - that was my claim and it is wrong at least here (an embedded Chromium; document.fragmentDirective exists). It may well work in desktop Chrome and Safari; it must not depend on that. It passed on the seed presumably because the seed's pages are short or the check was same-page. FIX 1 - search.js owns the fallback. search.js is on every page, so on arrival: on 'load' (not DOMContentLoaded - vashti's /jadd/ has 22 images with no width/height, and the heading moves 3000px as they load), read location.hash (the browser strips the ':~:' part from it, so it is '#train-song'), find the element, and if the reader hasn't scrolled (scrollY === 0) and the element isn't already in view, el.scrollIntoView(). If the text fragment DID work the page is already scrolled and this does nothing. Keep the text fragment: where it works it is the better landing. Decode the id (decodeURIComponent, in a try that logs - no bare catch). FIX 2 - the panel has no height limit. With 8 results at 375 wide the panel is 1144px tall on an 812px screen (measured); it is position:absolute so the page grows and the input scrolls away from the results. In server/base/site.css: .search-results { max-height: min(60vh, 28rem); overflow-y: auto; overscroll-behavior: contain; } so the input stays put and the list scrolls. Check 320/375/768 and desktop. FIX 3 - body matches are substrings. 'train song' ranks /look/reviews.html#bbc and #all-music-guide 5th and 6th, because 'train' is inside 'constraints' and 'restraint', and the text fragment then points at 'constraints of domesticity'. score() already makes a title match start a word ('art' finds Articles, not Stuttgart); do the same for section, description and text - one word-start regex per term, built once per search, not per entry. The phrase for the text fragment should start at that same word-start hit. Tests: search-js.test.ts - arrival with a hash and scrollY 0 calls scrollIntoView on load, and doesn't when already scrolled or when the id is missing; 'train' does not match an entry whose only hit is 'constraints'. Then repeat the real check: export a copy of ../../vashti/site-new (delete its static/site.css and search.js first), serve it with server/serve.ts, 375 wide, search 'train song', CLICK the first result, and assert the heading's getBoundingClientRect().top is under ~100 - not just that the URL is right. This blocks the release: n82 is its headline.", "note": "2026-09-21: found reviewing the unreleased tree against vashti. Everything else checked held: 282 tests at 100%, tsc clean; exporting vashti without its copied site.css/search.js writes both from server/base/, robots.txt and favicon.ico at the root, sitemap.xml with 15 page URLs and no #sections, 151 search entries in 160KB, and the link check reports exactly the two known mp3s (Hart's Leap's apostrophe is not a false positive); serve answers 301 /jadd, 400 for a bad escape, 404 for traversal, and logs pages and 404s but not static files; the empty-export guard runs before rmSync. | 2026-09-21: fixed in server/base/. (1) search.js arrive(): on load, if location.hash names an element, scrollY is 0 and the heading is out of view, scrollIntoView(); a bad escape is warned, not swallowed. (2) .search-results max-height min(60vh, 28rem), scrolls, overscroll contained. (3) score(), extract() and link() use one word-start regex per term, built once per search; the fragment starts at that hit. Tests in search-js.test.ts: arrival (fires, waits for load, not when scrolled or in view or no hash, above the window, missing id, undecodable hash) and 'train' not matching 'constraints'. Re-verified as asked on a scratch export of vashti/site-new without its site.css and search.js, served by server/serve.ts, real click at 375x812: 'train song' first result lands on /jadd/#train-song, scrollY 2872, heading 186px down (text fragment honoured this time); a fragment that cannot match (#train-song:~:text=zzz\u2026) lands with the heading 16px from the top, the case that failed before; result list 448px tall and scrolling, panel 517px, no sideways scroll. | 2026-09-21: independently re-verified by the reviewing session on a fresh export of vashti/site-new served by server/serve.ts, 375x812, real click on 'Diamond Day - Train Song': lands on /jadd/ at scrollY 3042 with the heading 16px from the top and 'Train Song' on screen; 'constraints'/'restraint' no longer among the results (6 results, was 8); list 448px and scrolling, panel bottom at 673 of 812; no sideways scroll. 288 tests at 100%, tsc clean."}
{"n": 85, "status": "fixed", "severity": "low", "area": "deploy", "file": "server/init.ts", "summary": "The scaffold writes railway.json, which both real sites just moved away from", "detail": "railway.json can only hold build/start/healthcheck - no variables - so DUCKDOWN_PATH and DUCKDOWN_ORIGIN still have to be set by hand in the Railway dashboard, and the generated CLAUDE.md says so as a workaround. website and vashti both moved to a .railway/railway.ts file (applied via `railway config plan` / `railway config apply`) so the whole deploy, variables included, is one file a review can see. Make scaffold() write that instead. Not for this release.", "note": "2026-09-22: raised during the 0.3.0 release review, not built. 2026-09-22: scaffold() now writes .railway/railway.ts (build, start, healthcheck, DUCKDOWN_PATH and DUCKDOWN_ORIGIN in one file, applied with railway config plan/apply) instead of railway.json, and adds a railway devDependency for its railway/iac import. CLAUDE.md and README.md agree, matching website's and vashti's own .railway/railway.ts; tests/setup-script.test.ts updated, 100% coverage held."}
{"n": 86, "status": "fixed", "severity": "medium", "area": "nav", "file": "server/markdown.ts (buildNav, KEYS), server/nav.ts, .claude/skills/duckdown/reference.md, tests/example/pages/guide/", "summary": "The nav's order is the folders' alphabetical order and nothing can change it without renaming a URL", "detail": "buildNav lists folders by name. The reference says 'name folders so they sort', which means the URL carries the ordering (1-about/), and renaming an existing folder to reorder it breaks every link to it. On vashti alphabetical gives Heartleap, Diamond Day, Lookaftering, News, Some Things..., Wayward - the last album first, the sixties fourth, News in the middle; Peter wants the albums in the order they were made and News last, and /news/ can't move. ADD `order:` to front matter (KEYS in markdown.ts, so it works in a plain block): an integer on a folder's index.md. buildNav sorts siblings by order ascending, then by folder name; a folder without order sorts after every numbered one, still alphabetical, so a site that never uses it sees no change. Root stays first. Depth-first as now, so a folder's order applies among its siblings only. Non-integer values are ignored (treated as unset) and, per 'failures speak', logged once in development. Consider whether {{pages}} and {{sitemap}} should honour it too: {{pages}} is date-then-title for posts and should stay so; {{sitemap}} lists folders and should follow the nav's order so the two agree - do that. Docs: reference.md's navigation section (replace the 'name folders so they sort' advice with order:, keep the folder-name fallback), the front-matter key table, the guide's pages.md; CHANGELOG. Tests: markdown.test.ts nav order with mixed numbered/unnumbered folders, a bad value, nested folders; sitemap follows. Then vashti sets order: on things (1), jadd (2), look (3), heartleap (4), wayward (5), news (6).", "note": "2026-09-22: asked for by Peter while adding the Wayward section to vashti: 'can News be the last item'. Goes in the release after 0.3.0. | 2026-09-22: fixed, folded into 0.3.0 at Peter's request rather than waiting. order: added to KEYS; folderOrder() and sortFolders() in markdown.ts, shared by buildNav and {{sitemap}}'s buildSiteMap; numbered folders first ascending, then unnumbered alphabetical; non-integer values ignored and logged once in development. Root unaffected. Docs (reference.md's navigation section and front-matter table, the guide, CHANGELOG) and tests (mixed numbered/unnumbered, a bad value, nested folders, sitemap following) done. vashti sets things:1, jadd:2, look:3, heartleap:4, wayward:5, news:6."}
{"n": 87, "status": "fixed", "severity": "low", "area": "skill", "file": ".claude/skills/duckdown/SKILL.md, .claude/skills/duckdown/reference.md, server/init.ts", "summary": "The authoring skill sends a session to a site's DEPLOY.md, which a site may not have", "detail": "SKILL.md line ~73 and reference.md lines ~367 and ~383 say a site's deployment is 'its DEPLOY.md'. vashti now keeps that in README.md (Peter: 'readme for humans, claude.md for you'), and a scaffolded site (n80) writes a CLAUDE.md and no DEPLOY.md, so the skill names a file that on most sites won't exist. Say instead: the site's README.md or CLAUDE.md says how it is deployed - read whichever it has, and don't invent a step that isn't written down. Three edits; no test. Check what the scaffold writes about deploying so the skill and the scaffold agree.", "note": "2026-09-22: found while folding vashti's DEPLOY.md into its README. 2026-09-22: SKILL.md (two spots) and reference.md (two spots) now say a site's README.md or CLAUDE.md says how it is deployed - read whichever it has - instead of naming DEPLOY.md. Matches what scaffold() writes (n85): it has never written a DEPLOY.md."}
{"n": 88, "status": "fixed", "severity": "low", "area": "serve", "file": "server/serve.ts, tests/serve.test.ts, CHANGELOG.md", "summary": "The published server answers on any host; with DUCKDOWN_ORIGIN set, another host should 301 to it", "detail": "blueshed.co.uk and www.blueshed.co.uk will both point at the Railway service once the apex is on a CNAME-flattening DNS (website ledger n1), and the same for anotherday.co.uk. Today serve.ts serves the site on whichever host arrives, so a reader on the apex stays there; the canonical link says www, which keeps search engines straight but leaves two addresses in the wild. When DUCKDOWN_ORIGIN is set and the request's host (x-forwarded-host, then host) differs from the origin's host, answer 301 to origin + path + query - relative to nothing, this one must be absolute, that's the point. Skip when DUCKDOWN_ORIGIN is unset, for /health, and for localhost, so dev and healthchecks are untouched. Log the view as now. Tests: host match serves; mismatch 301s with the path and query kept; unset origin serves anything; /health never redirects. CHANGELOG line for sites: set DUCKDOWN_ORIGIN and the apex redirects to www for free.", "note": "2026-09-22: raised while writing the apex runbook for both sites; a proxy-side redirect (Cloudflare rule) would do it but this keeps every record DNS-only. | 2026-09-22: built. serve.ts otherHost(): with an origin, x-forwarded-host, then Host, then the URL's host; a mismatch is 301 to origin + path + query; unset origin, the origin's own host, localhost and /health are served. serveDist takes origin as a fourth parameter (default empty), listen() passes DUCKDOWN_ORIGIN. Tested in serve.test.ts; 300 pass at 100%. CHANGELOG under Unreleased. A site picks it up by bumping its duckdown pin and having DUCKDOWN_ORIGIN set."}
{"n": 89, "status": "fixed", "severity": "medium", "area": "collections", "file": "server/collection.ts (new), server/page.ts, server/markdown.ts, server/routes/site.ts", "summary": "Collections: pages/<folder>/collection.json gives each item an address, rendered through a template with prev/next", "detail": "Driving case: antonydonaldson.com (Workshop/tony-site), ~440 works in sections -> optional subsections -> items {src, title, caption, index, prints}. Data: an ordered array of groups, each with optional subgroups and items, next to the folder's index.md and read through storage (local or S3). Array order is the prev/next order; prev/next wrap; keyed by index or slug, never by name (1960 has two groups called London). Each item is a page at /<folder>/<slug>/ rendered through `layout: item` (templates/item.html) with {{item-src}}, {{item-thumb}}, {{item-title}}, {{item-caption}}, {{prev}}, {{next}}, {{group}} and {{groups}} (section and subsection nav, the current one marked). An item slug that collides with a page fails loudly (the export's checks, and the editor's Notice). Goes through pageHtml() so the site, the preview and the export all render it; parsePage() is where an item is looked up when no .md answers. The nav and search caches drop on a collection.json write (pagesChanged), which the pages route already does for anything under pages/. Skill (.claude/skills/duckdown) and the seed's guide pages describe it with the feature.", "note": "2026-09-22: fixed. server/collection.ts: collection.json beside a folder's index.md, read through storage and cached like the nav (collectionsChanged(), called from routes/pages.ts beside pagesChanged and searchChanged). Each item is a page at /<folder>/<slug>/ through itemPage() + pageHtml(), so the site, the preview and the export render it identically. Decisions the item left open: the lookup is itemAt(pages, name), called from routes/site.ts when no .md answers, not inside parsePage() - parsePage is sync over a source string and an item lookup needs storage; the three folder addresses (/x, /x/, /x/index.html) all reach an item; an item's {{edit}} opens the collection.json it is written in. Slugs are [a-z0-9-] with accents folded, duplicates -1/-2 in file order, item-<n> for a title with nothing usable in it. A collision with a page is reported by collectionProblems() in the preview (Preview.tsx speaks it) and in bun run export, where --strict fails on it."}
{"n": 90, "status": "fixed", "severity": "medium", "area": "collections", "file": "server/collection.ts, server/page.ts", "summary": "Collection overviews: {{items}} is a thumbnail grid of the folder's collection, {{items by=<field>}} groups it by a field", "detail": "{{items}} on a collection folder's index page: the groups in order, each a grid of thumbnails linking to the item pages. {{items by=index}}: one grid per distinct value of that field, in order of first appearance (or sorted when the values are years); an item whose field is the string \"skip\" is left out. The year fallback tony-site needs (index, else a 4-digit year found in the title or caption) is a documented option on the placeholder, `{{items by=index year}}` or similar, not a rule baked into the field. Generated HTML with plain classes (.collection, .group, .item, .thumb), styled by theme.css through the variables; no loops in templates, exactly like {{pages}} and {{nav}}.", "note": "2026-09-22: fixed. {{items}}, {{items <collection>}}, {{items by=<field>}} and {{groups}} (also with a name), filled by fillCollections() over the page body outside <code> and again over the template, before {{content}}. Generated HTML with .collection/.group/.items/.item/.thumb, styled from --thumb and --thumb-gap in server/base/site.css. Per the porting session: the year fallback is dropped (the converter writes an explicit index on every item); a heading's id is the field's value when it can be a fragment and a slug when it can't (fragmentId), so a template can write href=\"/works/#{{item-index}}\"; \"labels\": {\"<field>\": {\"<value>\": \"<label>\"}} makes the heading \"1967 - California\"; \"skip\" leaves an item out and also fills {{item-<field>}} as nothing, so that href lands at the top of the overview rather than at #skip."}
{"n": 91, "status": "fixed", "severity": "low", "area": "collections", "file": "server/collection.ts", "summary": "A collection's images may live at a root outside static/: an \"images\" setting in collection.json", "detail": "tony-site's originals, 128px thumbnails (<name>_tn.png) and banners are on S3 at ad.blueshed.info/images/{original,thumbnails,banners}. collection.json takes an optional \"images\" base URL; {{item-src}} and {{item-thumb}} are built from it and the item's src, and without it they resolve under the site's own static/images/. The thumbnail naming rule (a suffix) is a setting too, defaulting to _tn.", "note": "2026-09-22: fixed. \"images\" is a base URL, or {src, thumb, suffix, extension} - thumb for a separate thumbnails folder and extension for thumbnails that are all .png whatever the original is, which is what ad.blueshed.info needs. suffix defaults to _tn and goes before the extension. Without the setting both resolve under /static/images/. An item with no src has no src and no thumb rather than a link to a folder."}
{"n": 92, "status": "fixed", "severity": "medium", "area": "collections", "file": "server/search.ts, server/sitemap.ts, server/export.ts", "summary": "Items are pages to search, the sitemap and the export", "detail": "buildSite() walks pages/; a collection's items must be in the same walk so search.json carries an entry per item (title, caption as the body), the sitemap lists their addresses, and `bun run export` writes each item at /<folder>/<slug>/index.html. brokenLinks() checks the thumbnails' targets. One walk, one cache, as now.", "note": "2026-09-22: fixed. buildSite() loads a collection.json it meets in its own walk and emits one Entry (title, the caption as the words) and one PageRef per item, so /search.json, dist/search.json and sitemap.xml carry them. export.ts renders each item at <folder>/<slug>/index.html through the same pageHtml. brokenLinks() checks the thumbnails for free, since they are ordinary src attributes - an absolute images base is skipped like any off-site link. The same walk now also returns the aliases (n93), so it is still one walk and one cache."}
{"n": 93, "status": "fixed", "severity": "medium", "area": "collections", "file": "server/routes/site.ts, server/serve.ts, server/markdown.ts", "summary": "Old addresses survive a move: a page or item can carry aliases that 301 to it", "detail": "tony-site's ~440 works are at flat URLs /<slug> today and those must keep working after the port. Rather than an option to mount a collection's items at the site root (which makes every miss a lookup across every collection and lets items shadow pages), items live at /<folder>/<slug>/ and a page or item lists `aliases:` (front matter for a page, a field for an item, or a generated list in collection.json); a request for an alias is a 301 to the canonical address. The export writes each alias as a tiny HTML redirect page (meta refresh + canonical) so the published flavour keeps them too. A parity check that follows redirects sees the same page.", "note": "2026-09-22: fixed, as aliases rather than a root mount (the porting session confirmed nothing needs the flat URLs to answer 200). \"aliases\" is a field on an item and repeatable front matter on a page (added to KEYS in markdown.ts); buildSite() collects both and aliasTarget() matches on the decoded path, so /l%22etoile-1976 finds the alias l\"etoile-1976. The site answers 301 to the canonical address; the export writes a small redirect page (canonical + meta refresh) at <alias>/index.html under the decoded name, which serve.ts finds after its own decodePath, so a published copy keeps them. An alias that is already a page leaves the page alone and is reported. Known limit, opened as n95: a filesystem or host that can't hold a name with a quote or a backtick in it can't keep that alias in the published flavour."}
{"n": 94, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/CollectionPane.tsx, server/routes/collection.ts, server/images.ts", "summary": "Editor: a collection pane \u2014 edit titles and captions inline, reorder, add an item by dropping an image, swap an image in place, manage groups", "detail": "Last and lowest priority; the site can go live with collection.json edited as a file. Add an item: upload the original through the browse route and write a 128px thumbnail with Bun.Image (present in Bun 1.4.2). Swap an image: same filename, cache-bust with ?v=<mtime or hash>. Add, rename, remove groups. Reference: tony-site src/edit (delta + railroad) and src/lib/s3.ts. No undo in duckdown: rely on bucket versioning on S3, and say so in the skill.", "note": "2026-09-22: proposed by the tony-site session. | 2026-09-22: built. CollectionPane.tsx opens in the slot ResourcePane uses (one thing below the page: opening either closes the other), offered automatically when a folder's index.md is opened and nothing else is in that slot, and from the tree - collection.json is listed beside the pages now, with a grid icon, and opens as the pane rather than as JSON. Titles and captions inline (on change, not per keystroke); reorder items by dragging (within a group or into another, dragged by the picture) and with up/down; groups reorder, rename (label when it has one, else name), gain subgroups, and go; add and remove items. Every change replaces the raw model and writes the whole file through PUT /edit/pages/<folder>/collection.json, serialised through one promise chain, and collectionChanged() has the preview re-render. Decisions the item left open: the pane keeps the RAW json, not the parsed Collection, so every key it doesn't show (year, index, slug, aliases, prints) survives untouched; only title and caption are editable - other fields are a file edit, deliberately; the upload is its own route (/edit/collection/*, GET problems+images, POST the picture) rather than an extension of browse, because only the collection knows where its pictures go and what its thumbnail is called - server/images.ts now holds imageUrl/thumbName so the pane and the renderer resolve the same URLs; ?v= is a hash of the uploaded bytes, from the server; a collection whose images base is outside static/images/ is read-only for pictures and says so (409 + a note in the pane); drag was verified by test only (CDP mouse drags don't start an HTML5 drag). Two things fixed in passing rather than filed: LocalStorage.write is now atomic (write beside, rename on) because a pane that writes on every change turned a torn read into something you watch, and the editor now stacks below 768px (tree capped and scrolling, preview stands down) - at 375px it was a 240px tree beside two columns one word wide. Skill, reference, the seed's guide page, CLAUDE.md and CHANGELOG say what the pane does and that there is no undo. 391 tests at 100% line and function coverage."}
{"n": 95, "status": "fixed", "severity": "low", "area": "export", "file": "server/export.ts, server/serve.ts, .claude/skills/duckdown/reference.md", "summary": "A published alias whose old address holds a character a filesystem won't take is dropped without a word", "detail": "An alias is exported as <alias>/index.html under the name a request decodes to, so serve.ts and any host that maps a path to a file find it - including l\"etoile-1976 and don’t-write-everything-down on macOS and Linux (tests/serve.test.ts proves the round trip). A filesystem that won't take the character (Windows refuses \") or a host that normalises names silently loses that alias, and the export says nothing. Either check that each alias name round-trips and say so when it doesn't, or write a _redirects map beside dist/ for hosts that read one. The served flavour answers 301 from the index and needs none of this, so it only bites a site that publishes.", "note": "2026-09-22: found while doing n93; both real sites are served, so nothing is broken today. 2026-09-24: planned for 0.7. 2026-09-24: fixed by checking each alias as it is written. A write the filesystem refuses (Windows' \", ENAMETOOLONG, ENOTDIR when the folder name is already a page) is caught; it had been crashing the whole export, not just dropping the alias. After each write, lands() reads the directories back segment by segment, which catches a filesystem that keeps the name under another spelling. Either way the alias is left out and reported as a problem (so --strict fails on it) and the rest is written. The _redirects map was not built: both real sites are served, and a host that reads one can be its own item when a site publishes to one. 2026-09-24: released in 0.8.0 (master had already released a 0.7.0, with reports)."}
{"n": 96, "status": "fixed", "severity": "low", "area": "collections", "file": "server/collection.ts", "summary": "{{items by=<field>}} can order its groups by value: sort=asc or sort=desc", "detail": "Groups come in first-appearance order, which for a collection written newest first is newest first; the old antonydonaldson.com shows oldest first, the one parity difference left. sort=asc|desc orders the groups by value, numbers as numbers (years) and the rest by string; items inside a group keep the file's order.", "note": "2026-09-22: asked for by the tony-site session for 0.4.0; built the same day. sortValues() in collection.ts, an option parsed by parseArgs like by=."}
{"n": 97, "status": "fixed", "severity": "medium", "area": "serve", "file": "server/serve.ts, server/init.ts, tests/serve.test.ts", "summary": "A published site deployed before its domain exists can't be viewed: serve.ts redirects its own Railway address to DUCKDOWN_ORIGIN", "detail": "tony-site, 2026-09-22: deployed to Railway with DUCKDOWN_ORIGIN set to the real domain (as the scaffold's railway.ts comment says), the deploy went green, and every request to tony-site-production.up.railway.app was 301'd by the 0.3.1 host redirect (n88) to the old site still holding that domain. Nothing broke, but the new site could not be looked at before cutover, when looking is the point. Every site will hit this. Design: the platform's own address is never a site's canonical name, so serve.ts treats a *.up.railway.app host (and localhost) as a place to look, not a place to be: served, never redirected, with X-Robots-Tag: noindex on every answer and /robots.txt answered as 'User-agent: *\\nDisallow: /' whatever the site has, so nothing there is indexed before or after cutover. Any other host that isn't the origin's still 301s (the apex to www). No variable to set or forget; DUCKDOWN_ORIGIN stays the real domain from the first deploy, so canonical links are right from day one. The scaffold's railway.ts comment says this. Tests: a railway.app host serves with the header and the robots answer; the origin's host serves without them; another host still moves.", "note": "2026-09-22: raised by the tony-site session (Peter: 'I do not see the site in railway - it just goes to the existing site'). tony-site worked around it with DUCKDOWN_ORIGIN as the railway address and a Disallow-all robots.txt, both on its cutover checklist; when this lands both go. | 2026-09-22 (late): built. serve.ts looking(host): localhost, 127.0.0.1 or *.up.railway.app is served whatever the origin (otherHost skips it), X-Robots-Tag: noindex on every answer there (serveDist), /robots.txt there is 'User-agent: *\\nDisallow: /' regardless of the site's file; the scaffold's railway.ts comment says to set the real domain from the first deploy. Tested in serve.test.ts. tony-site's workaround can go once it pins the release."}
{"n": 98, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/Browser.tsx, server/edit/components/NewDialog.tsx", "summary": "A collection can be edited in the editor but not started in it: a folder with no collection.json has no way to get one", "detail": "The collection pane (n94) edits a folder's collection.json and the tree lists it, but nothing in the editor creates one: the New buttons make a page and a folder, and a folder without a collection.json shows no offer. Someone told 'duckdown does galleries' opens the editor, finds no way in, and has to write the first file by hand or ask a session to. Smallest shape: a 'New collection' item on the folder's own header (or a fourth kind in NewDialog) that writes {\"layout\": \"item\", \"images\": {\"src\": \"/static/images/<folder>/\"}, \"groups\": [{\"name\": \"<folder>\", \"items\": []}]} with If-None-Match: * and opens the pane on it. Ask for the images folder, or default it to the folder's name. Also worth deciding whether the folder's index.md should get {{items}} appended when it hasn't got one, or whether that is too clever.", "note": "2026-09-22: found while building n94; nothing is broken, it is a way in that isn't there. 2026-09-23: in scope with n100 at Peter's word; being built in a worktree agent. Built: New collection in the tree header writes collection.json (declared fields), item.md (each:) and index.md if missing, create-only, and opens the pane."}
{"n": 99, "status": "fixed", "severity": "low", "area": "collections", "file": "server/edit/components/CollectionPane.tsx", "summary": "Renaming a work in the pane moves its page and says nothing about the address it leaves behind", "detail": "An item's slug comes from its title, so editing a title in the collection pane changes /<folder>/<slug>/ - the old address 404s at once, and anything linking to it (a printed catalogue, a search engine, another page) breaks. The file supports this already: aliases on the item is a 301 to the new address, and the export writes a redirect page. The pane should offer it - on a title change where the slug moves, say so once in the notice ('First Light is now /gallery/first-light-at-dawn/') with the old address added to aliases, or ask. Deliberately left out of n94 as scope; relevant the moment a real gallery is edited, because the tony-site port is exactly a set of addresses that must keep working.", "note": "2026-09-22: found while building n94, and visible in the dev run: renaming First Light moved the work with no word about /gallery/first-light/. 2026-09-23: in scope with n100 at Peter's word; being built in a worktree agent. Built: a committed field edit that moves an item's address adds the address the file had when opened to aliases (none for items added this sitting, none where an item now answers; renaming back removes it), and tell() says so. Slug rule moved to server/slugs.ts so both ends share it. Not covered: -1/-2 and item-<n> shifts from moves/removals."}
{"n": 100, "status": "fixed", "severity": "high", "area": "collections", "file": "server/collection.ts, server/page.ts, server/markdown.ts (KEYS), server/export.ts, server/search.ts, server/edit/components/NewDialog.tsx, CollectionPane.tsx, init.ts, the skill, the seed", "summary": "Collections redesigned: data in collection.json, transforms in pages. `each:` makes one page per item; `collection:` binds a page; `layout` leaves the data file; fields are declared", "detail": "Peter's design, 2026-09-23, after reviewing 0.4.0: a collection is addressed more than once (an overview by year, an overview by prints, a folder of item pages), so it cannot own a template; today's `layout` key in collection.json is wrong, the binding of page to collection is inferred from the folder rather than written down, fields are whatever keys the items happen to carry, and a collection can only be started by writing the file. The model: DATA lives in collection.json and declares itself — `fields: [{name, kind, label?}]`, `images`, `labels`, `groups` (no `layout`). TRANSFORMS live in markdown files, as for every page. (1) One page per item: a markdown file with front matter `each: <collection>` and `layout: <template>`; its body is the content of every item page with {{item-<field>}}, {{prev}}, {{next}}, {{group}}, {{groups}} filled, wrapped in the named template like any page. At pages/works/item.md it makes /works/<slug>/ for every item, as index.md makes /works/. (2) An overview: an ordinary page that says `collection: <name>` in front matter (optional when it sits in the collection's folder) and carries {{items …}} / {{groups …}} in its body; pages/works/index.md with {{items by=index sort=asc}}, pages/prints.md with {{items works by=prints}}. Consequences: the item template is an ordinary template and nothing special ships with the scaffold; a second presentation is a second markdown file, never a change to the data; the editor's New dialog offers 'a collection' (asks for its fields, writes the declared file) and 'pages from a collection' (writes an each: page); the pane shows a form per item from `fields`; `by=` and {{item-<field>}} are checked against the declaration and speak when a field doesn't exist; search, sitemap and export find item pages through the each: page in the walk, not by meeting collection.json. Compatibility: a collection.json with `layout` and no `fields` keeps rendering for one release (fields inferred, layout honoured with a logged warning) so tony-site's converter can catch up; then it goes. Skill, reference and the seed's guide rewritten to say: a collection is a folder, a list of fields, and the pages that present them.", "note": "2026-09-23: opened by Peter's review — 'having the template be in the collection is wrong'. Build this before tony-site's converter writes 440 items against the 0.4.0 shape. Supersedes n98 (creation) and folds in n99's concern (rename → alias offer belongs in the pane's form). 2026-09-23: server side built (6a6fb90) and documented (389ffc6), kept simple at Peter's word ('enough to work tony-site'): one each: page per collection, in the collection's own folder (so item addresses are unchanged); `each: <folder>` must name that folder; front matter title:/description: take {{item-…}}; kinds text/long/image/number, one image field; a collection with no each: page has no item pages and unlinked thumbnails; fillItem also fills markdown's %7B%7B…%7D%7D in link addresses; the preview of an each: page is its first item. 0.4 files (layout, no fields) render with a once-only warning. Editor side (pane form from fields) with n98 and n99 in a worktree agent. Editor side merged (pane form from declared fields; route returns fields). Also {{items template=<name>}} (b852333) at Peter's review: an overview names the template each item is drawn with, the counterpart of the each: page's layout."}
{"n": 101, "status": "fixed", "severity": "bug", "area": "serve", "file": "server/serve.ts", "summary": "A site whose DUCKDOWN_ORIGIN is its railway.app address was noindexed and had robots.txt closed at that address; localhost matched by prefix", "detail": "looking(host) treated every *.up.railway.app host as a preview address even when it was the origin (vashti: https://vashti-production.up.railway.app), so serveDist added X-Robots-Tag: noindex to every page and route() answered robots.txt with Disallow: / — contradicting the site's own sitemap. It also tested host.startsWith(\"localhost\"), so localhost.example.com counted as a local run. Found by the agent upgrading vashti to 0.5.0.", "note": "2026-09-23: looking(host, origin) is false for the origin's own host and compares the hostname whole; serve.test.ts covers both. Released as 0.5.1. Still open, not fixed here: the host is read from x-forwarded-host, which a client can set if the proxy passes it through — only that client sees the noindex answer, and nothing caches it (Cloudflare is DNS-only)."}
{"n": 102, "status": "fixed", "severity": "high", "area": "export", "file": "server/export.ts", "summary": "An alias with .. segments made the export write a redirect page outside dist/", "detail": "The alias went straight into outPath() and join(out, ...), so `aliases: ../../escaped` on a page (or in collection.json) wrote escaped/index.html two folders above dist/ — anything an editor can write could overwrite an index.html beside the export. aliasFile() now answers null for a . or .. segment and the export reports it as a problem.", "note": "2026-09-23: fixed; tests/export.test.ts covers aliasFile() and the export end to end."}
{"n": 103, "status": "fixed", "severity": "medium", "area": "export", "file": "server/export.ts", "summary": "An exported alias ending .html 404'd on a static host", "detail": "The served site matches /old.html through aliasKey(), but the export stripped .html and wrote old/index.html; serve.ts and a static host look for old.html, so the published redirect was never found. aliasFile() writes the name as given when it ends .html.", "note": "2026-09-23: fixed."}
{"n": 104, "status": "fixed", "severity": "medium", "area": "tests", "file": "tests/helpers.ts, tests/server.test.ts, tests/editor.test.tsx", "summary": "The export tests failed when server.test.ts ran first", "detail": "All test files share one scratch site, and server.test.ts left drafts (secret/, news/hidden.md) and a broken link in it, so export.test.ts counted 3 drafts and extra broken links. Whether the suite passed depended on the order the filesystem listed the files in. keepSite() copies the site before a file's tests and restores it after, dropping the nav, search and collection caches.", "note": "2026-09-23: fixed; server.test.ts and editor.test.tsx call keepSite(); passes in either order."}
{"n": 105, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/history.ts, server/routes/files.ts, server/edit/components/History.tsx, PaneHeader.tsx, Browser.tsx, ResourceList.tsx", "summary": "Earlier versions and Deleted: nothing the editor writes is lost", "detail": "There was no way back from a save or a delete short of git or bucket versioning, which the person using the editor is not going to reach for. fileRoutes() now keeps what a save replaces (once a sitting: none newer than 10 minutes) and what a delete removes (always) under .history/<section>/<key>/<time>, the last 30 per file, through the storage layer. The same routes answer ?versions, ?version=, ?deleted and POST ?restore=, which keeps what it replaces. The editor: a clock in every pane header, and Deleted atop the page tree and each resource list.", "note": "2026-09-23: built before Vashti's first session. Dot-folders are not listed by Deleted, as they aren't in the tree. Pictures uploaded through /edit/collection/ are not versioned."}
{"n": 106, "status": "fixed", "severity": "low", "area": "collections", "file": "server/edit/components/CollectionPane.tsx", "summary": "Undo and redo in the collection pane", "detail": "Every pane change is a whole new file, so the pane keeps the files it replaced (up to 100) and undo writes the one before; redo the one after. ⌘Z / ⇧⌘Z / Ctrl+Y anywhere in the pane but a field being typed in, and arrows in its header. A rename's kept alias goes with the rename. Loading or restoring a version starts the stack afresh.", "note": "2026-09-23: fixed."}
{"n": 107, "status": "fixed", "severity": "medium", "area": "serve", "file": "server/hosts.ts, server/routes/site.ts, server/serve.ts", "summary": "A served site with a domain answered in full under every name: apex, www and its railway.app address, each self-canonical", "detail": "The one-address rules (301 another host to DUCKDOWN_ORIGIN; noindex and a closed robots.txt on localhost and *.up.railway.app) lived only in serve.ts, the published server. vashti went served on 2026-09-23 just as anotherday.co.uk moved to Cloudflare and both names were added to Railway, so once the certificates issue it would be indexed three times.", "note": "2026-09-23: hosts.ts holds hostOf/looking/otherHost/hostAnswer, moved out of serve.ts unchanged; serve.ts's route() and the served site's siteHandler(origin) (routes/site.ts, handleSite = siteHandler(ORIGIN)) both ask it. Pages only on the served side: the other routes answer under any name. Side effect, deliberate: localhost's robots.txt is the closed one on a served dev run too, as for bun run start; the two robots tests now ask under a site name. 439 tests, 100%. Released as 0.6.1. vashti still has to set DUCKDOWN_ORIGIN=https://www.anotherday.co.uk in its served variables once both certificates are issued."}
{"n": 108, "status": "fixed", "severity": "medium", "area": "collections", "file": "server/collection.ts, server/slugs.ts, server/routes/site.ts, tests/collection.test.ts, feature.md, CHANGELOG.md, .claude/skills/duckdown/reference.md", "summary": "Remove the 0.4 collection shape (layout in the data, no fields): 0.5's changelog promised it would go in the release after", "detail": "0.5.0 said a 0.4 collection.json (\"layout\" in the data, fields inferred from the items) is read with a warning 'for one release'. We are at 0.6.1. Delete the fallback in collection.ts (the layout-as-empty-each: page, the inferred fields, the once-only log line, Collection.layout), whatever in slugs.ts and routes/site.ts exists only for it, and the tests that prove it; a 0.4 file then loads as a collection with no declared fields and no each: page, and collectionProblems() should say so plainly (\"no fields declared: add them, see the 0.5 upgrade note\") rather than render nothing silently. Tick feature.md's unticked 0.4 line, and put the removal and what an old file now does in the 0.7 changelog's Upgrading section.", "note": "2026-09-24: planned for 0.7. 2026-09-24: fixed. \"layout\" in the data is a problem naming the each: page to write; no each: page comes of it. No fields means PLAIN (src the picture, title, caption), defined in collection.ts and served to the pane, rather than fields guessed from the items; any other key is a problem saying to declare them. The Collection.declared and Collection.layout properties are gone, and the pane's own copy of PLAIN with them. The pane draws nothing when it can't read the fields, rather than drawing items against a guess. Changelog 0.7.0 (unreleased), CLAUDE.md, feature.md, skill reference and guide updated. 2026-09-24: released in 0.8.0 (master had already released a 0.7.0, with reports)."}
{"n": 109, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/routes/files.ts, server/routes/pages.ts, server/edit/store.ts, server/edit/components/Browser.tsx, server/edit/components/PaneHeader.tsx", "summary": "A page can't be renamed or moved in the editor; doing it by hand loses its address", "detail": "Items get this already (a committed title change adds the old address to aliases). Pages should too: Rename/Move in the page pane's header takes a new name or folder, and the server does it in one request (POST /edit/pages/<key>?move=<to>, in fileRoutes so every section gets it and history keeps the old file): create the new key only if nothing is there (412 otherwise), add the page's old canonical address to its aliases: front matter (unless the page is a draft, which never had a public address), delete the old key, drop the nav, search and collection caches. Refuse index.md (that is moving a folder: every page beneath would need an alias; a later item), an each: page and collection.json (they belong to their folder). A move onto an address an alias already claims should drop that alias from wherever it was. The editor opens the page at its new key and tells the news ('about.md moved; /about.html still answers'). Wiki links elsewhere that pointed at the old name keep working through the 301 but are worth fixing: n110 shows them.", "note": "2026-09-24: planned for 0.7. 2026-09-24: fixed. POST /edit/pages/<key>?move=<to> in fileRoutes, enabled by a Mover only pages pass (templates and stylesheets answer 405: pages name them). It refuses a target that exists (412), a change of case alone (400: on a case-insensitive filesystem the write-then-remove would delete the file), non-.md names, index.md and an each: page. The old canonical address goes into aliases: with addMeta() (after the existing front matter, fenced or bare, or a new block), and an alias equal to the new address comes off with dropMeta(). A draft keeps none. History.move() takes the versions along, merging with any the new name had and trimming to KEEP, and the pre-move page is kept at the new name. The editor has Rename or move in the page header (NewDialog, with a heading, a starting value and an action), moveFile() saves unsaved changes first and tells the kept address. Checked in Chromium as well as happy-dom. Not done: an alias on another page or item that claims the new address is left in place (the export already reports it as 'already a page'); moving a whole folder is still a later item. 2026-09-24: released in 0.8.0 (master had already released a 0.7.0, with reports)."}
{"n": 110, "status": "fixed", "severity": "low", "area": "editor", "file": "server/export.ts, server/routes/mark.ts, server/search.ts, server/edit/components/Preview.tsx", "summary": "Broken links are only found by bun run export; the preview could say so while the page is being written", "detail": "brokenLinks(pages, known) checks rendered HTML against the files the export wrote. The preview already returns problems beside the html (collectionProblems), and the Notice shows them. Give PUT /edit/mark/ the same check for the one page being previewed, against the addresses the site answers: every entry and page of buildSite() (pages, items, folders) plus aliases (aliasTarget) and static/ files, so it costs one cached walk, not an export. Move the link-extraction half of brokenLinks into a shared helper so the export and the preview can't disagree about what counts as a link. Report each dead link once per render, as a problem, never an alert on every keystroke: the preview debounces, but the Notice should only change when the set of broken links does.", "note": "2026-09-24: planned for 0.7. 2026-09-24: fixed. server/links.ts: linksIn() is what counts as a link for both the export and the preview (unescapeHtml, the scheme, #fragment and editor-address rules moved there from export.ts). brokenLinks() keeps the export's check against the files it wrote. deadLinks() is the preview's check, against the cached index (pageList, and a new aliasTargets), plus static/ exists and base/root files for only the files the page names, so it costs no export and no static walk. PUT /edit/mark/ adds 'Links that lead nowhere a reader can go: …' to problems (not when previewing a template through a sample page). Preview.tsx says it when the set changes and takes its own line down once the problems are gone, but never someone else's. Known differences: an alias counts as reachable by aliasKey, as the served site answers it, so /old.html passes for an alias of /old even though the export writes only old/index.html. A link typed half-way can show red for a moment (the preview debounces at 300ms) and clears when finished. In DEBUG the index is rebuilt per render, as the nav already is. 2026-09-24: released in 0.8.0 (master had already released a 0.7.0, with reports)."}
{"n": 111, "status": "fixed", "severity": "medium", "area": "site", "file": "server/search.ts, server/nav.ts, server/export.ts, server/main.ts, server/markdown.ts, .claude/skills/duckdown/reference.md, tests/example/pages/blog/index.md", "summary": "No feed: a blog on duckdown can't be followed in a feed reader", "detail": "Opt in per folder: feed: true on a folder's index.md makes /<folder>/feed.xml, an Atom feed of that folder's dated pages (the ones {{pages}} lists, newest first, drafts and 404 left out as they are everywhere), each entry title, canonical url, date (updated = date: at midnight UTC unless the page says a time), description as the summary and the rendered body as content. Built from the same buildSite() walk and cached with it, dropped by the same write path. {{feed}} in a template is the <link rel=\"alternate\" type=\"application/atom+xml\"> for the folder the page is in (empty when that folder has none), so a reader's browser can find it. Atom needs absolute ids and links: served, from siteOrigin(req); exported, from DUCKDOWN_ORIGIN, and like sitemap.xml the export only writes feeds when it is set (and says so when a folder asks for one and it isn't). A new key in KEYS and the skill's reference together; the seed's blog shows it.", "note": "2026-09-24: planned for 0.7. 2026-09-24: fixed. server/feed.ts builds Atom over nav.ts's folderEntries (exported, now with each page's key), so the feed and {{pages}} agree on drafts, - files, each: pages and order. Only entries whose date: parses (a feed is what's new and Atom needs a time); the feed's updated is the newest entry's, 1970 when there are none. The author is the origin's host, since Atom wants one and there is no author key. Content is the rendered markdown, escaped, with xml:base the page's own address so relative links resolve. The data is cached per folder without an origin and dropped by feedsChanged() from the pages route; feedXml() writes it for the request's origin or DUCKDOWN_ORIGIN. The site route answers …/feed.xml (a folder with no feed falls through to a 404). The export writes it only with an origin and says so otherwise, and {{feed}} is empty when pageHtml has no origin, so an export never links to a feed it didn't write. The seed's blog says feed: true and site/post/item templates have {{feed}}, which new sites get from the scaffold; existing sites add it (changelog). deadLinks() counts a feed as somewhere. Also fixed a flaky test: the sitemap 'not 404' check failed when the random port held 404. 2026-09-24: released in 0.8.0 (master had already released a 0.7.0, with reports)."}
{"n": 112, "status": "fixed", "severity": "low", "area": "site", "file": "server/page.ts, server/markdown.ts, server/collection.ts, tests/example/templates/site.html, .claude/skills/duckdown/reference.md", "summary": "A shared link shows no card: only og:description is written, never og:title, og:url or og:image", "detail": "{{description}} writes meta description and og:description. Add the rest where the template already says {{description}} (so every site gets it without editing its template): og:title (the page's title), og:url (the canonical address, as {{url}}), og:type (article for a dated page, website otherwise), and, when there is a picture, og:image and twitter:card summary_large_image. The picture is a new image: front matter key (a path under static/ or an absolute URL, guarded like css:), or on an item page the item's image field, resolved with imageUrl(). og:image must be absolute: the origin comes from the request or DUCKDOWN_ORIGIN, and with neither it is left out rather than written relative. A new key in KEYS and the skill's reference together.", "note": "2026-09-24: planned for 0.7. 2026-09-24: fixed. {{description}} writes og:title, og:type (article when dated), og:url (encoded) and, with a picture, og:image and twitter:card summary_large_image, beside the description tags it already wrote, so every template with {{description}} gets a card. New image: key; pictureUrl() in page.ts: a full http(s) URL as is, a site path or a path under static/ after the origin, nothing with a .. segment or without an origin; characters a URL can't hold are encoded and an existing %-escape kept, because an item's picture arrives encoded. itemMeta() gives an item its picture as image unless the each: page says image: (filled, so {{item-detail}} works). og:url and og:image are left out of an export with no DUCKDOWN_ORIGIN. A template with hand-written og: tags gets duplicates: changelog says to remove them. Not added: og:site_name (duckdown has no site-name setting). 2026-09-24: released in 0.8.0 (master had already released a 0.7.0, with reports)."}
{"n": 113, "status": "fixed", "severity": "high", "area": "auth", "file": "server/auth.ts, server/main.ts, server/routes/users.ts (new), server/edit/components/Users.tsx (new), server/cli.ts, .claude/skills/duckdown/SKILL.md", "summary": "User management: adding, removing or re-passwording an editor means hand-editing users.json with a hash made elsewhere", "detail": "users.json sits at the site root, out of every editor folder, so hashes never reach the browser; keep that. Add /edit/users (its own route, never fileRoutes): GET lists names only, never hashes; POST adds a user with a password (or answers with a one-time sign-in link to hand over, the user setting their own password on first use); DELETE removes one; PUT changes a password (your own needs the current one). Refuse removing the last user, or yourself while you are the only one. Sign-outs must actually happen: the JWT is stateless, so getUser() also checks the user still exists and the token is not older than their last password change; that needs users.json read on each authed request, so cache it the way the nav is cached and drop it on every write here. That changes the file's shape from {name: hash} to {name: {hash, changed}}: read both, write the new one. DUCKDOWN_ADMIN_PASSWORD still wins for its user at every start (the platform holds that secret), so the editor shows that user as set by the environment and won't change its password. CLI for a site with no server running: duckdown user add|remove|list|passwd (prompting, never taking a password as an argument). Roles: decide before building - every editor equal (today's model) is simplest; if some users may not manage users, that is a role field and a check in this one route. n115's duckdown remote needs tokens a local editor can use to push; issue them here (a named, revocable token per user, stored hashed like a password). Two rules for existing sites (added 2026-09-24): going back to an older duckdown must still sign everyone in, so users.json must stay readable by it; and a session made before the upgrade must stay valid (count 'no record' as 'never changed'), or everyone is signed out once.", "note": "2026-09-24: planned for 0.7 (now after 0.8.0). Roles are the decision to make first. 2026-09-24: decided, editors are equal: no roles. Every signed-in editor may manage users; the last-user and self-removal guards stay. 2026-09-24: fixed, with no change to users.json's shape at all: a session carries fingerprint() of its user's hash (sha256, 16 chars, inside the signed token), and getUser() checks the user still exists and the fingerprint still matches, so a new password or a removal ends their sessions without a session list or a 'changed' field. A cookie with no fingerprint (0.8 and earlier) stands until it expires. users.ts reads the file through currentUsers(), kept 5s (FRESH) and dropped by writeUsers(); login reads it afresh; a file that can't be read signs nobody in and says so, and the site stays up. /edit/users answers names only; POST adds (8+ characters), PUT sets a password (your own needs the current one, and the answer re-signs your cookie), DELETE removes. Every editor equal; you can't remove yourself (so 'the last user' can't happen either); the environment's admin is refused for both. Cross-site writes 403. Editors dialog in the header (components/Users.tsx). duckdown user list|add|passwd|remove (users.ts, via cli.ts) asks for the password (raw mode on a terminal, the first line from a pipe), never an argument. Not built: the one-time sign-in link, and API tokens for a duckdown remote — that remote kind isn't being built yet (git first). 2026-09-24: released in 0.9.0."}
{"n": 114, "status": "fixed", "severity": "high", "area": "config", "file": "server/config.ts, server/remote.ts (new), server/init.ts, CLAUDE.md, README.md", "summary": "A remote site: a local duckdown should know where its site is published, so editing happens locally and publishing is a push", "detail": "Today the scaffolded (published) flavour is edited by running duckdown locally and then committing site/ and pushing by hand; the served flavour is edited in place on the internet. Make 'where this site lives' configuration: DUCKDOWN_REMOTE, one of three kinds behind one interface (status, push, pull): git (commit the content folder and push the branch; the scaffold's Railway service rebuilds from it, so a push is a publish - no new infrastructure, the right first kind), https://<a duckdown> (a served duckdown, written through its own /edit/ routes with a token from n113, so its caches drop and its history keeps what a push replaces), and s3://bucket/prefix (a bucket a served duckdown reads; note its production caches won't hear about writes it didn't make, so this kind needs the server poked or is only for DEBUG/exported hosts). Record what was last pushed or pulled per file (key -> content hash, and the remote's version: git blob, ETag) in a manifest beside .history/, outside every served folder, so status is 'changed here / changed there / both' without fetching everything. users.json, .history/ and the manifest are never pushed. configLines() shows the remote; with none set nothing changes and no Publish appears. Decide which kinds 0.7 ships (git first, duckdown second, bucket later is the recommendation) before building; the interface should not assume git. Also decide whether reports/ is pushed. It sits in the content folder beside pages/ and is never served or exported, so carrying it into the repo is harmless, but reports are made from the platform's logs (n117) on the machine that edits, and a pull (n116) would then treat them as content that can conflict. Keeping reports/ off the push, like users.json, .history/ and the sync record, is the likely answer.", "note": "2026-09-24: planned for 0.7 (now after 0.8.0). Which remote kinds ship first is the decision to make. 2026-09-24: decided, git first. The duckdown and bucket kinds come after; the interface still mustn't assume git. 2026-09-24: added the reports/ question (it came up with n117). 2026-09-24: fixed. server/remote.ts: the Remote interface (name, status, push(message, by), pull(by)) and RemoteRefused for a reason the person can act on (answered 409). remoteFrom() reads DUCKDOWN_REMOTE (config REMOTE): unset is null and nothing changes; 'git' is the one kind; anything else, or git with a bucket, fails at startup with a line. The git kind finds the repository the content folder is in and works only on that folder, through pathspecs that exclude users.json, .history/ and reports/ whatever .gitignore says (decided: reports/ is never pushed). Status is git status of the folder plus ahead/behind its upstream, without fetching (fast); no upstream is a problem line, not an error. The git kind needs no sync record: git is the record, so the manifest waits for a kind that needs one. configLines shows the remote; the scaffold's .env mentions it. Not built: a fresh site pulling instead of seeding (for git that is a clone). 2026-09-24: released in 0.9.0."}
{"n": 115, "status": "fixed", "severity": "high", "area": "editor", "file": "server/routes/publish.ts (new), server/main.ts, server/remote.ts, server/edit/components/Header.tsx, server/edit/components/Publish.tsx (new), server/edit/store.ts", "summary": "Publish from the editor: with a remote configured, the header shows what has changed and pushes it", "detail": "Replaces feature.md's unticked 'duckie publish - deploy to S3'. With DUCKDOWN_REMOTE set (n114) the header gets Publish, with a count of files changed since the last push. It opens a list (added / changed / deleted, per section) and pushes them in one go: GET /edit/publish is the status, POST pushes. For git: one commit (message from the editor, defaulting to the files changed, author the signed-in user) then push; a rejected push (the remote moved) says so and offers pull. For a duckdown remote: each file PUT with If-Match on the version the manifest recorded, so a change made on the remote since is a conflict listed for that file, never overwritten; deletes likewise. Before any push, run the export's checks (n110's broken links, collection problems, render everything) and show them; refuse on failure only under DUCKDOWN_STRICT. Nothing is lost on either side: local history already exists, the remote's writes keep its own. Failures speak through api(); progress is news. Refuse to run on a server that is itself the remote (no DUCKDOWN_REMOTE, or DEBUG off on a deployed box): publishing is a local-editor action.", "note": "2026-09-24: planned for 0.7 (now after 0.8.0). 2026-09-24: fixed. publishSite() in remote.ts: checkSite() (export.ts: the whole export into a temp folder, keeping broken-link and collection lines, or why nothing exported), problems returned with the result and refusing only under DUCKDOWN_STRICT, a defaultMessage() naming up to three changes, then push. Commits carry Edited-by: <user> and use the machine's own git identity and signing. /edit/publish (routes/publish.ts): GET status, POST publish; 404 with no remote; RemoteRefused is 409 with its line. Header Publish button (components/Publish.tsx): hidden on 404, badge = changes + unpushed commits, refreshed 400ms after any write; the dialog lists changes, ahead/behind, a message, Publish (disabled when nothing waits) and Pull, and shows what the checks found. duckdown publish [message]. A real bug found end to end, not by the first tests: git add refuses a pathspec naming an ignored file even as :(exclude), and the scaffold ignores site/users.json and site/.history/ — now the folder is added whole and the never-pushed paths reset; the test repositories carry that .gitignore. Checked in Chromium against a real bare repository. 2026-09-24: released in 0.9.0."}
{"n": 116, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/remote.ts, server/routes/publish.ts, server/edit/components/Publish.tsx, server/cli.ts", "summary": "Pull from the remote: a local copy starts from, and catches up with, the published site", "detail": "The other half of n115, and what makes 'edit locally' safe when someone else edits too (or the served site's own editor was used). duckdown pull (CLI) and Pull in the Publish panel bring remote changes into the content folder: git pull (refusing, and saying why, when the working tree has uncommitted edits that would conflict); for a duckdown remote, fetch every file whose remote version differs from the manifest's, writing through fileRoutes' history so a pulled change can be taken back. A file changed on both sides since the last sync is a conflict: keep the local one, put the remote's in history, and list it. Pulling drops the nav, search and collection caches. A fresh site (duckdown init with DUCKDOWN_REMOTE set and an empty site/) pulls instead of seeding.", "note": "2026-09-24: planned for 0.7 (now after 0.8.0). 2026-09-24: fixed for git. pull(by): fetch; nothing behind is nothing; edits not yet committed are committed first ('Edits kept before pulling'), then merge the upstream. A conflict inside the site keeps this copy's side (checkout --ours, or git rm when this copy deleted it) and saves the other side's bytes into the file's history, where Restore finds it; a conflict outside the site or a merge git refuses is aborted and said. Returns what changed and what conflicted; the route drops the site's caches when anything changed; the dialog reloads the open page if it changed and has nothing unsaved. duckdown pull. A pull whose every change collided says so rather than 'nothing new'. Not built: a fresh site pulling instead of seeding — for git, clone the repository. 2026-09-24: released in 0.9.0."}
{"n": 117, "status": "fixed", "severity": "medium", "area": "reports", "file": "server/log.ts, server/report.ts (new), server/cli.ts, .claude/skills/duckdown/reference.md", "summary": "A published site's visitor report can't be made: the view lines are only in the platform's log stream, and nothing reads them", "detail": "A static site on Railway runs serve.ts with DUCKDOWN_LOG=1 (the scaffold sets it), so each view is a 'view <path> <status> <ms> [from=<host>] [crawler]' line on stdout, which lives only in Railway's logs. It isn't in the repo or the content folder, so DUCKDOWN_REMOTE (n114) can't reach it. Add `duckdown report`, which reads view lines on stdin (e.g. `railway logs | duckdown report`) and writes a markdown report into reports/<yyyy-mm>/ for the editor's Reports tab. It would show views per page, 404s worth fixing, the referring sites, and crawlers counted apart from readers. duckdown writes those lines, so parsing them belongs beside viewLine() in log.ts, and one shared format means the two can't drift. It needs no platform credentials: fetching the lines stays with the platform's own CLI. Lines that aren't view lines (the server's other output) are skipped. Running it twice over overlapping logs must not double-count: key each line by its timestamp if the platform gives one, or say that a report covers exactly the lines it was fed. The platform keeps logs for a limited time, so the docs should say to run it on a schedule. Privacy is unchanged: the lines carry no IP, user agent, cookie or session, so neither can the report. Decide first: master's 0.7.0 said duckdown writes nothing in reports/ ('it is for a site's own tasks'); this would be duckdown's first writer there, as a command a site chooses to run, never something the server does by itself.", "note": "2026-09-24: raised while planning n114. Deciding whether duckdown may write in reports/ comes first. 2026-09-24: decided yes, as a command a site chooses to run: duckdown writes in reports/ only through it, never by itself. Fixed: parseView() beside viewLine() in log.ts reads a view line back — from anything a platform prints before 'view' (a timestamp becomes 'at'), or a JSON line's message and timestamp. server/report.ts: reportCommand() reads a file argument or stdin (the file argument also keeps the test run from waiting on a terminal), and writes reports/<yyyy-mm>/<yyyy-mm-dd>.md with a title: readers and crawlers apart, 404s, 5xx when any, the top 20 read pages (reader views under 400), the top 404 addresses, the top referring sites. It keeps no count between runs: a report is of exactly the lines it was given, which is how overlapping logs never double-count; a second run the same day replaces that day's file and says so. No view lines is an error naming DUCKDOWN_LOG=1. Not built: scheduling it — the docs say to run it regularly, since the platform keeps logs for a limited time. 2026-09-24: released in 0.9.0."}
{"n": 118, "status": "fixed", "severity": "low", "area": "users", "file": "server/routes/users.ts, server/auth.ts, server/users.ts", "summary": "A user name like 'constructor' or '__proto__' reads Object's own properties from users.json's object", "detail": "NAME is /^\\S{1,100}$/, and users.json is parsed into a plain object, so users[name] for 'constructor', 'toString', '__proto__' and the like is an inherited property, not undefined. Seen in the 0.9.0 review: POST refuses 'constructor' as already signing in (users.constructor is truthy); PUT treats it as an existing user and writes a hash under that name - an editor can so create an account the Editors list never offered; DELETE of it is a no-op; a sign-in as one of these names hands Bun.password.verify a function or an object rather than a hash. Only a signed-in editor reaches the routes and no session can be forged, so nothing is exposed - but it is a quiet way to get a user the rules didn't mean. Fix: Object.hasOwn(users, name) wherever a name is looked up (routes/users.ts, auth.ts login and getUser, users.ts), or parse into Object.create(null); and a test with 'constructor' and '__proto__'.", "note": "2026-09-24: found reviewing PR #7 before merging it as 0.9.0; not blocking - the release went out with it open. | 2026-09-25: fixed. usersFrom() in users.ts: the names as an object with no prototype, used by readUsers(), by duckdown user when there is no file yet (the test caught that one; the route never starts from nothing), and by ensureAdmin's start. So constructor, __proto__ and toString are no one until added, and then ordinary names; the file's JSON is unchanged. Tests: over the route, sign-in refused (not a 500), PUT and DELETE 404, then added, signed in, listed and removed like anyone, both constructor and __proto__; from duckdown user, remove and passwd refused, add works."}
{"n": 119, "status": "fixed", "severity": "high", "area": "editor", "file": "server/edit/components/Browser.tsx, ImageBrowser.tsx, ResourceList.tsx, ReportList.tsx", "summary": "The editor's file trees can't be used from the keyboard: every row is an <li onclick>", "detail": "Browser.tsx (.., folders, files), ImageBrowser.tsx (folders, images), ResourceList.tsx and ReportList.tsx's folders are clickable list items with no tabindex, role or key handling, so without a mouse nobody can open a page, a stylesheet or an image. WCAG 2.1.1. Fix: a <button> inside each <li> carrying the click (report files already use <a>), styled to look as now, and aria-current on the open file. Tests: find each row's button by its name and click it.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-24: fixed: every row in Browser, ImageBrowser, ResourceList and ReportList is a <button class=\"row\"> inside its <li> (the way up is labelled 'Up a folder'); the open page's row has aria-current=\"page\", the chosen image's aria-pressed. styles.css: the button fills the row, a :focus-visible outline, the open/chosen row shaded. editor.test.tsx clicks each row's button (rowControl) and checks aria-current/aria-pressed. Seen in Chromium with the keyboard alone: Tab to a folder, Enter opens a page."}
{"n": 120, "status": "fixed", "severity": "high", "area": "editor", "file": "server/edit/components/ImageBrowser.tsx, server/edit/components/CollectionPane.tsx", "summary": "Uploading a picture needs a mouse: the image input is display:none, the collection's drop area a <div onclick>", "detail": "ImageBrowser wraps a display:none file input in a <label>: a hidden input takes no focus, so the label can't be reached by Tab. CollectionPane's 'Drop a picture' is a div with onclick. WCAG 2.1.1. Fix: hide the input visually (a .sr-only class) so it stays focusable and its label shows focus, or a <button> that calls input.click(); make the drop area a <button> keeping its drop handlers.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-24: fixed: the image upload's input is .visually-hidden rather than display:none, so Tab reaches it and its label shows the focus (:focus-within); the collection's drop area is role=button tabindex=0 with Enter/Space, through the same onpress() the picture thumbnail now uses (both hold the chooser's <input>, so neither can be a <button>). Tested: Enter and Space open the chooser, another key doesn't. Seen in Chromium: Tab lands on Upload."}
{"n": 121, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/components/Editor.tsx, ResourcePane.tsx, NewDialog.tsx, Users.tsx, Publish.tsx, ImageBrowser.tsx, ConfirmDialog.tsx, History.tsx, PreviewFrame.tsx, server/edit/login.html, server/auth.ts", "summary": "Unnamed controls in the editor: the textareas, the login fields, dialog inputs, the dialogs themselves, the preview iframe", "detail": "The page and resource textareas have no label; login.html's two inputs have placeholders only, no <label>, no autocomplete (username/current-password), its title is a div and its error (set by auth.ts) is not role=alert; NewDialog, ImageBrowser's rename, Users and Publish inputs are labelled by placeholder only, and their error lines are not announced; no <dialog> has aria-labelledby; PreviewFrame's iframe has no title. WCAG 1.3.1, 4.1.2, 1.3.5. Fix: aria-label (or a visible label) on each, ids on the dialog headings, role=alert on the error lines, title=\"Preview\".", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-24: fixed: the page and resource textareas are 'Contents of <name>'; every dialog is aria-labelledby its heading (via modal()); NewDialog's input 'Name', the image folder's 'Folder name', the Editors and Publish inputs labelled as their placeholders say; every .dialog-error is role=alert; each preview iframe has a title (of the page / stylesheet / template). login.html: visible <label>s ('Name or email', 'Password'), autocomplete username/current-password, the title an <h1>; auth.ts adds role=alert when it writes the error. Tested in editor.test.tsx and server.test.ts; seen in Chromium."}
{"n": 122, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/components/PaneHeader.tsx, Users.tsx, NewDialog.tsx, server/edit/styles.css", "summary": "Focus in the editor: lost when a dialog is confirmed, and invisible in the textarea", "detail": "Confirming a dialog removes it from the DOM without .close(), so focus falls to <body> instead of going back to the button that opened it. styles.css sets outline: none on the editor textarea with nothing in its place, and has no :focus-visible rule anywhere. WCAG 2.4.3, 2.4.7. Fix: close() before unmounting (or put focus back by hand); a :focus-visible style for the textarea and the editor's own controls.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-24: fixed: server/edit/modal.ts is every dialog's showModal, and its cleanup runs when the when() drops the dialog: close() it (a modal leaves the rest inert), then focus whatever had it when the dialog opened, if still in the page. All six dialogs use it. styles.css: the page/resource textarea shows focus as an inset ring (an outline would be cut off at the pane's edges), and one :focus-visible ring for everything else. Tested: a confirmed ConfirmDialog gives focus back to its opener, and forces it nowhere when the opener has gone. Seen in Chromium: Enter in New page makes the page and focus is back on the New page button. Not done: when the opener itself goes (Delete closes the pane it was in) focus still falls to <body>. | 2026-09-25: after the CSS review of 25 Sep 2026 (eta's, comparing paintbrush, simple, zeta, duckdown and eta) (every state needs a cue that survives forced colours): the textarea's focus is an inset outline, not a box-shadow, which forced colours removes; the open page's and the chosen image's row are bold as well as shaded, and outlined under forced-colors. Seen in Chromium with forced colours emulated. The rest of the editor was checked for the same: the tabs (a border), the unsaved dot (a glyph) and Save/Saved (its words) keep a cue."}
{"n": 123, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/ImageBrowser.tsx, Header.tsx, PaneHeader.tsx, Notice.tsx", "summary": "State the editor shows but doesn't say: the resource tabs, the Resources toggle, the unsaved dot, the Saved flash", "detail": "ImageBrowser's tabs mark the selected one by class only (role=tablist/tab + aria-selected, or aria-pressed); Header's Resources toggle has no aria-expanded; PaneHeader's unsaved '●' is read as 'black circle' (role=img, aria-label 'Unsaved changes'); the Saved / Not saved flash isn't announced. Notice.tsx inserts its live region together with its text, which some screen readers don't read: keep an empty region in the DOM and fill it. WCAG 4.1.2, 4.1.3.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: fixed. Resources' tabs are a role=tablist (aria-label Resources) of role=tab buttons, aria-selected, only the chosen one in the tab order, the arrows (round the ends), Home and End moving along them, over a role=tabpanel labelled by the chosen tab. The Resources button says aria-expanded. The unsaved dot is role=img 'Unsaved changes'. Saved / Not saved is said from a visually hidden role=status in the pane header, in the page before it's filled. The notice is two regions always in the page (role=alert, role=status) and the card is put inside the right one, so a screen reader is listening before anything is said. Tested in editor.test.tsx; the tabs by keyboard seen in Chromium."}
{"n": 124, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/styles.css, server/edit/login.html", "summary": "Editor colours below AA contrast: dim text, the dark accent, red text on dark, the green Saved button", "detail": "Computed: --text-dim #86868b is 3.3-3.6:1 in light; dark --text-dim #8888aa 4.2 on the surface; dark accent #7c6ff7 3.7 (folder names); white on it 3.8 (primary buttons); #dc2626 on the dark surface 3.0 (.dialog-error, .danger-subtle); login's #c00 2.4 on dark; white on #34c759 2.2 (Saved). Input borders 1.3-1.5 against 3:1 for controls. WCAG 1.4.3, 1.4.11. Fix: darken/lighten each to 4.5:1 (e.g. #6e6e73, #15803d).", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: the CSS review of 25 Sep 2026 (eta's, comparing paintbrush, simple, zeta, duckdown and eta) asks for contrast by rule, checked by a test: when fixing this, add one (the WCAG ratio of each token pair that draws text, 4.5:1, and each control edge, 3:1, light and dark, in site.css and the editor's styles.css), so a colour change can't quietly fail it again. | 2026-09-25: fixed. The editor's colours are tokens named for their job: --text-dim #6e6e73 / #9d9dbf; --accent the fill (dark #6458e8, 5.1 under white) apart from --accent-text, the accent as text (dark #a59cfa); --focus for rings and 'this one' marks (the tab underline, a field's focus); --field-border #86868b / #71719b for text fields' edges, --border kept for dividers; --danger/--danger-hover fills and --danger-text (#c81e1e / #f87171); --ok #15803d for Saved; --on-accent, --news. login.html uses them. tests/contrast.test.ts holds every pair (text 4.5, edges and rings 3) in light and dark for the editor, server/base/site.css and the seed's theme.css, and fails on a hex outside the token blocks; given the old values it names every pair above. It also found one on the site: the warning callout's title, #9a6700 on its 8% tint, was 4.4 in light: now #8a5c00. Not in this: the site's search input border (130)."}
{"n": 125, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/styles.css", "summary": "The editor ignores prefers-reduced-motion", "detail": "The notice's slide-in and the button transitions always run. Fix: a @media (prefers-reduced-motion: reduce) block that turns animation and transition off.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: the CSS review of 25 Sep 2026 (eta's, comparing paintbrush, simple, zeta, duckdown and eta): all motion stops under reduced motion — the notice's slide-in and every transition, not only the long ones. | 2026-09-25: fixed: a prefers-reduced-motion: reduce block at the end of styles.css stops every animation and transition (the notice's slide-in, the hovers)."}
{"n": 126, "status": "fixed", "severity": "medium", "area": "site", "file": "tests/example/templates/site.html, server/init.ts, server/page.ts", "summary": "No <main> and no skip link in the seed template every new site gets", "detail": "site.html puts {{content}} straight into <body>, after the topbar, and init.ts copies it into every scaffolded site: a keyboard reader tabs through the whole nav on every page and a screen reader has no main landmark. page.ts's BARE fallback also has no lang. WCAG 2.4.1, 1.3.1, 3.1.1. Fix: <a class=\"skip\" href=\"#content\">Skip to content</a> first in <body> and <main id=\"content\"> around {{content}} (post.html, item.html too), .skip styled in server/base/site.css (visible on focus); lang=\"en\" on BARE; the skill's reference and the templates guide say why. Existing sites keep their template: say so in the release notes.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: fixed: the seed's site.html and item.html open with <a class=\"skip\" href=\"#content\">Skip to content</a> and put the page in <main id=\"content\">; post.html (no nav before it) has <main>. .skip in the base site.css is off-screen until focused. page.ts's bare template is lang=\"en\" with <main>. The skill's reference shows the template with both and says why; server.test.ts checks the seed page and the bare one. Seen in Chromium: the first Tab shows it, Enter lands on #content. Existing sites keep their own templates: the release notes should say to add the two lines."}
{"n": 127, "status": "fixed", "severity": "low", "area": "site", "file": "tests/example/static/theme.css", "summary": "The seed's duck wobbles forever, whatever the reader's motion setting", "detail": "animation: wobble 3s infinite on #logo, and nothing in site.css or theme.css reads prefers-reduced-motion; the scaffold copies this theme.css into every site. Motion over 5s with no way to stop it is WCAG 2.2.2. Fix: wrap it in @media (prefers-reduced-motion: no-preference); consider a reduce block in base site.css for scroll-behavior and transitions.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: the CSS review of 25 Sep 2026 (eta's, comparing paintbrush, simple, zeta, duckdown and eta) counts duckdown as the only one of the modern three with no reduced-motion rule at all. | 2026-09-25: fixed: the seed's wobble is inside @media (prefers-reduced-motion: no-preference), and site.css ends with a reduce block (!important inside @layer base, so it stops a theme's animation too). The skill and the Styling guide tell a theme to do the same."}
{"n": 128, "status": "fixed", "severity": "low", "area": "site", "file": "server/base/site.css", "summary": "A wide table pushes the page sideways on a phone", "detail": "table is width: 100% with nothing letting it scroll, so a table wider than the screen makes the whole page scroll horizontally. WCAG 1.4.10. Fix: display: block; overflow-x: auto on table (or a wrapper the markdown pass adds), keeping it readable.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: fixed: markdown.ts wraps each markdown table in <div class=\"table-scroll\">, which site.css scrolls sideways; a table written in HTML is the author's and left alone. Measured at 390px on a seven-column table: the page was 683px wide, it is now 390 and the table scrolls in its own 350px box."}
{"n": 129, "status": "fixed", "severity": "low", "area": "collections", "file": "server/collection.ts", "summary": "An overview's thumbnail link reads its title twice, and the grid jumps as thumbnails load", "detail": "thumbLink() puts <img alt=\"<title>\"> and <span class=\"item-title\"><title></span> in the same link, so it is announced 'Sunflowers Sunflowers'. The img has no width/height, so layout shifts. Fix: alt=\"\" when the title is shown beside it; width/height (or aspect-ratio from --thumb) on .thumb.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: fixed: thumbLink()'s img is alt=\"\" (the title is printed beside it in the same link), and so is the seed's tile.html and the guide's and the skill's copies of it. The other half was wrong: the grid doesn't jump, because site.css's .item .thumb already reserves the space (width 100%, aspect-ratio 1). Item pages keep alt=title: there the picture is the work."}
{"n": 130, "status": "fixed", "severity": "low", "area": "site", "file": "tests/example/static/theme.css, server/base/site.css, .claude/skills/duckdown/", "summary": "Smaller site points: caption contrast, the search input's border, alt text in the skill", "detail": "The seed's .item-caption is --muted at opacity 0.8: 3.9:1 in light (4.5 needed). The search input's border #d1d9e0 is 1.4:1 (3:1 for a control's edge). The authoring skill never tells a writer that ![…] needs real alt text, or alt=\"\" for decoration. Self-linked headings make every heading a link, which some screen readers read on each one: consider it, no change needed yet.", "note": "2026-09-24: found in an accessibility audit Peter asked for (code read, contrast computed by the WCAG formula; no screen reader run). | 2026-09-25: fixed: site.css has --field-border (#818b98 / #656c76, 3:1 on both backgrounds) for the search box's edge, checked by contrast.test.ts; the seed's .item-caption lost its opacity (3.9 -> 6.1). SKILL.md and reference.md tell a writer to give every picture words that say what it shows (empty for decoration), and the Images guide says Copy Markdown's words start as the file name, to replace. Left as it is: self-linked headings (no change was asked). Not changed, a question for Peter: Copy Markdown still puts the file name in the brackets."}
{"n": 131, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/styles.css", "summary": "The editor's text is sized in px, so the reader's own font size is ignored", "detail": "From the CSS review of 25 Sep 2026 (eta's, comparing paintbrush, simple, zeta, duckdown and eta): duckdown's CSS counts 122 rem to 208 px, and nearly all the px are the editor's (styles.css: 13px text, 12px buttons, 11-9px details). A reader who sets a larger default font size gets a larger site and the same editor. Fix: a small type scale as tokens in rem (--text-sm, --text-base, …) on :root, and the font-size rules read them; spacing can stay px. Check the three columns and the 768px stack still fit at 16px and at 20px default.", "note": "2026-09-25: raised by that review. | 2026-09-25: fixed: the editor's type is a rem scale (--text-xs .. --text-xl, 11-16px at the default), every font-size reads it, and the page's 14px moved from html to body so a rem is the reader's size. The iOS guard is max(1rem, 16px). Spacing stays px, as the review advised. Seen in Chromium at a 20px default: the editor's text grows with it."}
{"n": 132, "status": "fixed", "severity": "low", "area": "css", "file": "server/base/site.css, server/edit/styles.css", "summary": "What modern CSS would simplify: light-dark() for the dark block, @layer for base and theme", "detail": "From the CSS review of 25 Sep 2026 (eta's, comparing paintbrush, simple, zeta, duckdown and eta), none of it a bug. site.css writes every colour twice, once in @media (prefers-color-scheme: dark): color-scheme: light dark plus light-dark() writes each once, and a theme that sets --accent: light-dark(a, b) says both in one line. @layer base, theme; would let theme.css override site.css by order, whatever the specificity, which is what 'a theme sets a few variables' already assumes. Also worth a look: text-wrap: balance on headings, scrollbar-gutter: stable. Hold light-dark() until it is safe in every browser a reader of a duckdown site may use; @layer is. Changing site.css changes every site that hasn't forked it, so the release notes say so.", "note": "2026-09-25: raised by that review. | 2026-09-25: done in part. site.css's rules are in @layer base, its variables outside, so a theme's rule wins by coming later whatever its specificity (the skill and the Styling guide say so); headings are text-wrap: balance. Deliberately not done: light-dark(). A browser without it (Safari before 17.5) drops the whole declaration, and a reader would get the browser's default colours, not duckdown's; the dark block costs a few lines and works everywhere. scrollbar-gutter doesn't apply: nothing on a site locks its scroll."}
{"n": 133, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/styles.css, server/edit/login.html, server/edit/components/Header.tsx, PaneHeader.tsx, Publish.tsx, Users.tsx", "summary": "The editor's controls were cramped: icon buttons 28x18, no sizing system, Delete 8px from Save", "detail": "Peter, 25 Sep 2026: 'those buttons are cramped as hell'. A design review of 15 screenshots (light, dark, phone) found: button.icon-btn had padding 0 and no height, so ~18px tall (under WCAG 2.5.8's 24px); the collection's tools were 16px apart by 2px; nothing shared a height (18px icons, 27px Save, 32px inputs); Delete sat 8px from Save; buttons and inputs used the browser's font, not the page's; the two file lists had different gutters; View and Logout were bare text; the login was cramped; and at 390px the page was 426px wide.", "note": "2026-09-25: fixed: :root tokens (--space-1..6 on a 4px grid, --control-h 30, --icon-btn 30, --field-h 34, --row-h 28, --icon 16, --radius-*), a size up under pointer: coarse; controls inherit the font; icon buttons square; header bars 48px, 4px between icons, Save behind a hairline and 16px from Delete; one gutter for the lists, hover lighter than the open row; dialogs' fields and buttons one height; login by the tokens. Phones: fields 16px on touch (iOS zooms below), header links icon-only below 768px and all four below 420px (names kept for screen readers). A pane's header answers to its pane (@container): no ⌘⏎ under 480px, and under 360px (the middle column between 768 and ~1000px) the name gets a row of its own and Save its icon. Dead CSS gone (.browser-list, .file-group, .new-options, .file-list .selected). Checked in Chromium at 320, 390, 768, 900, 1024 and 1440: no page scrolls sideways, no header overflows, no button under 28px. Also: the EditorsDialog test's intercept answered every request, and when the Header's Publish status check (400ms after it appears) met it, Publish got {ok: true} as a status and threw; the test now stages answers for /edit/users alone (0 failures in 12 runs, from 1 in 3)."}
{"n": 134, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/styles.css, server/edit/app.tsx, server/edit/components/Header.tsx, Browser.tsx, PaneHeader.tsx", "summary": "The bento's frame: one trail above, the columns as compartments cut into one tray", "detail": "From the design session of 25 Sep 2026 with Peter, putting eta's bento rules (Workshop/eta/examples/bento/README.md) to the editor as 0.10.0 left it. The canvas is https://claude.ai/artifact/Y1QWMsHCw5X8rHJepmzD3c (private to Peter: 'duckdown bento'), each state drawn beside 0.10.0's own. What stays, and is the point: three columns with one job each (where you are, what you are changing, what a reader would see), the words and the page side by side at equal weight, front matter as text, the one slot beneath the page, every pane's header with its tools in 0.10.0's order and sizes, the open file's light in the tree. This item: (1) one trail at the top left, 'duckie \u203a gallery \u203a index.md' (root, then the folder, then the open file bold; each crumb before the last goes there), replacing the three places that say it now: the tree's '/gallery' and its '..' row, and the 'gallery/' before each pane's name (the pane keeps the file's own name, since two can be open). The header's buttons stay at the right of the same line. (2) The columns, and the page and its slot beneath, become compartments cut into one tray on a darker paper: an 8px wall, square where compartments meet, the tray's curve (24px) only at its outer corners, the compartment's (16px) where one sits in a corner. (3) The preview sits flush in its compartment, with no inset frame. On a phone the tray is one column in today's order, curved at its top and foot, and the tree lays its files side by side so the page gets the height. Canvas artboards Main, Phone.", "note": "2026-09-25: fixed on claude/bento-editor. Trail.tsx says where you are (crumbsHere(): duckie, the tree's folder, the page in it); the tree's folder is the store's (folder, openFolder(), loadFile() moves it to the page's), so the tree lost its '/folder' and '..' and a pane header says shortName(). app.tsx puts the three columns in .tray: --wall 8px, --radius-tray 22px, --radius-cell 14px, paper and tray tokens light and dark (paper held to AA in contrast.test.ts), the preview flush, the middle's two panes two compartments. Phones: one column, the tree's files side by side, the trail giving way (folders to a few letters, the page's name last); #app's column is minmax(0, 1fr) so the header can't widen the page. Checked at 320 (in a 320px frame: the pane won't go under ~400), 390, 768 and 1280, light and dark: no sideways scroll."}
{"n": 135, "status": "fixed", "severity": "medium", "area": "collections", "file": "server/edit/components/CollectionPane.tsx, server/edit/styles.css", "summary": "The collection pane as a grid of pictures, with the chosen work's fields beside it", "detail": "From the design session of 25 Sep 2026 with Peter, putting eta's bento rules (Workshop/eta/examples/bento/README.md) to the editor as 0.10.0 left it. The canvas is https://claude.ai/artifact/Y1QWMsHCw5X8rHJepmzD3c (private to Peter: 'duckdown bento'), each state drawn beside 0.10.0's own. What stays, and is the point: three columns with one job each (where you are, what you are changing, what a reader would see), the words and the page side by side at equal weight, front matter as text, the one slot beneath the page, every pane's header with its tools in 0.10.0's order and sizes, the open file's light in the tree. This item: the pane shows each group's works as 56px pictures (8px corners) under the group's name, subgroups as 'Paintings \u203a Studies in Green', and an add tile last. Choosing one rings it and puts its fields beside the grid, inside the pane (236px): each declared field by its kind, the picture's file name, and replace, move up, move down, remove. Today every work is a card of inputs, so the pane shows two; the grid shows the collection, and the preview stays in view because the fields don't cover it. On a phone the groups run in one row and the fields sit under them. Keeps the raw JSON model, the write-on-every-change chain, undo and the aliases as they are: only the drawing changes. Check with a hundred works (the case the pane was written for) before settling the tile size. Canvas artboards Main, Phone.", "note": "2026-09-25: fixed on claude/bento-editor. Each work is a 60px tile (--tile) in its group, a button that chooses it (aria-pressed, a ring and bold); the chosen one's fields, picture (still the replace control, .item-thumb) and tools sit beside the grid in .collection-chosen, under it when the pane is under 480px. Chosen by position (Spot, same(), firstWork()): the first as it opens, a new work as it's added, and the choice follows a moved work. Model, write chain, undo and aliases unchanged. Inputs are named by a <label> now, not a placeholder. Not yet tried with a hundred works."}
{"n": 136, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/components/History.tsx, PaneHeader.tsx, Browser.tsx, ResourceList.tsx, server/edit/app.tsx", "summary": "Earlier versions and Deleted become a place where the tree was, not modals", "detail": "From the design session of 25 Sep 2026 with Peter, putting eta's bento rules (Workshop/eta/examples/bento/README.md) to the editor as 0.10.0 left it. The canvas is https://claude.ai/artifact/Y1QWMsHCw5X8rHJepmzD3c (private to Peter: 'duckdown bento'), each state drawn beside 0.10.0's own. What stays, and is the point: three columns with one job each (where you are, what you are changing, what a reader would see), the words and the page side by side at equal weight, front matter as text, the one slot beneath the page, every pane's header with its tools in 0.10.0's order and sizes, the open file's light in the tree. Peter chose this one: 'versions where the tree was'. A version is a place in time, so it goes where 'where you are' goes. The clock in a pane's header puts the versions in the tree's compartment (Now, then each kept version, newest first, time and size; the chosen one lit) and grows the trail by one crumb ('\u2026 \u203a index.md \u203a 23 Sep, 16:05'). The page's compartment shows that version read-only on a quieter ground, with the lines that differ from now marked, and the preview renders it (the mark route with that text). So a version can be seen before it is brought back, which the dialog can't do. Restore is one press beside it and asks nothing, since what it replaces is kept (as now); the x, or the crumb before, goes back to now. Deleted is the same place: the tree's archive button, and each resource list's, list what was deleted from that section ('duckie \u203a Deleted \u203a blog/old-draft.md'), and the chosen file shows read-only and rendered with Restore. The server already answers ?versions, ?version=<id>, ?deleted and ?restore=; the changed lines are a line diff in the editor. Canvas artboards Versions, Deleted.", "note": "2026-09-25: fixed on claude/bento-editor. past.ts (past, entries, seen; openVersions, openDeleted, look, restoreSeen, leavePast, changedLines with COMPARE_LIMIT) and Past.tsx (PastList where the tree is, PastPane read-only in the middle with <mark> lines, PastPreview rendering a page through /edit/mark/). The newest is shown at once; Now is a row; the trail grows a crumb (Trail.tsx crumbsThen()) and any crumb before leaves. The present's panes stay mounted, hidden by :has(), so unsaved words survive looking. History.tsx and its two dialogs are gone; PaneHeader's clock, the tree's and each resource list's Deleted open the place (the Resources drawer steps aside)."}
{"n": 137, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/Users.tsx, Publish.tsx, ImageBrowser.tsx, server/edit/styles.css", "summary": "Editors and Publish open in the drawer Resources uses; Resources takes the drawer's shape", "detail": "From the design session of 25 Sep 2026 with Peter, putting eta's bento rules (Workshop/eta/examples/bento/README.md) to the editor as 0.10.0 left it. The canvas is https://claude.ai/artifact/Y1QWMsHCw5X8rHJepmzD3c (private to Peter: 'duckdown bento'), each state drawn beside 0.10.0's own. What stays, and is the point: three columns with one job each (where you are, what you are changing, what a reader would see), the words and the page side by side at equal weight, front matter as text, the one slot beneath the page, every pane's header with its tools in 0.10.0's order and sizes, the open file's light in the tree. By the bento's rules a modal is for new or delete only. This item: Editors and Publish leave their modals for the right-hand drawer Resources already is, so the header's buttons all behave alike and the page stays in view. The drawer is inset in the tray (8px), 340px, 20px corners. Editors: each person a face and a row; choosing one shows their new-password field and Remove under them (Remove still confirms in a modal: delete is a modal's job); Add at the drawer's foot. Publish: what is waiting (path, new/changed/deleted), commits not pushed, the check's findings, then the message and Publish at the foot, Pull beside it. Resources: its four tabs as one segmented control, images as a grid of pictures with the chosen one ringed (as a work is), the folder as a small trail of its own, Copy Markdown and Upload as now. On a phone every drawer is a sheet from the foot and the page behind holds still. Least gain is Editors (consistency more than use). Canvas artboards Editors, Publish, Resources, PhoneSheet.", "note": "2026-09-25: fixed on claude/bento-editor. Drawer.tsx is the frame (inset in the tray, 20px corners, not modal: takes focus, Escape closes, focus given back); Drawers.tsx shows the one drawer the store names (drawer, toggleDrawer; showImages is now computed from it). EditorsDialog became EditorsDrawer (a face per editor, Add at the foot, Remove still a ConfirmDialog); PublishDialog became PublishDrawer (Publish and Pull at the foot). Resources: the tabs a segmented control, images a grid of 160px thumbnails with the chosen ringed, the images and reports folders a small trail (folderCrumbs()) in place of '..'. On a phone every drawer is a sheet from the foot."}
{"n": 138, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/components/CollectionPane.tsx, Drawer.tsx, Drawers.tsx, store.ts, app.tsx, styles.css", "summary": "A chosen work's properties open in a drawer from the left, not squeezed under the grid; drawers take the concentric radius", "detail": "n135 put the chosen work's fields beside the grid, and under it when the pane is narrow - and sharing the middle column with the page it always is, so the fields sat cramped beneath the tiles. Peter, 25 Sep: properties belong in a panel with room; not over the preview (it is what shows the change), so from the left, over the tree. And, looking at both drawers open: the drawers used --radius-panel (20px) a wall in from the tray's 22px corners - not concentric.", "note": "2026-09-25: store.ts leftDrawer (how to draw it, or null); Drawers.tsx LeftDrawer; app.tsx puts it in the tray beside Drawers; Drawer.tsx side=\"left\" (.sidebar-left) and onkey, so the pane's ⌘Z/⇧⌘Z answer from inside it. CollectionPane: a tile click chooses and opens (showing), the drawer shows the picture itself (pictureUrl, not the thumbnail; object-fit contain) with the fields and tools; the first work is still chosen as the pane opens but nothing covers the tree until a click; Escape closes, focus goes back to the tile, the choice stays; the drawer goes with the pane. Same width (340) and radius as the right-hand drawer; on a phone the same sheet from the foot. --radius-cell is now calc(var(--radius-tray) - var(--wall)) and .sidebar uses it (14px). Tests: the pane's tests mount LeftDrawer beside it and open the first work as a click would; one new test for the drawer (never by itself, a click, switching, ⌘Z inside, Escape, gone with the pane). 526 pass, 100%. Checked in the preview at desktop and 375."}
{"n": 139, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/store.ts, components/Preview.tsx, PreviewFrame.tsx, Browser.tsx, ImageBrowser.tsx, Publish.tsx, app.tsx, styles.css", "summary": "Opening a page says so in both columns; the preview no longer waits 300ms for a page just opened; the tree's '..' is back; Upload is a toolbar button in the images header; room round the chosen work's ring", "detail": "Peter, using the editor on vashti (a bucket, so every read is a round trip): after a click in the tree the middle column and the preview sat still. The chain was: the page's text from the bucket, then a 300ms debounce meant for typing, then the render, then the frame's own stylesheets and pictures. He also missed the tree's '..' (n134 took it out for the trail), found Upload at the foot of a long images listing, then that the header version was a label with none of a toolbar button's size, hover or title, and that the chosen tile's ring touched its label and an indented group started under the labels.", "note": "2026-09-25: store.opening (the path on its way, cleared either way); the middle column is aria-busy with a .loading-line and what it holds dimmed; Preview renders a newly opened page at once (debounce only for typing on the same page) and is busy from the click until the frame's load, or at once when the document is unchanged; the line keeps showing, unswept, under reduced motion. Browser: a '..' row first in every folder but the site's, named 'Up to <parent>' ('the site'), going there. ImageBrowser: Upload is a button.icon-btn beside New folder opening a .hidden-file input (a label styled as icon-btn got none of button.icon-btn's rules: 14x17, no hover). .item-tile gap 4 -> 8px (the ring reaches 4px out); a nested group gets 16px above it. Publish's badge counts defensively (a stray response in a test threw); the collection preview test's intercept answers only /edit/mark/. New test for the loading state and the immediate render. 527 pass, 100%."}
{"n": 140, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/styles.css, components/ConfirmDialog.tsx, Header.tsx, PreviewFrame.tsx", "summary": "The editor's consistency audit: controls swept in nine states, the stylesheet scanned for hand-written values; eleven found, all fixed", "detail": "After the Upload label that looked like a toolbar button and wasn't (n139), Peter asked how many other inconsistencies there were. The sweep measured every control (84 distinct) in the page+collection, work drawer, each Resources tab, Editors, a New dialog and Earlier versions: size, name, title, hover rule, radius, font, cursor. Clean on targets (none under 24px), names, titles, hover and cursor; heights keep one system (28 tabs, 30 toolbars, 34 dialog fields). Found: (1) the work drawer's Move up/down/Remove at 28px - the 'a size quieter' rule is for tools repeated on every group; (2) the group-name field 28px tall, 4px corners, against the fields' 6px. The stylesheet scan: (3-8) six hand-written radii (tree rows 4, thumbnails 3, notice 6, preview code 3, image preview 4, Publish badge 8); (9) 25 hand-written spacings against 61 token uses, several off the 4px scale (6px gaps in every button, row and header link; 2px, 3px, 14px, 20px, a -9px nudge); (10) inline styles in ConfirmDialog, Header's spacer and PreviewFrame's iframe; (11) the spacer done two ways (style=flex:1 and .pane-gap).", "note": "2026-09-25: new tokens --space-0-5 (2px), --space-1-5 (6px), --radius-pill; every radius and spacing now a token (3px corners and the notice's 14px left padding snapped to the scale; the dialog's 20px on a phone to --space-4); the pane header's divider centred with translate, not margin-top: -9px; .collection-chosen .item-tools is a bar's 30px; .group-name is --control-h with --radius; .dialog-message and .preview-frame for what was inline, and Header's spacer is .pane-gap. Left: CssPreview's sample page has inline widths - it is page content, not the editor. Not yet swept: the login page, the Publish drawer (no remote here), Deleted, the confirm dialog, and the editor at phone width."}
{"n": 141, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/help/*.md, server/routes/help.ts, server/main.ts, server/edit/components/Help.tsx, Header.tsx, Drawers.tsx, store.ts, Icon.tsx, styles.css", "summary": "Help: a drawer with a markdown cheat sheet and the editor's own ways, what fits what's open first", "detail": "Peter, 25 Sep: 'we don't even have an md cheatsheet'. Someone like Vashti had nothing in the editor to say that **bold** is bold, that a note can stand out, or what ⌘⏎ does; the seed's guide pages are content, so vashti hasn't got them. Chosen with Peter: the right drawer (like Resources), context first, a plain voice.", "note": "2026-09-25: eight pages in server/help/ (page, top, extras, collection, each, template, look, editor), shipped with duckdown. routes/help.ts (/edit/help, signed in) renders them; an `example` block is what you type over what readers get, through renderMarkdown, with headings unlinked and, in results, links as .help-link and pictures as their words (.help-picture). Help.tsx: helpFirst() (each: page > collection > template/css > page > editor), helpOrder() stable, first open, rest folded, reordered live. Found while checking it, at Peter's word: the example's ## rendered smaller than its ### (Help's label size caught it) - the labels are now .help-body > h2 and results keep their own scale; top.md gained feed: and x- keys. Also a twelfth inconsistency from the audit: three monospace stacks, now --font-mono. Tests: helpFirst/helpOrder, the drawer from the header reordering, the route's pairs and inert results, its cache. 531 pass, 100%. Checked at desktop and 375."}
{"n": 142, "status": "fixed", "severity": "low", "area": "site", "file": "server/base.ts, server/routes/site.ts, server/serve.ts, server/export.ts", "summary": "A phone's home-screen icon 404'd at the root under every name it asks for it by", "detail": "Seen in vashti's visitor report after Peter's mother put the site on her iPhone's home screen: /apple-touch-icon.png, -precomposed, -120x120(-precomposed), -240x240(-precomposed) all 404. vashti has static/apple-touch-icon.png and its templates link it, but iOS (and apps drawing link previews) also ask the root, and duckdown answered only robots.txt and favicon.ico there. (/about-us and /about/careers in the same report are bots probing, not a fault.)", "note": "2026-09-25: ROOT_FILES gains apple-touch-icon.png; rootFile() maps /^apple-touch-icon(-WxH)?(-precomposed)?\\.png$/ to it; the site route and serve.ts answer every such name with static/apple-touch-icon.png (dist/apple-touch-icon.png), a miss when a site has none; the export writes it and an -precomposed copy at the root. Tests on all three paths. Skill and CLAUDE.md say so."}
{"n": 143, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/icon.png, server/edit/index.html, server/edit/login.html, server/main.ts", "summary": "The editor has an icon: the example site's browser-window drawing, so its tab can be told apart from the site's", "detail": "Peter asked for tests/example/static/favicon.ico (a 1000px PNG under an .ico name) in the editor. It ships as server/edit/icon.png at 180px (5 KB). The editor page links ./icon.png, which Bun bundles and hashes as it does styles.css; an absolute href fails the bundle ('Could not resolve'). The login page isn't bundled, so it links /edit/icon.png, a route beside /edit/styles.css. It is black lines on transparent, so it is faint on a dark tab bar.", "note": "2026-09-25: done; test in server.test.ts; seen served in the preview."}
{"n": 144, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/base.ts, server/routes/browse.ts, server/help/", "summary": "A person in the editor can't set their site's icon: favicon.ico and apple-touch-icon.png must be in static/, and the editor only uploads to static/images/", "detail": "rootFile() and the export read the icons from static/ itself. The editor's Upload writes under static/images/ (IMAGES_PATH), and the css tab only makes stylesheets, so the only ways in are writing files (a session, or git on a published site) or the bucket directly. Vashti, on a bucket, has no way to do it. Help doesn't mention icons at all. Options: look for the root files in static/images/ as well as static/, so uploading an image with that name works; or a 'Site icon' drop target in Resources that writes both names at static/. Either way, add a line to Help (look.md or editor.md). Found when Peter asked how a user sets their icon, 2026-09-25.", "note": "2026-09-25: Resources → icon (SiteIcon.tsx) and /edit/site-icon (routes/site-icon.ts). The browser squares the picture on a canvas (Bun.Image can't crop, fill or read SVG): 180px on white, 48px as is; the route checks each is a PNG of its size, writes both or neither, keeps what it replaced in .history/static/. Found in the preview: an SVG sized in percent (the seed's logo, width=100%) says 150x150 and a square cut straight from the <img> came out a corner at 48px, so the whole picture is drawn to a canvas first and cut from that. Shown as a phone's home screen and a light and a dark tab bar. Checked at 1280, 375 and 320. Help (editor.md, look.md), the skill and CLAUDE.md say so."}
{"n": 145, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/routes/site-icon.ts, server/edit/components/SiteIcon.tsx", "summary": "The icon tab can set an icon readers never see: a template that links its own icon wins over /favicon.ico", "detail": "Nothing puts the icon in a page's head: browsers ask /favicon.ico and iPhones /apple-touch-icon.png at the root when a page names none, and duckdown answers both there. That covers the seed's templates and tony-site (no links) and vashti (links /favicon.ico and /static/apple-touch-icon.png, the same files). website's site.html links /static/images/mark.svg, /static/favicon.png and /static/images/mark-lg.png instead, so a picture chosen in Resources -> icon would change nothing a reader sees there. Fix: GET /edit/site-icon also reads the templates for <link rel=icon|apple-touch-icon> naming other files, and the tab says which template links what ('templates/site.html links its own icon, /static/images/mark.svg: this one is used only where no page names one'). Found when Peter asked whether the icon is set in every published page's header, 2026-09-25.", "note": "2026-09-25: GET /edit/site-icon answers elsewhere: [{template, icons}] from otherIcons() (rel icon, shortcut icon, apple-touch-icon[-precomposed]; not mask-icon; any attribute order or quoting) less ours() (favicon.ico and the touch icon's names, at the root, under static/ or on the site's own address). The tab says it in a note, built once since setting an icon changes no template. Seen in the preview with website's three links copied into a dev template: listed, and its /favicon.ico link left out. Released in 0.12.0."}
{"n": 146, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/edit/store.ts, server/edit/components/Header.tsx, server/edit/app.tsx, server/edit/styles.css", "summary": "On a phone the preview was gone: now the header's Preview lays it over the page, full height, and Edit takes it away", "detail": "Below 768px .column-preview was display:none, though still mounted and rendering. Peter asked for it as something shown over the content and hidden to edit; full height was his choice over a sheet like the drawers. previewShown in the store; the header's .preview-toggle (display:none above 768px) says Preview or Edit with an eye or a pencil, no aria-pressed since its name says what it does; app.tsx marks the tray .previewing and takes Escape. CSS: the preview spans the tray's rows, the tree and the middle stay pinned in theirs with visibility:hidden (so out of the tab order and the accessibility tree, text, caret and scroll kept). Showing it closes a drawer; opening a drawer puts it away. The extra button cut the trail's page name to 'i...' at 375, so the header's buttons are square (--icon-btn, no padding) below 420px, as every other icon button is: 'duckie > index.md' reads in full at 375; at 320 it is cut as it was before.", "note": "2026-09-25: done. Checked live at 375 (full height 56-796 of 812, unsaved edit in the preview, Escape back with text, caret and unsaved mark kept), 320 (no sideways scroll, Resources closes it, Preview closes Resources) and 768 (three columns, no button). Tests in editor.test.tsx (Header, the app)."}
{"n": 147, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/store.ts, server/edit/components/Browser.tsx, server/edit/components/PreviewFrame.tsx, server/routes/publish.ts, server/edit/components/Publish.tsx", "summary": "The editor's console is quiet: no 404 for a folder without a collection or a site without a remote, no 'Blocked script execution' per preview render", "detail": "Peter saw red in Chrome's console. Three sources, none a failure: (1) opening a folder's index.md fetched <folder>/collection.json to see if there was one, and most folders have none: now it asks the folder's listing, which always answers. (2) The preview's sandbox (no allow-scripts, on purpose) blocks a page's scripts and Chrome logs each one on every render: withoutScripts() in PreviewFrame takes them out first; they never ran there, so nothing seen changes. (3) GET /edit/publish was a 404 with no DUCKDOWN_REMOTE, which the Publish button read as 'hide me': it answers null now (POST stays 404). Found on the way: the server treats a name ending in .<letters> as a file, so a folder named v1.2 or .pane1 couldn't be listed without a trailing slash; folderUrl() in the store gives one, and the tree uses it too.", "note": "2026-09-25: done. A fresh tab walking /, gallery/ and blog/ and their index pages: nothing but Bun's HMR line in the console, no request over 400, the gallery's collection still offered, no Publish button."}
{"n": 148, "status": "fixed", "severity": "low", "area": "pages", "file": "server/markdown.ts, tests/markdown.test.ts", "summary": "The nav wrote a folder's title into its link unescaped", "detail": "buildNav() put meta.nav?.[0] || meta.title?.[0] into <a> as it was: title: A <b>bold</b> & co made markup of it. CLAUDE.md says every value is escaped, and nav.ts's listings and sitemap already escape theirs. Step 1 of the tidy from the cloud session's KISS/DRY/YAGNI review.", "note": "2026-09-25: escapeHtml(title), matching the other walks (the address stays encodeURI, as theirs does). Test in markdown.test.ts, failing before the fix. Every file of the seed's, website's, tony-site's and vashti's exports (DEBUG on and off) identical before and after: none of their nav titles has a character to escape."}
{"n": 149, "status": "fixed", "severity": "low", "area": "docs", "file": "feature.md (removed), create/setup.ts, tests/setup-script.test.ts, CLAUDE.md, CHANGELOG.md", "summary": "Doc drift: feature.md retired, CLAUDE.md's contradictions and stale lines, the 0.1.0 changelog's leftover theme section", "detail": "Step 5 of the tidy from the cloud session's review, done second. feature.md: todo.jsonl and the changelog replace it. Its eleven unticked boxes, so nothing is lost: done since - Help link (0.11.0 Help), Loading spinner states and Loading states (0.11.0), S3 integration tests (s3.test.ts against fake-s3.ts); ideas never decided on, left as ideas - custom scripts via x-script-* metadata (a template's <script> does it), maths and highlighted code (n27: decided against, the template is the way), emoji (twemoji), a separate image bucket, subdirectory isolation per user, a code editor with syntax highlighting, a production build (bun build --target=bun). CLAUDE.md: said both that the tree has no .. and that its first row is .. (0.11.0 brought it back): the first is gone. bun:sqlite as available for future use: nothing uses it, gone. CHANGELOG 0.1.0: its theme: section still said a -theme.css styles its folder, full stop, while the section above said -theme.css was gone; v0.1.0's code has neither, so the section now says what shipped. Other releases untouched.", "note": "2026-09-25: done. Read-through by machine rather than eye: every backticked function, CONSTANT, camelCase name, CSS token and file in CLAUDE.md checked against server/, tests/ and create/ - all 466 exist (site-content names like index.md aside). Every script under Running is in package.json. Found and fixed: tests/search-js.test.ts missing from the tree; Drawers.tsx and the bento paragraph naming Resources, Editors and Publish without Help. Left alone: the Architecture sample of main.ts shows some routes, not all - it says what main.ts is, not a list to keep in step; the 0.0.2 changelog line about feature.md is history."}
{"n": 150, "status": "fixed", "severity": "low", "area": "server", "file": "server/kept.ts (new), server/nav.ts, server/search.ts, server/collection.ts, server/feed.ts, server/routes/pages.ts, server/routes/publish.ts, tests/*", "summary": "One cache helper, one siteChanged(): six copies of keep-a-promise, drop-on-failure, bypass-under-DEBUG, cleared by four functions", "detail": "Step 2 of the tidy from the cloud session's review. nav.ts (nav, listings, map), search.ts (built), collection.ts (loaded) and feed.ts (kept) each wrote the same pattern, cleared by pagesChanged, searchChanged, collectionsChanged and feedsChanged, which the pages route called in a row through changed() (the publish route's pull used that). tests/helpers.ts keepSite() called three of the four and forgot feedsChanged: the slip this removes.", "note": "2026-09-25: server/kept.ts: kept(build, dropped?) returns (pages, key, debug) keyed by folder or \"\"; every cache registers, siteChanged() drops them all. collection.ts's warned set clears with its cache (the dropped hook). The four exported clears and pages.ts's changed() are gone; the pages route passes siteChanged to fileRoutes and the pull calls it; tests call it. One improvement: a failed build that finishes after siteChanged() used to delete the newer build made since; now it only deletes itself. Direct tests in units.test.ts (per key, after siteChanged, DEBUG bypass untouched by it, a late failure, a failure rebuilt). The public readers (siteNav, folderListing, siteMap, loadCollection, searchIndex...) keep their signatures. Exports of the seed, website, tony-site and vashti, DEBUG on and off: every file identical to before the tidy. CLAUDE.md's cache paragraphs and the tree say kept.ts."}
{"n": 151, "status": "fixed", "severity": "low", "area": "server", "file": "server/storage.ts, server/history.ts, server/collection.ts, server/search.ts, server/nav.ts, server/markdown.ts, server/export.ts, server/edit/components/Browser.tsx, ImageBrowser.tsx, ReportList.tsx, tests/*", "summary": "A listing's path is the key, ready to use: the leading slash every caller stripped is gone", "detail": "Step 4 of the tidy from the cloud session's review. Storage.list() gave each entry a path starting with /, and callers took it off with .replace(/^\\//, \"\") - 24 times in 13 files by the review's count. 19 of those were entries; the other 5 strip a URL or an argument for their own reasons (site.ts's request path, links.ts's page address, the S3 key builder, and loadFile()/createFile() in the store).", "note": "2026-09-25: changed path's shape rather than adding a key field beside it: one field, not two saying the same, and its only other reader is the editor, which ships with the server (vashti's usage script lists reports through duckdown's storage but reads only name). LocalStorage and S3Storage return the key; the 19 strips are gone; the editor's two URLs built on the slash (a picture's thumbnail, a report's link) add their own, and come out as before. Left alone: the 5 that aren't entries - loadFile() also takes the address bar's ?path=, where a slash may be typed, and a test says so. Tests: the three in-memory fakes and the storage tests say the new shape. Exports of the seed, website, tony-site and vashti, DEBUG on and off: identical to before the tidy. In the preview: the tree opens guide/pages.md and marks it, thumbnails load at the same addresses (a subfolder's too), a report's link answers 200."}
{"n": 152, "status": "fixed", "severity": "low", "area": "server", "file": "server/listed.ts (new), server/markdown.ts, server/nav.ts, server/search.ts, server/export.ts, server/routes/site.ts, tests/*", "summary": "One home for what counts as a page: listed.ts, asked by every walk of pages/", "detail": "Step 3 of the tidy from the cloud session's review. The rules each walk applied, before: buildNav (markdown.ts) - folders: skip . and -, order by order:; files: only index.md, needs nav/title, skips drafts, did NOT check each:. folderEntries (nav.ts; {{pages}} and the feed) - .md, not index.md, not a - file, not 404.md, not a draft, not an each: page; folders not walked. buildSiteMap (nav.ts; {{sitemap}}) - folderEntries per folder, folders skip . and -, order by order:, a draft index labels its folder by name. buildSite (search.ts; search.json, sitemap.xml, page list, aliases) - .md, not 404.md, not a draft, not an each: page; folders skip . only, sorted by name; INCLUDED - files and - folders. walk (export.ts) - every key, - included on purpose (served), and . names too when a bucket lists them (disk never does); drafts and each: pages left out later, at render. The folder filter !startsWith(\".\") && !startsWith(\"-\") was pasted in nav.ts and markdown.ts.", "note": "2026-09-25: server/listed.ts: NOT_FOUND (moved from search.ts), hidden() (a . name), unlisted() (hidden or -), pageKey() (markdown, not 404.md), readable() (not a draft, not an each: page). Every walk asks them; order and what each does with the answer stay its own. Where they disagreed by accident, the answer taken: (1) search.json and sitemap.xml included - files and folders, which the skill calls served-but-unlisted (\"left out of the navigation; still served to anyone with the URL\") and the nav, {{pages}} and {{sitemap}} leave out - a search result or a sitemap line hands the address out, so they are out of both now; (2) the nav would list an index.md saying each: true, whose address the site route answers as a miss - now it doesn't; (3) the export published a . name when a bucket listed one, which a disk never does - now neither does. Deliberate differences kept: the export publishes - names; only folders' indexes are in the nav; each output's own order. None of the three touches the seed, website, tony-site or vashti (no - or . content, no each: index): their exports, DEBUG on and off, are identical to before the tidy. A test for each, failing against the old walks. The skill (SKILL.md, reference.md: - names, Search) and CLAUDE.md say so."}
{"n": 153, "status": "fixed", "severity": "medium", "area": "cli", "file": "server/upgrade.ts (new), server/cli.ts, server/init.ts, tests/upgrade.test.ts, README.md, .claude/skills/duckdown/reference.md, CLAUDE.md", "summary": "duckdown upgrade: the upgrade done by hand for every release, as one command", "detail": "Peter asked for it after my review named the upgrade as the cost he pays every release: today 0.11.0 to 0.12.2 meant, for each of three sites, pull, re-pin, install, refresh the skill, export before and after, compare, commit. The command does all but the pull (site-specific: vashti's goes through railway run) and the commit.", "note": "2026-09-25: bunx duckdown upgrade [tag]: the newest vX.Y.Z tag from git ls-remote, or the one named (with or without v); exports with the installed version into a scratch folder, re-pins in dependencies or devDependencies (website keeps it in devDependencies, which the first real run found: init accepts either), bun install, checks the installed version, refreshes .claude/skills/duckdown when the site has one, exports again, compares every file (same, added, gone, changed), prints the changelog entries after the old version up to the new. Won't install or can't export: package.json and bun.lock put back and reinstalled. Commits nothing. Tests with git, install and export stood in (no network), plus cli(). Real run on website: 0.12.2 down to 0.12.1 and back up to the newest, real tags, real installs, real exports: all 81 files the same both ways, 0.12.2's notes printed, the folder left exactly as it was. README, the scaffold's CLAUDE.md, the skill (a section of its own) and CLAUDE.md say so."}
{"n": 154, "status": "fixed", "severity": "medium", "area": "editor", "file": "server/icons.ts (new), server/page.ts, server/routes/site-icon.ts, server/edit/components/SiteIcon.tsx, server/edit/styles.css, tests/*, docs", "summary": "Every shared link gets a picture: a sharing card in the icon tab, and the icon when there is none", "detail": "Peter: extend the icon tab for social media. Before, a page's link preview had a picture only when it named image:, so most pages on all three sites shared none.", "note": "2026-09-25: icons.ts: ICONS, CARD (card.jpg, 1200x630, JPEG since a card is often a photograph), sitePicture() kept like the nav: the card, else the home-screen icon, else nothing. pageHtml: a page with no image: shares it, with og:image:width/height (known) and twitter:card summary_large_image for the card, summary for the icon; a page's own image: is unchanged and says no size. Route: GET adds card and home (the front page's title and description, for the preview); POST ?card checks a 1200x630 JPEG; DELETE ?card removes it (kept in history); both call siteChanged(). Tab: a Sharing section drawn as a link preview (card over host, title, description; the icon small beside them with no card), Choose a picture... (cardJpeg: the middle of the picture at the card's shape, on white), Make one from the icon (iconCard: the icon at 60% of the height, centred on white), Remove. The two choosers named for what each is for (found in the screenshot: both said Choose a picture...). Exports: seed and website unchanged; vashti's 18 and tony-site's 5 pages without image: now share the icon - four added lines each, nothing else. Checked in the preview: a card made from the icon, the tab at 375."}
{"n": 155, "status": "fixed", "severity": "low", "area": "deps", "file": "package.json, bun.lock, .claude/skills/railroad/, .claude/skills/bun-route/, CLAUDE.md", "summary": "railroad 0.11.0 -> 0.14.0: nothing in the editor needed changing", "detail": "Peter: we've done a railroad update, can you check? Three releases since 0.11.0, each with breaking changes: 0.12 (an effect's run owns what its body creates; when()/list() render in mount(); function props reactive), 0.13 (render bodies untracked; an effect's first-run writes deferred; props after children; a boolean child renders nothing; patch and params$ types narrower), 0.14 (no global JSX namespace; when()'s truthy branch is handed v$, including one passed by reference).", "note": "2026-09-25: bumped to ^0.14.0. bun run check clean and all 571 tests pass at 100% with no change to the code. Read for what tests might miss: none of the editor's 17 effects creates a computed, map, when, list or component in its body (Help builds plain DOM); none reads back something derived from its own write; no JSX. annotations; no boolean shown as a child; the one when() given a branch by reference (CollectionPane's chosenPanel) takes no argument, so v$ is ignored. In the browser on 0.14, a fresh tab: the tree, a page and its live preview while typing, the gallery's collection and a work in the left drawer, every Resources tab, Help, Editors, an earlier version (read-only, Restore, rendered) and back, the phone preview toggle - and nothing in the console. The railroad and bun-route skills refreshed from the package (railroad's now ships reference.md). CLAUDE.md's railroad notes: the version, when() handing v$, reactive function props, boolean children, effects owning their run, untracked bodies, and when()/list() rendering in mount() (the microtask warning retired). | 2026-09-25, correction: that browser walk ran on a dev server started before the bun install, and its bundler was still serving railroad 0.11 (found when a when() branch was handed no value). Repeated on a restarted server whose bundle carries 0.13's second-copy check: the same walk, plus the sharing card's new button, all as before, and nothing in the console. Lesson: restart the dev server after changing a client dependency, before checking it in the browser."}
{"n": 156, "status": "fixed", "severity": "medium", "area": "cli", "file": "server/upgrade.ts", "summary": "duckdown upgrade compares the export as it runs here: without DUCKDOWN_ORIGIN, what only an origin writes is never compared", "detail": "Its first real job, 0.12.2 to 0.13.0 on the three sites (2026-09-25), reported all files the same on vashti and tony-site, though 0.13.0 adds og:image lines to their pages without image:. Neither sets DUCKDOWN_ORIGIN locally, and without an origin an export writes no absolute addresses: no og:url or og:image, no sitemap.xml, no feed.xml. So the comparison is blind to exactly the things a platform's build (with an origin) publishes. Fix: when the environment has no DUCKDOWN_ORIGIN, export both times with a stand-in (e.g. https://example.invalid) so those are written and compared, and say that it did; or say plainly that they weren't compared.", "note": "2026-09-25: found on the command's first real run; not started. | 2026-09-25: fixed. When the environment has no DUCKDOWN_ORIGIN (the site's .env included, which bun loads for both the command and its exports), both exports run with STAND_IN (https://example.invalid, a reserved name) passed through Run's new env (added to the environment, not in place of it), and the report says so under its summary; a site with its own origin is left to it. Tests: the stand-in reaches both exports and an origin-only file is compared; a site's own origin is left alone; spawnRun adds to the environment. Real run on vashti (no origin locally): 0.13.0 down to 0.12.2 and back, 18 pages changed each way - exactly the og:image lines 0.13.0 added, which the first run called all the same - and the folder left as it was. Released in 0.13.1."}
{"n": 157, "status": "fixed", "severity": "low", "area": "editor", "file": "server/edit/components/SiteIcon.tsx", "summary": "The sharing card crops a square logo to its middle strip: offer fit-whole as well as fill", "detail": "blueshed.co.uk's first card (2026-09-25) was the round mark chosen with Choose a picture...: cardJpeg() fills 1200x630 and cuts what doesn't fit, so the circle lost its top and bottom and the tree its trunk. Right for a photograph, wrong for a logo or anything near square. Options: when the picture's shape is far from 1.91:1, draw it whole on white (as iconCard does) and say so; or a choice beside the preview (fill / whole). The preview showed it before publishing, which is what it is for - but the default could be kinder.", "note": "2026-09-25: filed from the live card; Peter pointed at share.png (already 1200x630) or Make one from the icon. | 2026-09-25: fixed. cardFit(w, h): fill when the ratio is 1.5-2.5, else whole; cardJpeg(file, fit?) draws whole inside 90% of the width and 80% of the height on white, and says which it did. The tab keeps the picture just chosen and offers the other (Show it whole / Fill the card, through railroad 0.14's when() value); making one from the icon or removing the card forgets it. Seen in the preview: the seed's square duck came out whole and centred."}
{"n": 158, "status": "fixed", "severity": "medium", "area": "report", "file": "server/report.ts, server/log.ts", "summary": "duckdown report counts vulnerability scanners as readers: 939 of blueshed.co.uk's 993 views in a day were 404s for /.env, /.git/config, /phpinfo.php", "detail": "A view is a crawler only when its user agent says so (CRAWLER in log.ts), and scanners pose as browsers. So the first real report (website, 2026-09-25, 993 view lines over every deployment of the last day) said 931 readers, 939 not found; the readers who read something were about 35. Separate probes as their own line: a request for a path no site of this kind serves (a dot-file, .php, /wp-*, /.git, /cgi-bin, /actuator...) - vashti's scripts/usage.ts has a PROBE pattern for exactly this - counted as 'probes', left out of readers and out of the Not found table (which then says what readers missed). The view log itself stays as it is: what was asked for, never who.", "note": "2026-09-25: found on website's first report. | 2026-09-25: fixed. PROBES in report.ts, a short list with what each catches: dot-files or folders spelled or %2e-encoded (not /.well-known/), an encoded slash, PHP, other stacks' insides (wp-, cgi-bin, actuator, server-status, debug/...), secrets and configs by name (env, credentials, Dockerfile, appsettings, id_rsa...), config, backup and secret extensions, and scripts, data, source and keys outside static/ (search.json, sitemap.xml and feeds excepted). reportMarkdown counts them as Probes by scanners with a line saying what they are, and leaves them out of readers, crawlers, Not found and errors. Tuned on website's real day of log (993 views): 925 probes, 14 real misses left (the icons from before they existed, /edit, /feed/, /llms.txt and a few stray probes); no probe the site answered, and none of the three sites' own addresses counts as one. | 2026-09-25: a week of website's log (49 deployments, via its new bun run report) showed three more: /api, /@fs and /_ignition families (added: a site like this has no /api and nothing at the root starting _ or @); a certificate authority's /.well-known/acme-challenge/ checks, 263 on the 22nd, counted as readers and as Not found (now a crawler's view, and not a miss); and pictures, stylesheets and search.js counted as pages read (FILE: counted apart, out of the views and Most read, a missing one still Not found). The 22nd went from 444 readers and 343 misses to 168 and 80."}
{"n": 159, "status": "fixed", "severity": "low", "area": "report", "file": "server/report.ts", "summary": "duckdown report writes one file named for the day it runs, whatever days the lines cover", "detail": "The same first run: a day of Railway log, 2026-09-24T15:01 to 2026-09-25T14:26, all went into reports/2026-09/2026-09-25.md, titled 25 September. Each view line has its time (at), so the report could write one file per UTC day the lines cover, each replaced by a later run that covers that day, and say which it wrote. A week's log would then make seven day reports rather than one mislabelled one.", "note": "2026-09-25: found on website's first report. | 2026-09-25: fixed. dayOf(at): the UTC day of a line's own time, reading Railway's nanoseconds and a space for the T; untimed lines are today's. reportCommand writes a report per day, in order, each titled for its day, a day already there replaced, and says each file it wrote."}
{"n": 160, "status": "fixed", "severity": "low", "area": "report", "file": "server/report.ts, tests/report.test.ts", "summary": "duckdown report writes a month page: reports/<month>/index.md, each day linked, the month summed", "detail": "Peter: there is no index.md in my month report folder. vashti's own usage script writes one; duckdown report wrote only days.", "note": "2026-09-25: countViews() gives a day's whole counts, written as <day>.json beside <day>.md; after each run, every month touched is made again from all its days' .json by monthMarkdown(): a line per day linked to its report (readers, crawlers, not found, probes), then the month's totals, most read, not found and referring sites. A month keeps no count of its own, so a day reported again replaces its counts rather than adding. The Reports tab lists only .md, so the .json stay out of sight, and index.md sorts first. Tried on website's real week (6 days, 20-25 September): the month page renders in the Reports tab's own tab and each day's link opens its report."}
{"n": 161, "status": "open", "severity": "design", "area": "release", "file": "CHANGELOG.md, README.md", "summary": "A stability promise: duckdown 1.0, after which nothing a site depends on breaks without a major version", "detail": "From the review of 2026-09-25: 0.11.0 to 0.13.4 in a day, and the 0.4 to 0.8 collection shape, are fine for three sites the author keeps, a deterrent for anyone else. 1.0 would say what is promised - front matter keys, placeholders, template names, the collection.json shape, the CLI, the folder layout - and that breaking any of it is 2.0 with an upgrade note. duckdown upgrade already compares a site's export across versions, which is the enforcement tool. NEEDS PETER: whether and when; it is a commitment, not a change.", "note": "2026-09-25: filed from the review (it was only in a reply until Peter said the ledger is where it belongs)."}
{"n": 162, "status": "open", "severity": "medium", "area": "images", "file": "server/collection.ts, server/routes/collection.ts, server/markdown.ts, server/export.ts", "summary": "Pictures for the screen they're on: a srcset of sizes instead of one original for every reader", "detail": "From the review of 2026-09-25: a collection has a 128px thumbnail and the original, nothing between; a markdown picture is the file as uploaded. On tony-site (442 works) a phone downloads originals meant for a desktop. Make widths (e.g. 480, 960, 1600) when a picture is uploaded (the collection route already makes a thumbnail with Bun.Image) and at export for pictures that have none, and write <img srcset sizes> for an item's picture and for markdown images under static/images/. Keep the original as the largest; never enlarge; leave SVG alone.", "note": "2026-09-25: filed from the review."}
{"n": 163, "status": "fixed", "severity": "medium", "area": "scale", "file": "tests/", "summary": "Find where duckdown slows: a site of thousands of pages, measured", "detail": "From the review of 2026-09-25: the site's caches rebuild whole walks on any change (kept.ts), the nav and the index are one walk each, and every page render reads the template. tony-site at 901 exported files is fine; nothing says what 5,000 or 20,000 pages cost. Generate a large site in a scratch folder (pages across folders, a big collection), measure a cold build of the nav, index and listings, one page render, a save (siteChanged + the next render) and a full export, and write down the numbers and where the time goes. Fix what is cheap; file what isn't.", "note": "2026-09-25: filed from the review. | 2026-09-25: measured, with bench/ (bun run bench [pages folders works]), which makes a site in a scratch folder and times it in a fresh process. Pages carry two sections of ~1,500 characters; every folder lists its pages with a feed; the front page has {{sitemap}}. At 5,000 pages, 50 folders, 2,000 works: nav cold 62 ms, {{sitemap}} 249 ms, search index 361 ms, a page's first render 11 ms then 0, after a save the next render 67 ms and the index 334 ms again, the whole export 3.1 s. At 20,000 pages, 100 folders, 5,000 works: 224 ms, 946 ms, 1.3 s, 16 ms, after a save 242 ms, export 11.8 s - four times the site, about four times the time: linear, no cliff, and a save costs a quarter of a second at that size. Nothing cheap to fix in time. Two things don't scale, filed: search.json's size (n166) and the export's memory (n167)."}
{"n": 164, "status": "fixed", "severity": "low", "area": "pages", "file": "server/page.ts", "summary": "A shared card has no og:image:alt: say what the picture is", "detail": "blueshed.co.uk's template carried og:image:alt until 0.13.0 took over its card, and duckdown writes none. For the site's own picture (card or icon) the alt could be the front page's title; for a page's own image:, its title. Offered to Peter on 2026-09-25 when he removed the template's tags.", "note": "2026-09-25: filed. | 2026-09-25: fixed. og:image:alt after the picture's size: the site's card or icon is of the site, named by the front page's title (sitePicture() finds it with the picture, cached alike); a page's own image: is of the page, its title. Left out when there is no title to say. Exports: every page that shares a picture gains exactly that line - tony-site 447 files, vashti 18, the seed 4 - and nothing else."}
{"n": 165, "status": "fixed", "severity": "low", "area": "report", "file": "server/report.ts", "summary": "The report's totals table renders with an empty header row", "detail": "The totals are written as a two-column table whose header is '| | |', which renders as a blank grey row above the numbers (seen in the month page's screenshot, 2026-09-25). Give it words (| What | Count |) or write the totals as a list.", "note": "2026-09-25: filed. | 2026-09-25: fixed: the totals table's header says | In the log | Count |."}
{"n": 166, "status": "open", "severity": "medium", "area": "search", "file": "server/search.ts, server/base/search.js", "summary": "search.json grows with every word on the site: 68 MB at 20,000 pages, all of it downloaded before a reader's first search", "detail": "Measured by bench (n163): an entry per section carries that section's whole text, because search.js shows a snippet from it and builds the :~:text= link from it. The sites gzip it at Railway's edge (seen 2026-09-25), but real prose compresses about 4x, not the bench's 50x, so a few thousand pages means several MB before the first result. Fine for the three sites today (tens to hundreds of pages). Options: cap each section's text (a snippet needs the first few hundred characters; recall falls for long sections), keep a word list for matching beside a short snippet, split the index by folder and fetch what a search needs, or answer search on the server for the served flavour. A design choice: NEEDS PETER once a site gets near it.", "note": "2026-09-25: filed from the bench."}
{"n": 167, "status": "open", "severity": "low", "area": "export", "file": "server/export.ts", "summary": "The export holds every rendered page in memory before it writes: 2.7 GB at 20,000 pages", "detail": "Measured by bench (n163): peak 2.7 GB for the run with the export, 620 MB without it. The export renders everything before it deletes dist/, so that a failure never replaces a good site with a broken one - which is right, but it needn't hold the pages in memory to do it: render into a folder beside dist/ as it goes and swap the two when it is done (a rename), keeping the same promise at a fraction of the memory. A platform's build machine is where this would bite (Railway's builders have limited memory). Not urgent at today's sizes (tony-site's 901 files export in a few seconds).", "note": "2026-09-25: filed from the bench."}
